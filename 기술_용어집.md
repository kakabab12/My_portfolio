# 기술 용어집

README의 기술 배지를 누르면 여기로 온다. **각 기술이 무엇이고, 이 저장소에서
어디에 왜 썼는지**를 적었다. 일반적인 소개가 아니라 **내가 실제로 쓴 맥락**이
중심이다.

---

## 언어

### <a name="python"></a>Python

주력 언어. 이 저장소의 거의 모든 프로젝트가 파이썬이다.

**어디에 썼나** — 헤드트래커·비전 파이프라인·ROS 2 노드·머신러닝 학습·웹 서버·
윈도우 유틸리티까지 전부.

**이 프로젝트에서 특이한 점** — 광명테크 인턴 프로젝트는 **3.11.5로 버전을
고정**했다. 여러 PC에 배포해야 하는데 mediapipe·opencv의 버전 궁합이 까다로워,
`requirements.txt`에 정확한 버전을 박아 재현성을 확보했다.

### <a name="cpp"></a>C / C++

**어디에 썼나** — Arduino(분류기 제어), ESP32(자이로 장갑 펌웨어).

**구체적으로** — TurtleBot3 프로젝트의 자이로 장갑은 ESP32 + MPU6050 센서
펌웨어를 직접 작성했다. 기울기를 읽어 Wi-Fi로 로봇에 보낸다.

### <a name="javascript"></a>JavaScript

**어디에 썼나** — 웹 대시보드(실시간 스트리밍·차트), Express 서버,
roslib으로 브라우저에서 ROS 토픽 직접 발행.

---

## 비전 · AI

### <a name="mediapipe"></a>MediaPipe

구글이 만든 **실시간 인식 라이브러리**. 얼굴 468점, 손 21점 같은 landmark를
CPU만으로 실시간 추출한다.

**왜 골랐나** — 라이선스가 **Apache-2.0**이라 상업적 사용에 제약이 없다.
초기에 쓰던 라이브러리가 AGPL이라 회사 제품에 못 넣는 문제가 있어 갈아탔다.

**어디에 썼나**

| 프로젝트 | 쓴 모듈 | 용도 |
|---|---|---|
| [광명테크 키오스크](2026/01.%20광명테크%20인턴%20-%20배리어프리%20키오스크/) | FaceLandmarker, HandLandmarker | 고개·입으로 커서 제어, 손 제스처 |
| [웹캠 로봇팔](2026/05.%20웹캠%20로봇팔%20원격조종/) | Pose, Hand | 팔 관절각 추출 |
| [TurtleBot3](2026/02.%20SW파일럿%20-%20TurtleBot3%20자율주행%20로봇/) | HandLandmarker | 손 제스처로 로봇 조종 |

**직접 파고든 부분** — 랜드마크 좌표뿐 아니라 **blendshape**(입 벌림·눈 감김
점수)과 **facial transformation matrix**(머리의 3D 자세 4×4 행렬)까지 꺼내 썼다.
후자는 2D 투영의 원근 왜곡 문제를 근본적으로 없애기 위해 도입했다.

### <a name="yolo"></a>YOLO / YOLOv8-seg

실시간 **객체 탐지** 모델. `-seg`는 픽셀 단위 윤곽(세그멘테이션)까지 준다.

**어디에 썼나**

| 프로젝트 | 용도 |
|---|---|
| [라즈베리파이 선별](2025/01.%20캡스톤%20-%20라즈베리파이%20선별시스템/) | 물체의 **마스크 면적으로 크기**, **사각형 비율로 불량**을 판정 |
| [캡스톤 스마트팩토리](2026/03.%20캡스톤%20-%20스마트팩토리%20비전검사/) | 박스 분류 + 무게 숫자 인식 (모델 2개 병렬) |

**왜 seg를 썼나** — 단순 박스(bbox)로는 물체의 실제 크기를 알기 어렵다.
세그멘테이션 마스크의 **면적**을 쓰면 크기 판정이 훨씬 정확하다.

### <a name="opencv"></a>OpenCV

영상 처리 표준 라이브러리. 카메라 입출력·이미지 변환·그리기 전반.

**이 저장소에서 특히** — CLAHE(저조도 보정), 색공간 변환(BGR↔LAB),
오버레이 렌더링, 카메라 백엔드 제어(MSMF/DShow)에 썼다.

### <a name="lightgbm"></a>LightGBM · <a name="xgboost"></a>XGBoost

**그래디언트 부스팅** 계열 머신러닝 모델. 표 형태(tabular) 데이터에서
딥러닝보다 잘 맞는 경우가 많고 학습이 빠르다.

**어디에 썼나** — [메디컬 머신러닝 대회](2026/04.%20메디컬%20머신러닝%20대회/).
수술 전 데이터로 **ICU 입원 위험도(분류)** 와 **수술 소요시간(회귀)** 을
동시에 예측했다. 처음엔 둘 다 LightGBM이었는데, ICU 분류만 XGBoost로 바꿔
하이브리드로 운영했다.

### <a name="shap"></a>SHAP

**설명가능 AI(XAI)** 기법. 예측값에 각 변수가 얼마나 기여했는지 게임이론
(Shapley value) 기반으로 계산한다.

**왜 필요했나** — 의료 현장에서 "AI가 그렇다니까"는 통하지 않는다.
"이 환자는 **왜** 수술이 오래 걸리나"를 의료진에게 설명해야 한다.

**어떻게 썼나** — 전체 변수 중요도(Summary Plot)와 **개별 환자별 폭포수
차트(Waterfall)** 두 가지를 만들었다. 후자가 실제 진료에 쓸모 있는 쪽이다.

---

## 추론 최적화

> 세 가지 모두 **"같은 모델을 더 빠르게 돌리는"** 도구다. 이 저장소의 프로젝트는
> 대부분 그래픽카드가 없거나 약한 환경(키오스크·라즈베리파이·젯슨)이라
> 최적화가 선택이 아니라 필수였다.

### <a name="tensorrt"></a>TensorRT

NVIDIA GPU 전용 **추론 가속 엔진**. 학습된 모델을 그 GPU에 맞게 다시
컴파일해서 훨씬 빠르게 돌린다.

**핵심 개념 — FP16 양자화**
모델 내부 계산을 32비트 실수 대신 **16비트**로 한다. 정밀도를 조금 내주고
속도와 메모리를 크게 얻는다. 인식 정확도 손실은 대개 무시할 수준이다.

**어디에 썼나** — [캡스톤 스마트팩토리](2026/03.%20캡스톤%20-%20스마트팩토리%20비전검사/).
`turnonnx.py`가 PyTorch(`.pt`) → TensorRT(`.engine`) 변환을 담당하고,
`half=True`로 FP16을 켠다.

**주의할 점 (실제로 겪음)** — `.engine` 파일은 **빌드한 그 장비에서만** 돈다.
GPU 모델이나 드라이버가 다르면 못 쓴다. 그래서 저장소에 넣어도 다른 PC에선
다시 만들어야 한다.

### <a name="openvino"></a>OpenVINO

인텔이 만든 추론 가속 툴킷. **인텔 CPU와 내장그래픽(iGPU)** 을 함께 굴린다.

**왜 썼나** — 정부 민원발급기(키오스크)에는 **별도 그래픽카드가 없다.**
인텔 칩만 있는 환경에서 최대한 성능을 뽑아야 했다. NVIDIA용인 TensorRT는
애초에 쓸 수 없는 상황이었다.

**어디에 썼나** — [광명테크 인턴](2026/01.%20광명테크%20인턴%20-%20배리어프리%20키오스크/)의
`주 목표/OpenVINO.ver`. ONNX Runtime에서 이쪽으로 갈아탔다.

### <a name="pytorch"></a>PyTorch

딥러닝 **학습·연구의 사실상 표준** 프레임워크. 모델을 만들고 학습시키는 단계에서 쓴다.

**어디에 썼나**

| 프로젝트 | 용도 |
|---|---|
| [라즈베리파이 선별](2025/01.%20캡스톤%20-%20라즈베리파이%20선별시스템/) | 모델 점검·정리 (`checkt.py`, `clean_model.py`), YOLO 학습 |
| [캡스톤 스마트팩토리](2026/03.%20캡스톤%20-%20스마트팩토리%20비전검사/) | 메인 서버의 모델 로딩·GPU 가용성 확인 (`jetson_USB2.py`, `testgpu.py`) |

**중요한 구분 — 학습과 배포는 다르다**
PyTorch는 **학습·개발**에는 좋지만 무겁다. 그래서 이 저장소의 프로젝트들은
학습만 PyTorch로 하고, **실제 배포에는 가볍고 빠른 형식으로 변환**해서 쓴다.

```
PyTorch(.pt)  →  ONNX(.onnx)  →  TensorRT(.engine)
   학습·개발      공통 형식·CPU      NVIDIA GPU 가속
```

> ⚠ [광명테크 최종본](2026/01.%20광명테크%20인턴%20-%20배리어프리%20키오스크/리모콘%20ui/)에는
> **PyTorch가 없다.** 초기엔 있었지만 최종적으로 MediaPipe 단독 구성이 되며 빠졌다
> (아래 ONNX Runtime 항목의 라이선스 이야기 참고).

### <a name="onnx"></a>ONNX / ONNX Runtime

**ONNX**는 딥러닝 모델의 **공통 저장 형식**이다. PyTorch에서 만든 모델을 ONNX로
바꿔 두면 특정 프레임워크에 묶이지 않고 여러 환경에서 돌릴 수 있다.
**ONNX Runtime**은 그 ONNX 모델을 실행하는 엔진이다.

**왜 쓰나** — PyTorch를 그대로 배포하면 무겁고 느리다. ONNX로 바꾸면
**PyTorch 없이도** 추론이 되고, CPU 최적화가 들어가 훨씬 빠르다.

**어디에 썼나**

| 프로젝트 | 시기 | 용도 |
|---|:-:|---|
| ⭐ [**라즈베리파이 선별**](2025/01.%20캡스톤%20-%20라즈베리파이%20선별시스템/) | `2025` | **주 추론 경로.** YOLOv8-seg를 ONNX로 변환해 **GPU 없는 라즈베리파이에서 CPU만으로** 돌렸다 |
| [캡스톤](2026/03.%20캡스톤%20-%20스마트팩토리%20비전검사/) | `2026` | TensorRT로 넘어가기 전 중간 단계 (`jetson_main1(2).py` 등 초기판) |
| 광명테크 초기판 | `2026` | 손끝 추적 엔진 (rtmlib + ONNX Runtime) |

**2025년 라즈베리파이에서 구체적으로 어떻게 썼나**

```python
# onxxtst.py — 변환된 모델이 제대로 되었는지 직접 검증
import onnxruntime as ort
session = ort.InferenceSession("yolov5/best.onnx")
out = session.run(None, {input_name: img})     # 입출력 형태 확인

# app_ck.py:37 — 실제 서비스에서 ONNX 모델 로딩
model = YOLO('yolov5/best.onnx', task='segment')
```

`.pt`를 그대로 쓰지 않고 **ONNX로 바꾼 이유**는 라즈베리파이에 GPU가 없기
때문이다. PyTorch를 통째로 올리면 무겁고 느린데, ONNX Runtime은 CPU 추론에
최적화돼 있어 같은 모델이 훨씬 잘 돈다.

**라이선스 때문에 갈아탄 이야기** — 광명테크 프로젝트는 처음에 **AGPL-3.0**
라이선스 라이브러리를 쓰고 있었다. AGPL은 이 코드를 쓴 제품의 소스까지 공개해야
할 수 있어 **회사 제품에 넣을 수 없다.** 그래서 ONNX Runtime + rtmlib
(둘 다 Apache-2.0) 조합으로 갈아탔고, 최종적으로는 MediaPipe(Apache-2.0)
단독으로 정리됐다. **기술 선택이 성능이 아니라 라이선스로 결정된 사례.**

**변환 흐름** — `.pt`(PyTorch) → `.onnx`(공통 형식) → `.engine`(TensorRT, NVIDIA GPU에서만)

---

## 로보틱스

### <a name="ros2"></a>ROS 2 (Humble)

로봇 소프트웨어의 **사실상 표준 프레임워크**. 기능을 작은 프로그램(node)으로
나누고, 그것들이 topic이라는 통로로 메시지를 주고받는다.

**핵심 개념**

| 용어 | 뜻 |
|---|---|
| node | 하나의 기능 단위 프로그램 (예: 카메라 읽기, 모터 제어) |
| topic | node끼리 메시지를 주고받는 통로 (예: `/cmd_vel` = 속도 명령) |
| publisher / subscriber | topic에 메시지를 넣는 쪽 / 받는 쪽 |
| launch | 여러 node를 한 번에 띄우는 설정 파일 |

**어디에 썼나** — [TurtleBot3 프로젝트](2026/02.%20SW파일럿%20-%20TurtleBot3%20자율주행%20로봇/).
Ubuntu 22.04 + Humble 버전.

**직접 만든 노드** — 제스처를 속도 명령으로 바꾸는 브리지, 여러 조종 입력 중
하나만 통과시키는 **mux**, 웨이포인트 순찰, 안전 정지, LED 상태 표시.

### <a name="slam"></a>SLAM · <a name="nav2"></a>Nav2

**SLAM** = 로봇이 돌아다니며 **지도를 만들고 동시에 자기 위치를 파악**하는 기술
(Simultaneous Localization And Mapping).
**Nav2** = 그 지도 위에서 목적지까지 **경로를 짜고 따라가는** ROS 2 내비게이션 스택.

**어디에 썼나** — TurtleBot3로 공장 지도를 만들고, **A→B→C→D→A 웨이포인트
순찰**을 자율로 돌렸다. 순찰 중 장애물을 만나면 **정지 → 후진 → 재개**하는
안전 로직을 별도로 얹었다.

### <a name="lerobot"></a>lerobot

HuggingFace가 만든 **로봇 제어 오픈소스**. 여러 로봇팔의 모터 통신을 공통
인터페이스로 감싼다.

**어디에 썼나** — [웹캠 로봇팔 원격조종](2026/05.%20웹캠%20로봇팔%20원격조종/).
SO-ARM-101(Feetech STS3215 모터 6개)에 관절 각도를 보내는 데 썼다.

> ⚠ 이 저장소 안의 `lerobot/` 폴더는 **HuggingFace의 오픈소스 원본**이다.
> 직접 작성한 것이 아니라 실행에 필요해 함께 보관한 것이다.

### <a name="dynamixel"></a>DynamixelSDK

ROBOTIS 로봇 모터(Dynamixel) 제어 SDK. TurtleBot3의 바퀴 모터가 이 계열이다.

> ⚠ ROBOTIS 공식 오픈소스다. 직접 작성한 코드가 아니다.

---

## 하드웨어

### <a name="jetson"></a>Jetson Orin Nano

NVIDIA의 **소형 AI 보드**. 손바닥 크기에 GPU가 들어 있어 현장에서 딥러닝
추론을 돌릴 수 있다.

**어디에 썼나** — [캡스톤](2026/03.%20캡스톤%20-%20스마트팩토리%20비전검사/)(듀얼 카메라
YOLO 추론), [TurtleBot3](2026/02.%20SW파일럿%20-%20TurtleBot3%20자율주행%20로봇/)(로봇 제어 컴퓨터).

**실제로 겪은 어려움** — 제어 컴퓨터를 젯슨으로 옮기며 **6가지 문제가 한꺼번에**
터졌다(rosdep 오선언, Conda/시스템 Python 충돌, 빌드 잔재, 모터 토크 비활성,
라이다 모델·포트 오류). 원인부터 해결까지 전부 문서로 남겼다.

### <a name="raspberrypi"></a>Raspberry Pi

신용카드 크기의 소형 컴퓨터. **GPU가 없어** AI 추론이 느리다.

**어디에 썼나** — [라즈베리파이 선별 시스템](2025/01.%20캡스톤%20-%20라즈베리파이%20선별시스템/).
카메라는 `Picamera2`로 FHD·오토포커스·화이트밸런스를 하드웨어 레벨에서 제어했다.

**여기서 배운 것** — "자원이 부족한 보드에서 AI를 어떻게 돌릴 것인가"라는
문제의식. 이게 이후 TensorRT·OpenVINO 작업으로 이어졌다.

### <a name="arduino"></a>Arduino · <a name="esp32"></a>ESP32

**Arduino** — 간단한 하드웨어 제어용 마이크로컨트롤러. 시리얼 통신으로
PC에서 명령을 받아 모터·분류기를 움직인다.
**ESP32** — Wi-Fi가 내장된 마이크로컨트롤러.

**어디에 썼나**

| 보드 | 프로젝트 | 용도 |
|---|---|---|
| Arduino | 라즈베리파이 선별, 캡스톤 | `SORT:x` / `MOVE:x,y,z` 명령으로 분류기·로봇팔 제어 |
| ESP32 + MPU6050 | TurtleBot3 | **자이로 장갑** — 손 기울기를 Wi-Fi로 전송 |

### <a name="realsense"></a>Intel RealSense

**깊이(거리)를 측정하는 카메라**. 일반 카메라와 달리 물체까지의 거리를 직접 잰다.

**어디에 썼나** — 캡스톤에서 듀얼 카메라 중 한 대로 썼다.

**설계 포인트** — RealSense 연결을 먼저 시도하고, **실패하면 자동으로 일반 USB
웹캠으로 넘어가는** 폴백 구조를 만들었다. 현장에서 장비가 빠지거나 인식이 안
될 때 시스템 전체가 멈추지 않게 하기 위해서다.

### <a name="opencr"></a>OpenCR

TurtleBot3의 **제어 보드**. 모터·센서·LED를 담당한다. 이 프로젝트에선 소리
이상감지 결과를 **LED 색으로 표시**하는 데 썼다.

---

## 백엔드 · 웹

### <a name="flask"></a>Flask

파이썬 **경량 웹 프레임워크**. 필요한 것만 골라 쓰는 방식이라 작은 서버에 좋다.

**어디에 썼나** — 라즈베리파이 관제 대시보드, 메디컬 예측 API,
[웹서버 DB 기초](2026/06.%20웹서버%20DB%20연동%20기초/) 실습 2종.

### <a name="fastapi"></a>FastAPI

파이썬 웹 프레임워크. **비동기(async)** 를 기본으로 지원해 동시 요청 처리에 강하고,
API 문서가 자동 생성된다.

**어디에 썼나** — [캡스톤](2026/03.%20캡스톤%20-%20스마트팩토리%20비전검사/)에서
**두 카메라의 YOLO 추론을 멀티스레딩으로 병렬 처리**하는 서버.

**Flask와의 차이를 직접 비교** — 웹서버 DB 기초 실습에서 **같은 방명록 앱을
Flask판과 FastAPI판으로 각각** 만들어 무엇이 다른지 정리했다.

### <a name="nodejs"></a>Node.js / Express

JavaScript로 서버를 만드는 환경(Node.js)과 그 위의 웹 프레임워크(Express).

**어디에 썼나** — [Express 블로그 - 게시판](2021/01.%20Express%20블로그%20-%20게시판/) (2021).
게시글·댓글 CRUD를 Mongoose로 구현했다.

### <a name="mongodb"></a>MongoDB

**NoSQL** 데이터베이스. 표(테이블) 대신 JSON 비슷한 문서를 저장한다.
스키마가 유연해 구조가 자주 바뀌는 데이터에 쓰기 좋다.

**어디에 썼나** — Express 블로그(게시글 안에 댓글을 **내장 문서**로 넣었다),
웹서버 DB 기초 실습 3번.

**직접 비교해 본 것** — 같은 앱을 SQLite판과 MongoDB판으로 만들어
"관계형 vs 문서형"의 차이를 체감했다.

### <a name="mysql"></a>MySQL · <a name="sqlite"></a>SQLite

**관계형(RDB)** 데이터베이스. 표와 표 사이의 관계를 SQL로 다룬다.

- **SQLite** — 파일 하나가 곧 데이터베이스다. 서버를 따로 안 띄워도 되고
  **파이썬에 내장**돼 있어 설치가 필요 없다. 실습·소규모에 적합.
- **MySQL** — 서버형. XAMPP 웹 과제에서 썼다.

---

## 기타

### <a name="git"></a>Git · Git LFS

버전 관리. **LFS(Large File Storage)** 는 100MB가 넘는 큰 파일을 별도로
관리하는 확장이다.

**이 저장소에서** — 발표자료(.pptx) 12개를 LFS로 관리한다. 최종보고서 초안
하나가 115MB라 일반 방식으로는 GitHub이 받아주지 않았다.

### <a name="pyinstaller"></a>PyInstaller

파이썬 코드를 **단일 실행파일(.exe)** 로 묶는 도구. 파이썬이 안 깔린 PC에서도
돌릴 수 있게 한다.

**어디에 썼나** — [고클린](개인%20프로젝트/고클린%20-%20윈도우%20최적화%20도구/)(단일 exe),
광명테크 프로젝트(**얇은 런처** 방식 — exe가 무거운 라이브러리를 담지 않고
시스템 파이썬으로 실제 코드를 실행시킨다).

### <a name="delphi"></a>델파이 7 연동

회사 키오스크 UI가 **델파이 7**(1990년대 후반 언어)로 만들어져 있었다.
파이썬 인식 프로그램과 그 UI를 연결해야 했다.

**연동 방식의 변천** — UDP → 웹소켓 → **stdio 파이프**로 최종 확정.
파이썬이 이벤트를 표준출력에 한 줄씩 찍으면 델파이가 파이프로 읽는 방식이다.
가장 단순하고 의존성이 없어 채택됐다.

### <a name="chartjs"></a>Chart.js

웹 페이지에 그래프를 그리는 JavaScript 라이브러리. 관제 대시보드의
**생산량·불량률 실시간 차트**에 썼다.

---

## 적용한 논문 · 표준

코드 주석과 문서에 출처를 남긴 것들이다.

| 기법 | 출처 | 어디에 썼나 |
|---|---|---|
| <a name="oneeuro"></a>**1€ Filter** | Casiez, Roussel, Vogel — *ACM CHI 2012* | 커서·로봇팔 떨림 제거. **속도에 따라 세기가 변하는** 필터라, 가만히 있을 땐 강하게 떨림을 잡고 빠르게 움직일 땐 지연을 줄인다 |
| <a name="clahe"></a>**CLAHE** | Zuiderveld — *Graphics Gems IV, 1994* | 저조도 보정. 화면을 타일로 나눠 **국소적으로** 대비를 올리되 상한을 둬 잡음 폭주를 막는다. LAB 색공간의 밝기 채널에만 적용해 색은 건드리지 않는다 |
| **ISO 9241-411:2012** | 국제 표준 | 포인팅 장치 성능(처리량) 측정. 헤드트래커가 마우스 대비 어느 정도인지 재는 근거 |
| **중앙값 캘리브레이션** | 로버스트 통계 기본 기법 | 눈 깜빡임 같은 순간적 이상치 제거. 평균과 달리 이상치에 끌려가지 않는다 |
| **히스테리시스 + 쿨다운** | 제어·신호처리 기본 기법 | 입 벌림·눈 감김 판정. 켜는 문턱과 끄는 문턱을 다르게 둬 경계에서 깜빡이는 오발을 막는다 |
| **MiDaS** | Ranftl et al. — Intel ISL | 일반 카메라 한 대로 깊이 추정. 키오스크의 "뒷사람 인식" 문제의 근본 대안으로 검증 |
| <a name="pnp"></a>**3D 자세 추정** | MediaPipe facial transformation matrix | 머리의 실제 회전각(yaw/pitch/roll)을 얻어 2D 투영의 원근 왜곡을 원리적으로 제거 |
| **뼈 길이 제약** | 직접 설계 (물리 제약 활용) | 깊이 추정 오차를 뼈 길이 불변성으로 역산해 보정. 관절각 떨림 **48~78% 감소** |

---

<div align="center">

[← README로 돌아가기](README.md)

</div>
