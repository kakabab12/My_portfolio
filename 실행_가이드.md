# 실행 가이드

각 폴더를 어떻게 돌리는지, **무엇이 저장소에 없는지**, 하드웨어 없이 어디까지
볼 수 있는지를 정리했습니다.

> 먼저 알아 둘 것 — 이 저장소는 **아카이브**입니다. 설치하거나 빌드하면 다시 생기는
> 것은 올리지 않았습니다. 그래서 대부분 프로젝트는 `pip install` 한 번을 거쳐야 돕니다.

---

## 0. 저장소에 일부러 넣지 않은 것

| 빠진 것 | 왜 뺐나 | 어떻게 되살리나 |
|---|---|---|
| 파이썬 가상환경 (`venv/`, `.venv/`) | 기기마다 새로 만들어야 합니다. 합계 약 1.5 GB였다 | `python -m venv .venv` 후 `pip install -r requirements.txt` |
| `wheelhouse/` (pip 캐시) | 3.0 GB. 인터넷이 되면 필요 없다 | 필요 없음 |
| `node_modules/` | 5,676개 파일. 경로가 길어 **윈도우에서 clone 자체를 실패시켰다** | `npm install` |
| 모델 가중치 일부 (`.task`, `.tflite`) | MediaPipe가 배포하는 파일이라 다운로드로 받는다 | 각 프로젝트의 `scripts/download_weights.py` |
| ROS 2 빌드 산출물 (`build/`, `install/`, `log/`) | `colcon build` 하면 다시 생긴다 | `colcon build` |
| 의료 원본 데이터 (`*.csv`) | INSPIRE는 **PhysioNet 이용 승인**이 있어야 받을 수 있는 데이터다 | [PhysioNet](https://physionet.org/)에서 직접 신청. **학습된 모델(`.pkl`)은 저장소에 있어 재학습 없이 서버 실행 가능** |

> 직접 학습시킨 모델(`best.onnx`, `box.onnx`, `number.onnx` 등)은 **성과물이라 남겨 뒀습니다.**
> `sub/1000장/`, `sub/1500장/` 의 `best.onnx`는 크기만 같고 내용이 다른
> **학습 데이터 양이 다른 별개 실험 결과물**입니다.

---

## 1. 공통 준비

```bash
# Git LFS가 필요하다 -- 발표자료(.pptx)를 LFS로 관리한다
git lfs install
git clone https://github.com/kakabab12/My_project.git
```

파이썬 프로젝트는 **Python 3.11** 기준입니다. 특히 광명테크 프로젝트는
`3.11.5`로 버전을 고정해 두었다(타 PC 재현성 때문).

---

## 2. 하드웨어 없이 바로 돌려볼 수 있는 것

### 웹서버 DB 연동 기초 — 가장 쉽게 확인 가능

> `2026/06. 웹서버 DB 연동 기초/실습코드/`

```bash
cd "2026/06. 웹서버 DB 연동 기초/실습코드/01_flask_sqlite"
pip install -r requirements.txt      # Flask 하나뿐이다
python app.py                        # http://localhost:5000
```

같은 앱의 다른 조합도 있습니다.

| 폴더 | 조합 | 실행 | 추가 준비 |
|---|---|---|---|
| `01_flask_sqlite` | Flask + SQLite | `python app.py` | **없음** — SQLite는 파이썬 내장 |
| `02_fastapi_sqlite` | FastAPI + SQLite | `uvicorn main:app --reload` | 없음 |
| `03_flask_mongodb` | Flask + MongoDB | `python app.py` | **MongoDB 서버 설치·실행 필요** |

### 고클린 — 윈도우 최적화 도구

> `개인 프로젝트/고클린 - 윈도우 최적화 도구/alpha.ver/`

```bash
python alpha_test1.py
```

빌드된 `alpha_test1.exe`도 함께 있습니다. **관리자 권한이 필요한 작업이 많아서**
실행하면 UAC 창이 뜨고 스스로 재실행합니다.

### 메디컬 머신러닝 대회 — 학습 없이 서버만

> `2026/04. 메디컬 머신러닝 대회/AI/`

```bash
pip install flask lightgbm xgboost scikit-learn shap pandas numpy
python server.py         # 또는 serverbro.py (XGBoost 하이브리드)
```

학습된 `model_cls.pkl` · `model_reg.pkl` 이 저장소에 있어 **재학습 없이 바로**
예측 서버가 뜹니다. `index.html`을 열면 대시보드를 볼 수 있습니다.

> ⚠ `train.py`로 **재학습**하려면 INSPIRE 원본 CSV가 필요하다(위 0번 표 참고).

### Express 도서관 웹앱

> `개인 프로젝트/Express 도서관 웹앱/복구/`

```bash
npm install          # node_modules는 저장소에 없다
npm start            # http://localhost:3000
```

> ⚠ MongoDB 연결이 필요합니다. 연결 문자열은 `app.js`에서 확인·수정합니다.

---

## 3. 카메라(웹캠)만 있으면 되는 것

### 광명테크 — 배리어프리 키오스크 ★ 대표 프로젝트

> `2026/01. 광명테크 인턴 - 배리어프리 키오스크/리모콘 ui/gesture_kiosk/`

**Windows + Python 3.11.5 + 일반 웹캠 1대**면 됩니다. 별도 센서나 그래픽카드가 필요 없습니다.

```bat
install.bat                 :: 의존성 설치 + 모델 다운로드
py main.py                  :: 손 제스처 모드 (이벤트가 stdout으로 나온다)
py main.py --debug          :: 카메라·판정 계기판 창을 켠 채 시작
```

헤드트래커(고개·입으로 마우스 대체)는 별도 진입점입니다.

| 실행 | 기준점 | 설명 |
|---|---|---|
| `py head.py` | 코끝 | 처음 만든 방식 |
| `py eyebrow.py` | 미간 | 두 눈 사이 중점 |
| `py forehead.py` | 코-눈 혼합점 | **최신** — 몸 밀림을 원리적으로 없앤 방식 |

실행 중 콘솔에 명령을 칠 수 있습니다.

| 입력 | 동작 |
|---|---|
| `cam on` / `cam off` | 카메라 디버그 창 켜기/끄기 |
| `p` | 실제 마우스 제어 켜기/끄기 |
| `tune` | **실시간 감도·곡률 조절 슬라이더 창** |
| `quit` | 종료 |

> 상세 설치 절차(내부망 오프라인 반입 포함)는 그 폴더의 `설치가이드.md`,
> 델파이7 UI 연동은 `docs/델파이7_연동가이드.md` 참고.

### 웹캠 로봇팔 원격조종 — 팔 추적까지만 확인 가능

> `2026/05. 웹캠 로봇팔 원격조종/MediaPipe-SO-ARM101/`

```bash
pip install mediapipe opencv-python numpy
python download_models.py    # MediaPipe 모델 받기 (저장소에 없다)
python arm_tracker.py        # 웹캠으로 팔 관절각 추적 -- 로봇 없이도 확인 가능
python teleop_so101.py       # 실제 로봇 전송 -- SO-ARM-101 하드웨어 필요
```

★ `arm_tracker.py`는 **로봇이 없어도** 웹캠만으로 관절각 계산 결과를 화면에서
확인할 수 있습니다.

---

## 4. 하드웨어가 있어야 돌아가는 것

아래는 특정 장비가 없으면 실행이 안 됩니다. **코드와 문서 위주로 봐 주면 됩니다.**

| 프로젝트 | 필요 장비 | 하드웨어 없이 볼 수 있는 것 |
|---|---|---|
| [**TurtleBot3 자율주행 로봇**](2026/02.%20SW파일럿%20-%20TurtleBot3%20자율주행%20로봇/) | TurtleBot3 Burger + Jetson Orin Nano + Ubuntu 22.04 + ROS 2 Humble | ★ **단위 테스트는 하드웨어 없이 돕니다.** 손 모양 판별·D-pad 매핑·mux 로직·웨이포인트 판정·LED 상태 머신을 검증한다 (`sw/robot/tests/`) |
| [**캡스톤 — 스마트팩토리**](2026/03.%20캡스톤%20-%20스마트팩토리%20비전검사/) | Jetson Orin Nano + RealSense + USB 웹캠 + 아두이노 | TensorRT 엔진(`.engine`)은 **빌드한 장비에서만** 동작합니다. 코드와 `index.html` 대시보드 구조를 참고 |
| [**라즈베리파이 선별 시스템**](2025/01.%20캡스톤%20-%20라즈베리파이%20선별시스템/) | Raspberry Pi + Picamera2 + 아두이노 | `camera.py`를 일반 웹캠용으로 바꾸면 PC에서도 추론만 시험 가능. `best.onnx`가 저장소에 있다 |
| [**임베디드 리눅스**](2024/01.%20임베디드%20리눅스%20-%20배달감지%20카메라/) | 임베디드 보드 | **시연 영상 3편**이 들어 있다 (평상시 / 저조도 / 바디캠) |
| [**SW파일럿 웹 로봇제어 UI**](2025/02.%20SW파일럿%20-%20웹%20로봇제어%20UI/) | ROS + rosbridge 서버 | `robotics.html`을 브라우저로 열면 **캔버스 UI 자체는** 확인 가능(로봇 연결만 실패) |

### TurtleBot3 단위 테스트 돌려보기

```bash
cd "2026/02. SW파일럿 - TurtleBot3 자율주행 로봇/로봇 프로젝트 본체/sw/robot"
python -m pytest tests/
```

로봇·카메라 없이 판단 로직만 검증합니다. **GPIO 접근부와 로직을 분리해 둔 덕분**입니다.

---

## 5. 코드만 보면 되는 것 (실행 대상 아님)

| 폴더 | 성격 |
|---|---|
| `2026/01. .../GMtech_project`, `프로토타입_1`, `주 목표`, `헤드트래커_프로토타입_*` | 최종본([`리모콘 ui/`](2026/01.%20광명테크%20인턴%20-%20배리어프리%20키오스크/리모콘%20ui/))에 이르기까지의 **이전 세대**들. 발전 과정을 보여주려고 남겼다 |
| `2026/02. .../chapter 2`, `과정 1` | ROS 2 교육 실습 13편 · 로봇 설계 과제 9편 (문서) |
| `2026/01. .../광명테크 개발 일지` | 날짜별 개발일지 30여 편 (문서) |
| `2024/02. ASP + DB 연동` | ASP 과제. IIS 환경이 필요하다 |
| `개인 프로젝트/XAMPP 웹 자기소개서` | XAMPP(Apache+PHP+MySQL) 환경이 필요하다 |
| `개인 프로젝트/[외부코드] 마인크래프트 falling-pickaxe` | ⚠ **타인 오픈소스**. 자체 실행 스크립트(`scripts/run.ps1`)가 있다 |

---

## 6. 자주 막히는 곳

| 증상 | 원인 · 해결 |
|---|---|
| `.pptx` 가 이상한 텍스트 파일로 받아짐 | Git LFS 미설치. `git lfs install` 후 `git lfs pull` |
| 광명테크 프로젝트에서 모델을 못 찾음 | `scripts/download_weights.py`를 아직 안 돌렸습니다. `install.bat`이 이걸 대신 해 준다 |
| `mediapipe` 설치 실패 | Python 3.11이 아닐 가능성이 높습니다. 이 프로젝트는 **3.11.5 고정**이다 |
| 카메라가 안 열림 | 장치 번호가 다를 수 있습니다. `--device 1` 처럼 번호를 바꿔 보거나, `scripts/camera_check.py`로 진단한다 |
| 한글이 깨져 보임 | 터미널 인코딩 문제. `chcp 65001`로 UTF-8로 바꾼다 |
