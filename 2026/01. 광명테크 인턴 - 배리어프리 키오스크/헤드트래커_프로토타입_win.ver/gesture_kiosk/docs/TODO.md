# TODO — 작업 분해 및 진행 상황

작성: 2026-07-08 · 개정: 2026-07-21 3차 (커서 재정렬 제스처 최종 교체 — 입 오므리기).
기획서(기획서.docx) 주차 계획·9장 체크리스트와 연동. **기획서 2.2/3.1/5.1은
2026-07-10(타깃)·2026-07-18(동작 체계) 변경을 반영해 개정 필요.**

## ✅ 완료 (2026-07-21 3차 — 커서 재정렬 제스처 재교체, 사용자 실측 리포트)

- [x] **커서 재정렬 제스처 교체 (코 찡그리기 → 입 오므리기)**: 2차의 코 찡그리기
      (noseSneer)도 실기에서 인식이 안 된다고 확인됨(사용자 실측) — cheekPuff에
      이어 두 번째 실패. **입 오므리기**(mouthPucker)로 최종 교체 — 이 신호는
      원래 go_back 후보였다가 select(jawOpen)와 전환 중 겹쳐 폐기된 이력이 있는데,
      그 폐기 이력 자체가 MediaPipe가 이 신호는 강하게 잡는다는 실증이라 재사용
      (recenter는 드물고 독립적인 동작이라 그 전환-겹침 위험이 select만큼 크지
      않음). 두 번 연속 잘못 고른 것을 계기로 `/data`의 `debug.all_blendshapes`에
      **전체 블렌드셰이프 원점수**를 노출 — 다음에 또 교체가 필요하면 추측 대신
      실측값으로 후보를 고를 수 있다. 단위 테스트 총 43건.

## ✅ 완료 (2026-07-21 2차 — 커서 재정렬 제스처 실기 재교체, 사용자 실측 리포트, 이후 3차로 재교체됨)

- [x] ~~**커서 재정렬 제스처 교체 (볼 부풀리기 → 코 찡그리기)**~~ — 위 3차에서 다시
      교체됨. 사용자가 실기에서 확인 — 양 볼 부풀리기(cheekPuff)가 거의 인식되지
      않았다. MediaPipe 블렌드셰이프 모델이 이 신호를 약하게 낸다고 추정(원인
      확정은 아님). noseSneerLeft/Right로 교체하면서, go_back(양쪽 다 필요·min)과
      반대로 **좌우 중 큰 쪽만 넘으면 인정**(max)하도록 해 카메라 각도·조명으로
      한쪽이 약해도 인식되게 구조적으로 보완했으나 — 그럼에도 실기 인식률이
      부족했다.

## ✅ 완료 (2026-07-21 — TTS 안정화·커서 재정렬 제스처·UI 버그, 사용자 요청)

- [x] **TTS(음성 안내) 간헐적 무음 진단·보완**: `announce` 워커 스레드가 COM(SAPI)
      아파트를 초기화하지 않아 `pyttsx3.init()`이 실행마다 성공/실패가 갈렸다 —
      스레드 시작 시 `pythoncom.CoInitialize()`(윈도우 전용, 실패해도 무해하게 통과)를
      추가. 또한 초기화 실패를 `logger.warning`(원인 미기록)이 아니라
      `logger.exception`으로 바꿔 재발 시 원인을 로그에서 바로 볼 수 있게 함
      (`src/announce/announcer.py`).
- [x] **커서 재정렬 제스처 신설 (원안 양 볼 부풀리기 = cheekPuff — 이후 코
      찡그리기→입 오므리기로 재교체됨, 위 항목들 참고)**: №1-신 접근성 항목 해결 —
      자리 이동 없이 `recenter` 이벤트로 커서 중심을 즉시 다시 캘리브레이션.
      입/눈 판정 기준선은 건드리지 않아 재정렬 직후에도 select/go_back이 바로
      동작한다.
- [x] **"취소" 버튼 비율 버그 수정**: `.btnrow`의 `.btn` 자식에 `flex` 지정이 없어
      "발급하기"(4자)와 "취소"(2자)가 글자 수만큼 다른 너비로 그려졌다 —
      `.btnrow > .btn { flex: 1 }`로 균등 분할 고정.

## ✅ 완료 (2026-07-20 2차 — 성능·정렬·안내 개선, 사용자 요청 "추론 30·카메라 30+·디자인")

- [x] **추론 FPS 30 달성 (KPI)**: 실측 27~31, 대부분 29~30 (개선 전 17~27에 수시 급락).
      적용한 개선 4가지 —
      ① person_lock이 잠금 중에도 매 프레임 계산하던 라플라시안 선명도 제거(잠금 중엔
        위치 추적만 쓰므로 순수 낭비였음 — 회귀 방지 테스트 추가),
      ② BGR→RGB 변환을 numpy 역순 복사에서 cv2.cvtColor(SIMD)로,
      ③ **파이프라인 처리 해상도(camera.proc_* = 640x360) 도입** — 720p를 그대로 흘리면
        추론이 빨라져도 프레임 배관(복사·시각화·JPEG)이 3배로 늘어 오히려 10 FPS로
        무너지는 것을 실측으로 확인 후, 캡처만 크게 받고 진입 즉시 축소하는 구조로,
      ④ 프로세스 우선순위 ABOVE_NORMAL 상향(브라우저·TTS와의 CPU 경합 방어).
      추가 발견: 시연 화면을 여러 창에서 열면 창마다 MJPEG 인코딩이 따로 돌아 FPS가
      떨어진다 — 설치가이드 문제해결 표에 기록.
- [x] **커서·호버 정렬 버그 수정**: 카메라가 4:3(640x480)으로 협상되는데 데모 UI
      스테이지는 16:9(cover)라 세로가 잘려 커서와 호버 판정이 가장자리에서 최대
      12.5% 어긋나던 문제 — preprocessor가 중앙 크롭으로 항상 16:9를 보장하게 수정
      (카메라가 어떤 해상도로 협상되든 동일). person_lock 추적 반경도 처리 해상도
      기준으로 초기화하도록 수정. 전처리 단위 테스트 4건 신설(총 39건).
- [x] **상태 안내 오버레이(접근성)**: 인식 대기("카메라 앞에 서 주세요" + 거리 안내)와
      캘리브레이션 중("잠시 정면을 바라봐 주세요")을 화면에 크게 표시 + 음성 안내 —
      반응이 없을 때 이유를 화면만 보고 알 수 있게. 단계 표시를 점(dot) 그래픽으로,
      배경 그라데이션·커서 시인성(외곽선+크기 비례) 등 시각 다듬기.

## ✅ 완료 (2026-07-20 — 정확도·유지보수·UI 개선 라운드, 사용자 요청)

사용자가 "정확도·유지보수·비용·UI"를 함께 개선해 달라고 요청 — AskUserQuestion으로
각 항목의 구체적 의미를 확인한 뒤 진행. 비용(연산·하드웨어)은 조사 결과 더 가벼운
공식 FaceLandmarker 모델이 없고 이미 CPU 전용·4MB 모델이라 추가 조치 없음(현황 유지가
곧 답).

- [x] **정확도 — 판정을 고정 임계값 → 평상시 기준선 상대값으로 전환**: 잠금 직후
      코 위치 캘리브레이션과 같은 구간에서 입벌림(jawOpen)·눈감김(eyeBlink) 평상시
      값도 함께 캡처해(`_MedianCalibrator`) 그 기준선 + margin으로 판정한다. 실기
      관찰(2026-07-18)상 사람마다 평상시 eyeBlink가 0.1~0.6까지 편차가 커서 고정
      임계 하나로는 오탐/미탐이 갈렸는데, 실측으로 확인(2026-07-20, eye_baseline=0.51)
      — 이 사용자의 기준선이 옛 고정 임계(0.6)에 근접해 있어 오탐 위험이 실제로 컸다.
      `configs/config.yaml`의 `mouth_click.open_threshold/close_threshold` →
      `open_margin/close_margin`, `eye_close_cancel.close_threshold` → `close_margin`으로
      스키마 변경. 계기판(`/data` debug, 비디오 오버레이)에 현재값과 기준값을 함께 노출.
- [x] **유지보수 — 리팩토링**: 코 위치·입벌림·눈감김 캘리브레이션 로직이 사실상
      동일 패턴(짧은 구간 표본→중앙값)이라 `_MedianCalibrator` 공용 클래스로 통합 —
      `_CursorMapper` 내부 구현도 이 위에 재구성. `_ThresholdGate`는 임계값을 생성자
      고정값이 아니라 매 `update()` 호출 인자로 받도록 변경(호출부가 기준선+margin을
      계산해 넘김 — 관심사 분리).
- [x] **유지보수 — 테스트 커버리지**: `tests/test_head_tracker.py`에 기준선 적응 동작
      검증 3건 추가(AdaptiveBaselineTest — 평상시 값이 높은 사용자도 오탐 없음, 기준선
      준비 전엔 판정 보류). `tests/test_face_estimator.py` 신설(bbox 계산·데이터 접근자
      순수 로직, mediapipe 불필요) — 전체 25→34건.
- [x] **유지보수 — 문서 정리**: 설치가이드.md·README.md·설명서.md에 남아있던 이전
      제스처(눈썹·입 오므리기) 단계의 stale 설명을 전부 최신화. config 스키마 변경
      (margin 기반)도 각 문서 반영.
- [x] **UI — 응시·눈감기 진행률 시각 표시**: 서버 커서 주변에 초록 진행 링(응시,
      dwell_progress_ratio), 스테이지 상단에 주황 진행 바(눈감기, eye_close_progress_ratio)
      신설 — 클릭/뒤로가기가 얼마나 남았는지 보여준다. 커서와 다른 시각 언어(링/바)를
      써서 "포인터가 또 하나 생긴 것처럼" 보이지 않게 설계.
- [x] **UI — 뒤로가기를 단계별로 + 입력값 보존**: 기존엔 go_back이 항상 처음(home)으로
      가서, 주민등록번호를 다 입력하고 실수로 뒤로가기하면 전부 날아갔다. 이제
      `PREV_SCREEN` 맵으로 한 단계씩만 이동하고 `goBack()`은 `enterScreen()`과 달리
      입력값을 초기화하지 않는다 — 시뮬레이션으로 13자리 보존 확인(2026-07-20).
      완료 화면만 예외로 처음으로 돌아간다(더 되돌아갈 단계가 없음).
- [x] **UI — 시각 다듬기**: 화면 전환 페이드인 애니메이션, 강조 표시에 스케일 효과
      추가, "N/4 단계" 진행 표시 신설.

## ✅ 완료 (2026-07-18 — 팔 쓸기 → 헤드트래커 전면 전환, head_tracker_manual.md 기반)

- [x] **판정 엔진 교체**: RTMPose(포즈, 팔 쓸기) → MediaPipe FaceLandmarker(얼굴 랜드마크
      +블렌드셰이프) — 코끝=포인터·입 벌리기/1.5초 응시=선택·눈썹 올리기=뒤로·처음으로(겸용).
      사용자 결정: 팔 쓸기 체계는 config 토글 없이 **완전 교체**, 클릭은 입 벌리기·응시
      **둘 다** 항상 활성.
- [x] blendshapes(jawOpen·browInnerUp·browOuterUpLeft/Right) 채택 — 매뉴얼의 랜드마크
      거리비(입술 13/14, 눈썹 105/334) 직접 계산 대신 MediaPipe가 이미 정규화·학습해
      내보내는 신호를 써서 얼굴 형태·거리 편차에 더 강건하게 설계.
- [x] **GPU/CUDA 스택 전면 제거** — onnxruntime-gpu·torch(CUDA DLL 등록용)·rtmlib 삭제.
      MediaPipe FaceLandmarker는 CPU만으로 23~27 FPS 실측(1280x720 웹캠, 30 FPS KPI에
      근접) — 구 RTMPose는 CPU 0.5 FPS로 GPU가 필수였던 것과 대비. 설치가 크게 단순해짐
      (이 branch에서 NVIDIA MX250(Pascal, compute 6.1)이 최신 onnxruntime-gpu와 근본
      비호환이라 구버전 조합으로 우회해야 했던 문제 자체가 소멸).
- [x] 실기 튜닝(2026-07-18, 실사용자 웹캠 테스트): `pointer.sensitivity` 3.0→0.8
      (미세한 고개 움직임에도 커서가 화면 끝까지 즉시 saturate되던 문제),
      `eyebrow_cancel.raise_threshold` 0.5→0.75(평상시 표정도 0.5를 넘어 오탐 연발).
- [x] 실기 2차 튜닝(2026-07-18, 사용자 보고 "커서가 상단에서만 움직임"): 캘리브레이션이
      잠금 직후 코 위치 **평균**을 중심으로 삼았는데, 과도기 프레임(고개 숙임 등) 1~2장이
      섞이면 기준점이 그쪽으로 끌려가 이후 계속 "위로 벗어난" 상태로 읽혀 커서가 상단에
      눌러붙음 → 평균 대신 **중앙값**으로 교체 + 캘리브레이션 구간 0.3→0.5초로 확대.
      추가로 `sensitivity`를 `sensitivity_x`/`sensitivity_y`로 분리해 상하(0.5)를
      좌우(0.8)보다 낮게 — 고개 상하 회전 범위가 좌우보다 좁아 같은 민감도면 상하가
      더 쉽게 끝까지 튀는 문제 보완. 재검증: 커서가 중앙(0.5,0.5)에서 시작해 화면 전체
      (x: 0.05~0.94, y: 0.01~0.61)를 정상 이동, 입벌리기·응시·눈썹 전부 실기 확인.
- [x] 이벤트 스키마 축소: `classes`가 `[move_left, move_right, select, go_back, go_home]`
      → `[select, go_back]`로. `/data`에 연속 `cursor_x_ratio`/`cursor_y_ratio` 신설(UDP
      계약에는 미포함 — 아래 미확정 항목 참고).
- [x] `demo_ui/index.html`: `focusIdx` 선형 포커스(토크백식) → 커서 호버 기반 선택으로 전환.
      화면 전체를 덮는 고정 위치 커서 점 + `.item`/`.btn` 경계 대조로 호버 판정.
- [x] `person_lock.py`: 얼굴 bbox 직접 기반으로 단순화, `lock_generation` 카운터 신설
      (head_tracker가 새 잠금 시점에 커서 캘리브레이션을 재실행하는 트리거).
- [x] 단위 테스트: `test_gesture_filter.py` 삭제 → `test_head_tracker.py`(22건 중 다수) 신설,
      `test_person_lock.py` 얼굴 기반으로 재작성. 전체 스위트 통과 확인.
- [x] 설치가이드.md·README.md·requirements.txt·install.bat·make_offline_bundle.bat·
      download_weights.py 전부 헤드트래커 기준으로 갱신.
- [x] **(같은 날 2차) 커서 상단 고착 버그 수정** — 캘리브레이션 평균→중앙값(이상치
      프레임 방어), `sensitivity`를 `sensitivity_x`(0.8)/`sensitivity_y`(0.5)로 분리
      (상하 회전 범위가 좌우보다 좁아 같은 민감도면 상하가 더 쉽게 saturate).
- [x] **(같은 날 3차) 캠+메뉴 오버레이 통합** — 서버가 영상에 그리는 커서와 화면 전체
      DOM 커서, 두 개가 따로 보이던 문제(사용자 지적) 해결: 메뉴를 캠 영상 위에 얹는
      단일 스테이지로 재구성, 클라이언트 DOM 커서 제거 — 서버가 프레임마다(25~30FPS)
      그리는 코 커서 하나만 남김.
- [x] **(같은 날 4차) 뒤로가기 제스처 교체 — 눈썹 올리기 → 입 오므리기**: 앞머리(bangs)가
      눈썹을 가리는 사용자는 매뉴얼 원안(눈썹) 인식이 안 됨(사용자 지적) — mouthPucker
      블렌드셰이프로 교체. `pucker_cancel` config, `_pucker_gate` 등 이름도 정리.
      아직 실기 튜닝 전(기본 임계 0.5) — 회사·현장 검증 필요.
- [x] **(같은 날 4차) 데모 UI에 주민등록번호 입력 키패드 신설**: 증명서 선택 →
      **주민등록번호 입력(신규)** → 발급 확인 → 발급 완료 흐름으로 확장. 0~9 키패드를
      커서 호버+선택으로 누르는 방식, 뒷자리는 입력 즉시 마스킹(●) 표시. **입력값은
      브라우저(demo_ui) 안에만 머물고 엔진으로 전송되지 않음** — 엔진은 여전히
      select/go_back 이벤트만 다뤄 "엔진은 개인정보를 다루지 않는다" 원칙 그대로 유지.
- [x] **(같은 날 5차) 데모 UI에 발급 장수 선택 화면 신설**: 주민등록번호 입력 완료 →
      **발급 장수 선택(신규, 1~5장)** → 발급 확인 → 발급 완료로 흐름 확장. 발급 확인·
      완료 화면에 장수가 함께 표시된다("등본 (3장)" 등).

## ✅ 완료 (2026-07-15 — 동작 체계 범용 설계 개편, 같은 날 2차 확정)

- [x] 쓸기 동작 도입: 팔 좌/우 쓸기=포커스 이동 · 아래=이전 화면(**go_back 이벤트 신설**) ·
      위=처음으로 — 포즈 손목 궤적 판정이라 손·손가락이 없는 사용자도 동일 조작
- [x] **(2차) 선택(OK) = 고개 꾸벅 2회 확정** — "끄덕임=예"라 직관적이고 팔이 전혀 없어도
      가능. 신호 = 목 길이 비율(코~어깨 중점, 어깨 너비 정규화), 숙였다 제때(0.8초) 복귀 ×2.
      내려다보기(지갑·신분증)는 복귀 시간 초과로 무효 — 오탐 방지 설계
- [x] (2차) 1차 안이던 손등/팔등 보이기 경로 전면 제거 — MediaPipe 손 검출·팔등 CNN·
      수집/학습 스크립트 삭제 → **포즈(RTMPose) 단일 엔진** (requirements에서 mediapipe 제거)
- [x] 구 동작 전면 제거: 주먹→펴기·OK핀치·양손바닥 10초·레거시(5.1 초안) 토글
- [x] 구 HaGRID ONNX 제스처 엔진 제거 — 신규 스펙 판정 불가로 사장 + AGPL 리스크 해소
      (제거 산출물 백업: ~/Documents/backup/gesture_kiosk_2026-07-15_before_gesture_rework/,
       _before_nod_select/)
- [x] 단위 테스트 43건 재작성·통과 (쓸기·꾸벅 판정·목 길이 신호·잠금)
- [x] **(2026-07-16) 쓸기 팔꿈치 폴백** — 손 절단 사용자는 손목 키포인트 신뢰도가
      낮을 수 있어(사용자 지적), 손목 미달 시 팔꿈치 궤적 + elbow_gain 배율로 판정.
      추적점 전환 시 궤적 리셋, 화면 라벨 "(E)" 표시. 테스트 48건
- [x] **(2026-07-16) 쓸기 정지 재장전** — 우로 쓸고 원위치 복귀가 좌로 오인되는
      실기 오탐(사용자 보고) 해결: 이벤트 후 추적점이 잠깐 멈춰야(rearm_still) 다음
      쓸기 장전 — 복귀는 연속 동작이라 구조적으로 통과 불가. 상하 복귀·왕복 흔들기
      연발까지 같은 원리로 차단. 테스트 52건
- [x] **(2026-07-16) 한 번에 한 팔만 인식** — 양팔 동시 추적 시 쉬는 팔의 잡음이
      간섭(사용자 보고: 인식 저하). 활성 팔 = 더 높이 든 팔(switch_margin으로 교체
      진동 방지), 팔 교체 시 궤적 리셋. 맥 config 추론 상한 60→30 고정(캡처 FPS
      반토막 방지 — 배포와 동일 조건). 테스트 55건
- [x] **(2026-07-16) 쓸기 임계 어깨너비 정규화** — 카메라 위치·거리에 따라 인식이
      갈리는 문제(사용자 보고): 임계 단위를 화면 비율 → 어깨너비 배수로 전환
      ("자기 어깨너비의 몇 배를 움직였나"). EMA 평활·측면 회전 하한 클램프·미검출
      폴백(fallback_ratio=0.22, 종전 임계와 등가) 포함. 꾸벅은 애초에 정규화돼 있어
      전 제스처가 거리 불변. 프레임 밖 이탈은 소프트웨어로 불가 — 설치 기준 문서화. 테스트 58건
- [x] **(2026-07-16) 주민등록증 OCR 기능 전면 제거 — 제스처 집중(사용자 결정)**:
      src/ocr·OCR 워커·/ocr 엔드포인트·fill_id_fields 이벤트·easyocr(+맥 torch) 의존성 삭제.
      **№11 주민등록번호 처리 이슈 소멸**, 미니PC CPU 부담·설치 용량 대폭 감소.
      classes 계약 변경(fill_id_fields 제외 — №7 UI 반영 확인). 백업: _before_ocr_removal/
- [x] **(2026-07-16) 판정 계기판 + 임계 완화** — 실기 인식 저하(사용자 보고) 원인이
      근거리 시험에서 정규화 임계가 커진 것(어깨 1.1배=화면폭 40%+·프레임 이탈)으로
      추정 → min_dist 0.8/0.5·window 0.8초로 완화 + 판정 내부값(스케일·진행도·장전)을
      /data·화면·데모UI에 노출 — 이후 튜닝은 수치 근거로. 테스트 48건
- [x] **(2026-07-16) 토크백/보이스오버 문법 정렬 — 사용자 결정**: 위/아래 1회=확인
      (read_focus 신설)/선택, 위/아래 2연속=go_home/go_back(파괴적 동작 안전장치),
      고개 꾸벅 제거(쓸기 일원화). classes 6종 — №7 UI 반영 확인.
      1회 동작은 판정 창(double_within_sec 1.2초)만큼 지연 발화 — 실기 튜닝 대상. 테스트 42건
- [x] **(2026-07-16) 정지 재장전 제거 → 반대 방향 1회 삼킴** — 멈춤 판정이 키포인트
      떨림에 갇혀 인식 불능 유발(사용자 보고: 인식 안 됨) + 복귀 중 멈칫하면 방어가
      뚫림(복귀 오탐 재발). 대체: 동작 후 return_suppress_sec(2.5초) 안의 같은 축
      반대 방향 쓸기 1건을 복귀로 무시 — 멈춤 불요·연속 쓸기 즉응. 테스트 35건
- [x] **(2026-07-16 최종) 확인·선택 통합 + 아래 1회=go_back 즉시 + 전완 플릭 인식** —
      read_focus 제거(위 1회=select 통합), 아래는 대기 없이 즉시 이전 화면,
      임계 0.55/0.35로 2차 완화(팔꿈치 고정 플릭 인식 — 사용자 보고 대응).
      classes 5종 — №7 UI 반영 확인. 테스트 35건.
      정정(같은 날): **처음으로=아래 2연속**(위 2연속 오배치 수정) — 분기가 아래로
      이동해 select(위 1회)는 즉시 발화, go_back이 판정 창(1.2초) 지연
- [x] **(2026-07-16) 좌·아래·아래2연속 인식 불량 해결** — 복귀 판정을 시간→위치 기반
      (직전 획 끝 경유 검사)으로: 의도적 반대 쓸기 통과, 삼킴 창 1.6초 단축.
      획 유예(0.5초, 빠른 2연속 차단 부작용) → 복귀 확인 게이트(어깨 0.15배 되돌림)로
      교체 — 빠른 아래 2연속 시간 제한 없이 인정. 테스트 37건

## ✅ 완료 (2026-07-08 — 1주차)

- [x] 기획서 2.3 디렉터리 구조 + 4장 코딩 컨벤션 적용한 저장소 골격
- [x] 캡스톤 코드 구조 이식 — 단일 USB 카메라·제스처용으로 정리
- [x] 실시간 파이프라인: 캡처 스레드 → 전처리 → YOLO 추론 → 판정 → 이벤트
- [x] 무학습 모델 경로 확보: HaGRIDv2 사전학습 YOLOv10n (다운로드 스크립트 포함)
- [x] 예시 UI · 이벤트 전송 접점(event_sender) · 단위 테스트

## ✅ 완료 (2026-07-10 — 전면 리팩토링, feat/think_win_gpu)

- [x] 타깃 확정: **윈도우 + NVIDIA + Python 3.11.5** (정부 민원발급기)
- [x] 신규 동작 체계: 주먹→펴기 이동(왼손=왼쪽·오른손=오른쪽) / OK=선택·확인 통일 /
      양 손바닥 10초=처음으로. 레거시(5.1 초안)는 config 토글로 병행 유지
- [x] 사용자 잠금: YOLO11n-pose + 얼굴 선명도(라플라시안)×크기 점수 —
      잠긴 사용자의 손목 근처 손만 인식 (다른 사람 손 차단, 거울 좌/우 보정)
- [x] 주민등록증 OCR(EasyOCR): 이름·주민번호 자동 입력(fill_id_fields) —
      프레임·원문 미저장, 로그 마스킹
- [x] 토크백: 이벤트 자동 안내 + UI 요청(POST /announce) TTS(pyttsx3)
- [x] 이식 체계: requirements 버전 고정 · install.bat / run.bat ·
      내부망용 make_offline_bundle.bat · 설치가이드.md · smoke_test
- [x] 예시 UI를 민원발급기 흐름(증명서 선택→본인확인→발급)으로 교체
- [x] 단위 테스트 43건 통과 (FSM·사용자 잠금·OCR 파싱)

## 🔴 회사 확인 필요 — 멋대로 진행 금지 (기획서 9장 연동)

- [x] ~~**№1 제스처 스펙 확정 (구 팔 쓸기 체계)**~~ — 2026-07-18 헤드트래커 전환으로
      **대체됨**. 좌/우 쓸기=이동 등 팔 스윙 체계는 폐기, 코끝=포인터·입벌리기/응시=선택·
      눈썹=뒤로/처음 체계로 교체(사용자 결정, head_tracker_manual.md 근거).
      ⚠ 새 체계는 양팔이 없는 사용자도 동일하게 조작 가능(얼굴 신호만 사용) — 구 체계의
      "양팔 없는 사용자 선택 수단 부재" 우려는 해소됐으나, 아래 №1-신 접근성 항목 참고.
- [x] ~~**№1-신 (신규, 2026-07-18) 접근성 — 재캘리브레이션 UX**~~ — 2026-07-21 해결
      (같은 날 트리거 제스처를 2차례 재교체 — 최종 입 오므리기, 위 changelog 참고).
      제스처로 자리 이동 없이 커서 중심을 다시 잡을 수 있게 됐다(`recenter` 이벤트,
      `head_tracker.recenter_gesture` config). 입 벌리기·눈 감기와 다른 근육이라
      오작동 겹침 없음 — 자동 재중심화(가장자리 오래 붙어 있으면 등)는 이 명시적
      제스처로 대체돼 더 이상 필요 없다고 판단.
- [ ] **№1-신2 (신규, 2026-07-18) 회사 UI의 실시간 커서 필요 여부** — 데모 UI는 `/data`
      폴링으로 연속 커서(cursor_x_ratio/y_ratio)를 받아 호버 판정하지만, UDP 이벤트
      계약에는 이산 이벤트(select/go_back)만 포함했다. 회사 프로그램이 호버 기반
      상호작용을 쓸 경우 연속 좌표 전송 경로(UDP 등)가 별도로 필요한지 협의 필요.
- [ ] **№7 회사 키오스크 프로그램(UI) 파일 수령** — 수령 후 demo_ui 교체 작업 시작
- [ ] **№7 이벤트 전달 규격 합의** — udp JSON 예시 구현 상태. 소켓/시리얼 등 확정 시 Sender 추가.
      **2026-07-18 계약 변경: classes가 select·go_back 2종으로 축소 — UI 측 반영 확인 필요**
- [ ] **№8 통합 범위 확정** — UI 통합까지인지 이벤트 출력까지인지
- [ ] **№5·№6 KPI 측정 기준 합의** — 정확도 85% 산식 / 30 FPS(엔드투엔드 vs 추론 단독).
      2026-07-18 실측: CPU 단독 23~27 FPS로 KPI에 근접(GPU 없이)
- [ ] **№9 라이선스 검토** — 2026-07-18로 검토 대상이 더 단순해짐: 추론 모델이
      MediaPipe FaceLandmarker(Apache-2.0) 하나뿐 (rtmlib·HaGRID·onnxruntime-gpu 모두 제거).
      현 스택(mediapipe·pyttsx3 무수정)은 카피레프트·저작자표시 의무 없음 — 원문 확인 2026-07-18.
      잔여: 배포물에 Apache/MIT 라이선스 문서 동봉 절차만 최종 빌드 때 확인
- [ ] **№10 코드 저장 위치·백업 정책** — 외부 Git 허용 여부
- [x] ~~**№11 주민등록번호 처리 법적 근거**~~ — 2026-07-16 OCR 기능 제거로 **이슈 소멸**
      (엔진은 더 이상 개인정보를 다루지 않는다. 본인확인은 회사 프로그램 담당)
- [x] ~~**№12 설치 대상 PC 사양·망 환경**~~ — 2026-07-18 GPU 요구사항 자체가 소멸
      (MediaPipe CPU 추론으로 충분, 실측 23~27 FPS). 인터넷/내부망 여부는 여전히
      설치가이드 B절(오프라인 번들) 대상이라 확인 필요.

## 🟡 윈도우 실기기 검증 (설치 PC 확보 시 — 맥 개발기에서 불가한 항목)

- [ ] install.bat / run.bat / make_offline_bundle.bat 실기 동작 확인 (작성만 됨 — 미검증)
- [ ] torch(EasyOCR용) 2.11.0+cu128 + onnxruntime-gpu 1.23.2(CUDA 12 빌드 — 1.27.0은 CUDA 13이라 금지) 설치, CUDA EP 인식 (smoke_test)
- [ ] TensorRT 가속(config use_tensorrt: true — 첫 실행 캐시 생성) → benchmark 30 FPS 기록
- [ ] 오토포커스 카메라로 person_lock 튜닝 (sharpness_weight·wrist_match_ratio)
- [ ] 한국어 TTS 보이스(Heami 등) 설치 확인 + 안내 문구 낭독 품질
- [ ] 두 사람 동시 프레임 진입 시 잠금 유지 확인 (다른 사람 손 차단)

## 🟢 이후 (조건부 — 학습이 필요해질 때, feat/study)

- [ ] 제스처 정확도 미달 시: 데이터 수집·라벨링 → training/으로 파인튜닝 (기획서 5.4 인물 단위 분할)
- [ ] 학습 가중치 명명 규칙: {모델}_{데이터셋}_{지표}_{날짜}.pt (기획서 4.8)

## 메모

- 학습은 5080 / 맥북 M5 Pro 등 아무 환경 — 결과 .pt만 추론(윈도우)으로 이식 (2026-07-10)
- 예시 UI의 증명서 목록은 데모용 더미 — 실제 화면·업무 흐름은 회사 프로그램 담당
