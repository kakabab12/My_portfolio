# 데이터 수치 — 논문용 측정 자료 모음

2026-09-09 정리. 헤드트래커 논문에 쓸 수치를 한곳에 모았습니다.

## 가장 중요한 규칙

**시뮬레이션 값과 실기 값을 섞어 쓰지 마세요.** 파일을 그 기준으로 나눴습니다.

| 파일 | 출처 | 논문에서 쓸 때 |
|---|---|---|
| `01_실기_측정치.md` | **실제 장치**에서 잰 값 | "실측" 이라고 쓸 수 있음 |
| `02_시뮬레이션_요약.md` + `*.csv` | **가상 카메라 합성** | 반드시 "시뮬레이션" 이라고 밝힐 것 |
| `03_발견사항.md` | 위 두 가지를 분석한 결과 | 논문의 논의 부분 재료 |
| `04_기법별_전후비교.md` | **기법을 하나씩 끄고 켜서** 잰 값 | 논문의 핵심 근거 표 |
| `05_렌즈보정_전후비교.md` | 렌즈 자가 보정 전/후 (예열 조건 맞춤) | 논문의 핵심 근거 표 |

## 기법별 전/후 비교를 볼 때 주의할 것

`04_기법별_전후비교.md`가 이 폴더에서 가장 중요한 문서입니다. 다만 **원값만
보면 틀린 결론이 나옵니다.**

감도를 올리면 신호도 인공물도 잡음도 **같은 비율로** 커집니다. 그래서 감도를
낮추는 기법은 모든 지표가 좋아진 것처럼 보이고, 올리는 기법은 나빠진 것처럼
보입니다. 반드시 **커서 이동폭으로 나눈 정규화 값**을 함께 보세요.

정규화하면 7개 기법 중 **진짜 개선은 셋**입니다(상대 회전 매핑, 얼굴 기준 좌표계,
렌즈 자가 보정). 나머지는 도달 범위나 대응 관계 같은 다른 것을 해결합니다.

## 다시 뽑는 법

```bash
py "데이터 수치/generate.py"          # 기본 수치
py "데이터 수치/ablation.py"          # 기법별 전/후 비교
py "데이터 수치/lens_ablation.py"     # 렌즈 자가 보정 (공정 비교, 15분)
```

프로젝트 루트(`gesture_kiosk/`)에서 실행하면 CSV가 이 폴더에 다시 떨어집니다.
난수 씨앗을 고정해 두어 몇 번을 돌려도 같은 값이 나옵니다.

## CSV 목록

| 파일 | 내용 | 행 |
|---|---|---|
| `혼합비_스윕.csv` | 혼합비 r을 0.05~1.0으로 훑으며 회전이득·곡률·떨림·끌림 | 20 |
| `거리별_기하반폭.csv` | 화면 4종 × 거리 7종의 기하학적 반폭 | 28 |
| `가동범위별_도달률.csv` | 목 가동범위별 화면 도달률과 필요 배율 | 10 |
| `평활_비교.csv` | 평활 없음 / EMA 2종 / 1유로 2종의 떨림·지연 | 5 |
| `배치렌즈별_끌림.csv` | 카메라 배치 5종 × 렌즈 4종의 몸 평행이동 끌림 | 20 |
| `상대거리_추정오차.csv` | 얼굴 6종 × 거리 5종의 상대 거리 추정 오차 | 30 |
| `기법별_전후비교.csv` | 기법 9종의 적용 전/후 원값 | 22 |
| `기법별_전후비교_정규화.csv` | 위를 커서 이동폭으로 나눈 값 | 22 |
| `렌즈보정_전후비교.csv` | 렌즈 3종 × 보정 끔/켬 × auto_arc 끔/켬 | 12 |

## 못 뽑은 것

- **입 벌림에 의한 커서 밀림** — 가상 카메라가 입을 벌릴 때 코가 딸려 움직이는
  변형을 모형에 넣지 않았습니다. 시뮬레이션으로 재면 0이 나오는데 그건 사실이
  아닙니다. **실기 측정이 필요합니다.**
- **저조도 인식률** — 조명 모형이 없습니다. 실기 측정치는 `01_실기_측정치.md` 참고.
- **사용자 수행 지표**(과제 완료 시간, ISO 9241-411 처리량) — 참가자를 모집한
  실험이 없습니다. 논문의 가장 큰 공백입니다.
