# TODO — 작업 분해 및 진행 상황

작성: 2026-07-08 · 개정: 2026-08-03 (main 통합·py 직접 실행·실기 보정).
기획서(기획서.docx) 주차 계획·9장 체크리스트와 연동. **기획서 2.2/3.1/5.1은
2026-07-10(타깃)·2026-07-15(동작 체계) 변경을 반영해 개정 필요.**

## ✅ 완료 (2026-08-03 — main 통합 · py 직접 실행 · 실기 보정)

- [x] **몸통판을 main으로 승격(사용자 결정)** — 최종 추론판 = 포즈 머리 앵커
      (마스크·썬글라스·모자 무관) + 원거리 ROI 줌 + 머리 추론 스레드 분리.
      브랜치 merge 금지 규칙대로 checkout 방식 채택, 판 브랜치는 기록 보존
- [x] **공식 진입점 main.py** — 델파이가 `py main.py`로 직접 실행(bat 경유 제거).
      CWD 자동 고정·콘솔 QuickEdit 해제(터치 먹통 방지). 카메라 창은 실행 중
      stdin 명령(`cam on`/`cam off`)으로 토글 — 재실행 불필요
- [x] **가상환경 제거(사용자 결정)** — 시스템 파이썬 직접 설치. 연구소 시선추적은
      exe(파이썬 내장)라 환경 공유가 원리적으로 불가함을 확인 — 별개 프로세스
- [x] **bat 최소화 → install.bat 하나** — run.bat·run_debug.bat·eval.bat 삭제,
      make_offline_bundle.bat 흡수(wheelhouse 있으면 오프라인, 없으면 받아서 번들 생성)
- [x] **시작 최적화** — 카메라 오픈을 모델 로딩과 병렬화 + 프로브 기본 off:
      실측 20초+ → 1.3~3.4초(개발 PC). 단계별 시간 로그 상시 기록
- [x] **오프라인 구동 검증** — 소켓 전면 차단 상태로 설치·스모크·엔진 기동 정상
- [x] **실기 보정 세션** — 보정 하네스(feat/calibrate)로 "작게/크게" 동작 범위 측정
      → 쓸기 임계·손 폄 기준·탭 판정을 데이터에 맞춰 재설정 (실기 확인은 위 🟡)
- [x] **코드 설명서 신설**(docs/코드설명서.md) — 데이터 흐름·파일 지도·개념 사전·
      "고치려면 어디를" 표

## ✅ 완료 (2026-07-31 — 단일 손 추적 · 신규 제스처 · 현장 대응)

- [x] **단일 손 추적 리팩토링(사용자 결정)** — 좌/우 라벨을 손 정체성 키에서 제거하고
      공간 연속성으로 손 하나만 추적(획득=이동+모양 · 이음=재등장 반경 · 해제=유예).
      라벨 불안정에서 파생되던 버그 계열(획 씹힘·배구 토스·라벨 충돌)이 구조적으로
      소멸, 판정 코드 약 900줄→600줄. 좌/우는 이벤트 참고 정보로만 유지
- [x] **신규 제스처 4종(회사 요청)** — 손바닥 3방향(temp_left/temp_right/temp_top)
      + 한 손가락 제자리 더블 탭(click) → 이벤트 총 10종
- [x] **제스처 배치 개편(2026-08-03 사용자 결정)** — ①탐색 계층과 temp 계층의 손
      모양을 맞바꿈: 손 펼침 = left/right, 한 손가락 = temp 3종 ②select를 쓸기에서
      **모양 전환**으로 이동: 손 펼침 → 주먹 2회(gesture_filter `_update_grab_select`,
      config `gestures.grab_select`). 종전 "한 손가락 + 위 쓸기 = select"는 팔을
      들어올리는 예비 동작과 구분이 어려워 오발이 잦았다 — 게이트로 막던 경로를
      아예 없앴습니다. 이벤트명 10종은 불변이라 델파이 수신 코드는 그대로 동작합니다.
      ※단 `select`가 방향 동작이 아니게 됐다 — UI 분기에서 "포커스 위로"가 아니라
      "현재 항목 선택"에 걸어야 맞다 (№7 반영 확인 필요, 델파이 가이드에 명시)
- [x] **실기 버그 2건 해소(2026-08-03 사용자 보고)**
      ①"제스처 시전 중인 손보다 가만히 있는 손이 인식이 더 잘돼서 포커스가
        이동된다" — 제스처 손이 빨라 모션 블러로 끊기면 그 프레임엔 쉬는 손이
        마지막 추적점에 더 가까워 정체성을 승계했습니다. 반경으로는 못 고칩니다
        (끊긴 프레임엔 그 손밖에 없습니다). **내 손이 보이던 동안 함께 보이던 손은
        내 손이 아니다**를 궤적에 기억시켜(_other_tracks) 이음 후보에서 제외
      ②"내 몸에서 뻗어나가는 손이 아닌데도 남의 몸 손이 잡힌다" — 머리 중심
        반경 하나로는 원리적으로 못 가른다(팔을 뻗으면 내 머리~내 손 거리가
        옆 사람 손까지 거리와 같습니다). 포즈 **손목**(BlazePose 15·16)을 앵커에
        실어 `wrist_reach_head_widths` 3.0 심사 추가 — 판정 영역이 내 팔을 따라갑니다.
        실측: 내 손↔내 손목 최대 2.30(머리 폭 배수, 포즈 10 FPS·미검출 0) →
        3.0은 30% 여유. 실카메라 통과율 100%/100%/99.7%(평소·팔 끝까지·몸 앞)
      ③"아예 배에 있는 손에 고정돼 버린다" — ①은 "뺏기는" 경로만 막았습니다. 남은
        절반은 **한번 잡힌 손을 영영 안 놓는 것**(해제는 2초 소실뿐). 추적 손이
        `takeover_idle_sec`(1.5초) 동안 연속으로 놀 때만 인계를 허용한다 —
        획을 그리는 중엔 아무도 못 뺏는다(구 배구 토스 방지).
        실측: 조건을 "유휴"만으로 걸었을 땐 10초에 인계 4회로 추적이 두 손을
        오갔다 → 지속 시간 요구 후 인계 1회(두 손)·0회(한 손), 제스처 손 추적 77%
- [x] **연속 제스처 속도 개선(2026-08-04 사용자 요청)** — 실측으로 병목 확정:
      빠른 쓸기에서 추적률 92.7%·동작→이벤트 187ms로 파이프라인은 이미 따라갑니다.
      막는 건 `cooldown_sec` 0.9였습니다. 내리기 전에 **복귀 삼킴을 먼저 고쳤다** —
      쿨다운 중에도 궤적을 쌓아(판정만 차단) 삼킴이 "직전 획 끝 경유"를 판단할
      증거를 남깁니다. 0.9→0.5로 이벤트 최소 간격 1040→650ms.
      하한 0.4(0.3에서 한 획 재확정 — 시나리오 15)
- [x] **"배 위 손이 잡히도록"(사용자 제안) — 우선순위로 구현** — 높이로 **자르는**
      안은 실측이 반증했다: 낮은 제스처(어깨 아래 0.55~0.82)와 배 앞 손이 겹쳐,
      자르면 팔을 높이 못 드는 사용자가 못 쓴다(배리어프리 위배). 그래서 같은
      프레임 경쟁에서만 위쪽 손이 이기게 했다 — 손이 하나면 높이 무관하게 잡힙니다.
      ※보장 범위: 아래 손이 임계를 **먼저** 넘으면 그 손이 잡히고, 교체는
      인계 규칙(takeover_idle_sec)이 담당합니다
- [x] **다른 사람 난입 노이즈(2026-08-04 사용자 보고)** — 증상 3종(앵커가 상대에게
      옮겨감 · 인식 손이 꺼짐 · 골격 선 요동)이 **한 연쇄**였다: 포즈가 1인 고정
      (num_poses=1)이라 모델이 상대로 옮겨가면 내 머리가 관측에서 사라져 앵커가
      낡고 → 유예 후 해제 → 게이트가 통째로 꺼지고 → 상대가 앵커를 차지해 내 손이
      거부됐습니다. num_poses=2로 내 골격이 매 프레임 유지돼 연쇄가 시작되지 않습니다.
      더해 **손 소유자 판정** 신설 — 각 손에 대해 "누구 손목이 더 가까운가"로
      주인을 가린다(임계값 없는 상대 비교. 양쪽 다 신뢰 관문을 통과한 손목만 비교해,
      못 믿을 추정으로는 아무도 탈락시키지 않습니다). 비용 실측 11.2→11.5ms
      ※실기 확인 필요: 2인 상황은 사람이 한 명 더 있어야 재현됩니다
- [x] **앵커 고정(2026-08-04 사용자 결정 — "선은 인식되면 고정으로 박아야")** —
      사람이 여럿이면 내가 포즈 관측에서 잠깐 빠지는 일이 생기는데, 그때 유예가
      끝나 앵커가 풀리면 **가장 큰 사람**에게 다시 붙어 내 손이 통째로 거부됐습니다.
      → **추적 중인 손이 있는 동안엔 앵커를 풀지 않는다**(hand_select
      _drop_expired_head_anchor). 해제는 세션이 끝난 뒤(추적 정체성이
      release_sec 초과 소실로 풀린 뒤)에만 — 다음 사용자 인수는 그대로 됩니다
- [x] **앵커 기준 귀 → 어깨(2026-08-04 사용자 결정)** — "귀는 구레나룻이 길거나
      여자분이면 안 잡힐 수 있잖아." 가림 말고도 실측이 더 큰 이유를 보여줬다:
      귀-귀 폭 변동폭 정면 5.4% / **고개 돌림 71.1%**(130→47px) / 비스듬히 33.1%
      (어깨 3.4 / 8.5 / 5.0%). 임계 3종이 머리폭 배수라 곁눈질만으로 도달 반경이
      1/3로 쪼그라들었습니다. 단위를 어깨너비로 통일(정면 비율 3.16으로 환산 —
      reach 5.0→1.58 · wrist 3.0→0.95), 어깨선 추정 항목은 **삭제**(실측 어깨 y).
      ROI 줌 크롭도 머리폭 기반이라 곁눈질에 함께 쪼그라들던 것을 어깨 기준으로.
      ※중심은 머리 그대로 — 어깨로 내렸더니 위쪽 도달이 줄어 팔을 끝까지 뻗을 때
      게이트 통과율 100%→88.5%로 떨어졌다(실측 후 되돌림). 최종 98.9/97.2/97.7%
- [x] **머리 기준점 제거 — 앵커 중심을 어깨로(2026-08-04 사용자 결정)**
      사용자 보고: "기준점하고 선이 많이 흔들린다. 얼굴 쪽도 많이 흔들려서 없애라."
      실측이 뒷받침한다 — 귀 중점은 어깨 중점 기준으로 고개 돌릴 때 어깨너비의
      0.104배까지 흔들렸다(제자리 0.035의 **3배**). 자(尺)는 c5c03cb에서 이미
      어깨로 옮겼고, 이번에 중심까지 옮겨 **머리 좌표를 판정에서 완전히 뺐다**.
      표시(디버그 창 원)도 귀 좌표에서 앵커(어깨·EMA 평활)로 바꿨다 — 판정은
      멀쩡한데 화면만 요동치던 것이 사라집니다.
      ★**reach_shoulder_widths 1.58 → 2.09를 반드시 함께 간다**: 중심이 귀에서
      어깨로 내려간 만큼(어깨너비 × 0.51) 위쪽 도달이 줄기 때문입니다. 보정 없이
      중심만 내렸다가 팔을 끝까지 뻗을 때 게이트 통과율이 100%→88.5%로 떨어져
      되돌린 적이 있다(같은 날). 옆으로도 0.51 넓어지지만 옆 사람 손은 손목
      반경(0.95)이 가른다 — 그 심사를 08-03에 넣은 이유가 정확히 이것입니다.
      ※얼굴 **검출기**는 07-31에 이미 소멸(face_detector.py는 git 미추적·임포트
        없음·config 항목 없음). 이번에 남은 머리 **좌표 의존**까지 없앤 것입니다
      ※실기 확인 필요: 위쪽 도달(팔 끝까지 뻗기) 통과율과 옆 사람 손 유입
- [x] **select 즉시 발화 — dwell_sec 0.4 → 0 (2026-08-04 사용자 결정)**
      "주먹 접히는거 인식되면 바로 보내면 된다." 실기에서 굼떴다: 래치 전환
      0.27초(switch_frames 8) + 유예 0.4 ≈ **0.67초**라 체감 1초였고, 쥐었습니다
      **바로 펴면** 유예를 못 채워 통째로 씹혔다(사용자 보고).
      ⚠**감수한 것**: 유예가 이 경로의 유일한 방어였습니다. 손바닥 → 주먹 → 쓸기로
      명령을 내면 주먹 순간 select가 먼저 나가고, cooldown_sec(0.5)이 뒤이은
      쓸기를 삼켜 **명령이 사라진다** — 붙어 나오는 게 아니라 대체됩니다.
      시나리오 03c를 이 결정이 보이도록 뒤집어 두었다("왜 back이 안 나가지?"를
      다시 파기 전에 그 주석을 볼 것).
      ※한 손가락 → 주먹이면 select가 안 나간다(_has_seen_open) — 실사용에서
        거슬리면 dwell_sec **0.2~0.25**가 첫 후보다
- [x] **노이즈의 정체 — 포즈 어깨 관측이 붕괴한다(2026-08-04 실사용 로그로 특정)**
      사용자 보고: "기준점하고 선이 많이 흔들린다 · 검은 옷인데 주머니에 넣으면
      손을 못 찾고 손목 기준점이 흔들린다 · 추론 FPS도 낮아졌습니다."
      점프 로그(jump_log_ratio로 받아 적은 것)가 거의 **전부**
      `관측 1명 · 반경 안 후보 1`이었다 — 남에게 끌려간 게 아니라 같은 사람의
      어깨 관측이 무너진 것이다:
        어깨폭 209→18px(91%) · 73→192px(163%) · 163→12px(92%) · 188→21px(89%)
      12~21px는 사람 어깨가 아니다. BlazePose가 양 어깨를 거의 같은 점으로
      붕괴시키는 프레임이 있다(검은 옷 = 실루엣이 안 갈린다가 유력).
      **한 뿌리가 증상 셋을 다 만든다**: 반경 = 어깨폭 × 2.09라 어깨폭이 12px면
      반경 25px로 쪼그라들어 손이 통째로 거부되고(인식 끊김), 화면 원·중심·손목
      기준점이 전부 같은 관측에서 나와 함께 요동한다(흔들림).
      → **jump_reject_ratio 0.35** 신설: 어깨폭이 한 주기에 그 이상 변하면 그
        관측을 통째로 버린다(중심·폭·손목·어깨·팔꿈치 — 같은 붕괴에서 나옵니다).
        근거는 같은 날 실측 — 정상 변화율 중앙 0.02 · 95% 0.10 vs 붕괴 0.2~1.6.
        연속 5회 거부하면 받아들인다(앵커가 낡은 채 굳는 것 방지).
      ※실기 확인 필요: 흔들림이 잡혔는지 · 내 손이 잘리지 않는지
- [x] **중재기 슬롯 소실 유예(2026-08-05)** — 두 손 판정(hand_arbiter)이 손이
      한 프레임만 끊겨도 슬롯(필터째)을 버려서, GestureFilter의 소실 기계(궤적
      유예 0.35초 · 래치 유예 1.5초)가 아예 못 돌았다 — 빠른 쓸기의 모션 블러
      끊김에서 획이 통째로 리셋되는 구조(07-31 "획 씹힘"의 재림 경로).
      유예 신설: 경쟁 슬롯 0.5초(EXEMPT_FRESH_SEC와 같은 근거) · 확정 슬롯
      release_sec(2.0 — 추적 정체성과 같은 시계). 유예 중엔 필터에 None을 먹여
      소실 기계가 종전 단일 손 경로처럼 돕니다.
      ★구현 중 테스트가 승계 버그 2단을 미리 잡았다:
      ①유예 슬롯의 재등장 반경(4손폭)이 옆에서 계속 보이던 손을 훔친다 —
        쉬는 손이 궤적에 이어 붙어 가짜 획 발화
      ②거리 우선으로 바꿔도, 손이 유예 슬롯의 저장 위치를 **관통하는 프레임**엔
        그 손이 제 슬롯보다 진짜로 더 가깝다 — 거리로는 원리적으로 못 가릅니다
      → 유예 슬롯을 연속 이음(1차)에서 완전히 배제: 다른 슬롯이 연속으로
        따라오던 손은 재등장 후보가 아니다(hand_select _other_tracks와 같은 원칙).
      ※실기 확인 필요: 빠른 쓸기가 안 씹히는지
- [x] **추론 FPS 저하 — 두 손 판정이 만든 회귀(2026-08-04 사용자 보고)**
      candidate_signals가 게이트 통과 손 **전부**에 finger_states +
      classify_hand_shape를 매 프레임 돌렸습니다. 판정 손 하나만 계산하던 것이
      max_num_hands(4)까지 늘었습니다. → 지연 계산으로 교체(candidate_hands +
      signal_for): 중재기가 실제로 판정할 슬롯만 계산한다(확정 시 1개·경쟁 시 2개).
      테스트로 박음 — 확정 뒤 프레임당 1회, 경쟁 중 2회 이하
- [ ] **⚠겹침에서 앵커가 뒷사람에게 넘어간다 — 미해결, 실사용 로깅 중**
      (2026-08-04 사용자 보고 "뒷사람하고 나랑 겹치면 생김")
      2e772f5는 **위치가 떨어진** 통행인만 막았습니다. 겹치면 위치가 안 갈립니다.
      재현(단위): 겹쳐서 나를 놓친 프레임에 반경 안에 뒷사람만 남으면 min()이
      그를 고르고 EMA가 2프레임 만에 앵커를 넘긴다 → 내가 다시 잡혀도 **안 돌아옴**.
      어깨폭까지 316→222로 끌려가 도달 반경이 499→351px로 30% 줍니다.
      ★실측 2회(15:45·15:48)로 **크기 관문 안은 막혔다**:
        · 겹치면 포즈가 둘을 **동시에 못 잡는다** — 215관측 중 0회, 검출 문턱을
          0.5→0.3으로 낮춰도 같습니다. 문턱 문제가 아니라 구조적입니다
        · 그래서 5단계에서 **버그가 재현되지 않았다**(어깨폭 시계열 안 갈림
          0.917/0.900 — 혼자 있는 단계 0.950과 같은 수준). 뒷사람이 관측 자체가
          안 되면 앵커가 그쪽으로 갈 경로가 없습니다. 실제 조건이 이 단계와 다릅니다
        · **어깨 가시성 관문은 안 듣는다**(아래 별도 항목) — 이 안도 폐기
      → 현재: 실사용 로깅(jump_log_ratio 0.20)으로 증상 순간을 받아 적는 중.
        "관측 1명 · 반경 안 후보 1"인데 어깨폭이 크게 바뀌면 그게 지문입니다
      ※미확인: 겹침에서 2인 관측이 안 되면 cfd738a의 소유자 판정
        (_collect_other_wrists)도 상대편이 비어 무력하다 — 별도 확인 필요
      ★2026-08-07 실험(사용자 보고 재유입 — "뒷사람·옆사람에게 조작권 뺏김"):
        이 항목이 재구성한 실측(어깨폭 316→222px = 29.7% 변화)이 jump_reject_ratio
        0.35보다 낮다 — 그 관문이 원래 안 걸렸을 수 있습니다. 0.35→0.25로 낮춤
        (config.yaml 주석 참고). 이건 "넘어가는 것"만 늦출 뿐 "넘어간 뒤 안 돌아오는"
        문제(위 165행)는 그대로다 — 앵커가 살아있는 동안(뒷사람이 안 떠나면)
        복귀 경로 자체가 없습니다. 복귀 경로 설계는 미착수(재현 불가능한 채로 새
        상태기를 넣으면 구 "배구 토스" 재발 위험 — 로그로 실제 패턴 확인 후 설계할 것).
        ※"옆사람" 쪽은 이 항목과 별개 원인 — wrist_reach_shoulder_widths가
        전날(08-06) 삭제됐다가 오늘 복원됐다(config.yaml head_anchor 주석 참고,
        내 손 잘림과의 트레이드오프 있음)
      ★2026-08-07 2차(사용자 실기 재확인 — "뒷사람이 여전히 인식된다"): 위 0.25
        완화 뒤 실제 로그를 열어보니 이번 사례는 폭 관문 대상이 **아니었다** —
        1초 안에 점프 4연속인데 폭은 4~10%만 변하고 중심만 23~35%(어깨너비
        기준) 튀었습니다. 비슷한 크기의 옆/뒷사람 사이에서 위치만 바뀌는 패턴이라
        jump_reject_ratio(폭 전용)를 그냥 통과했습니다. → `_is_implausible_observation`을
        위치 관문(move_reject_ratio 0.20, jump_log_ratio가 이미 재던 move_ratio를
        관문으로 승격)까지 보도록 확장, 폭·위치 OR 조건으로 거부. 테스트 3건
        추가(test_hand_select.py ImplausibleObservationTest). "넘어간 뒤 안
        돌아오는" 문제(위 165행)는 여전히 별개 — 이건 안 넘어가게 하는 쪽만 보강
      ★2026-08-07 3차(사용자 실기 재확인 — "뒷사람은 안 잡히는데 뒤에 사람 있으면
        내 손도 안 잡힘"): move_reject_ratio 실측 로그를 다시 보니 뒷사람이 있을
        때 자세 추정 자체가 **상시로 노이즈**를 냈다(같은 세션에서 어깨폭 40~
        60%대·중심 60~69% 튐이 반복). 매 프레임 독립 판정(폭·위치 관문)만으로는
        REJECT_STREAK_LIMIT(5회=0.5초)까지 매번 얼어붙어 그 사이 내 손이 반경
        밖으로 밀린다 — 값을 더 풀면 이번엔 진짜 하이재킹도 통과시킨다(트레이드
        오프 해소 불가, 임계값 하나로는 한계). → **2연속 합의 빠른 수용** 도입
        (hand_select._update_head_anchor·_accept_anchor_frame): 거부된 관측끼리
        서로 가까우면(노이즈면 매번 다른 방향, 합의는 실제 변화의 지문) 스트릭
        한도를 안 기다리고 2번째에 바로 받아들입니다. 실제 사람의 움직임(연속성
        있음)과 노이즈(방향 무작위)를 구분하는 원리 — 단일 프레임 임계값보다
        견고합니다. 테스트 2건 추가. 실기 재확인 필요: 뒷사람 있을 때 내 손이
        다시 잡히는지 + 하이재킹 억제가 유지되는지 둘 다
      ★2026-08-07 4차(사용자 실기 재확인 — "뒤에 사람 있으면 내 손이 여전히
        안 잡힘", 3차 배포 뒤 재발): 로그를 다시 보니 3차의 전제가 틀렸다 —
        뒷사람이 있을 때는 **단발성 노이즈가 아니라 지속적 드리프트**였습니다.
        hands=1(활성 추적 유지) 상태에서도 앵커가 30~41%씩 4번 연속 이동했습니다.
        2연속 합의는 "노이즈는 랜덤, 진짜 변화는 일관됨"을 전제로 하는데, 이
        지속 드리프트는 일관되게(같은 방향으로) 튀기 때문에 오히려 2연속 합의를
        "진짜 변화"로 더 쉽게 착각시켰다 — 3차 수정이 의도와 반대로 작동한
        셈. → 손이 **지금 활성 추적 중이면** 2연속 합의 빠른 수용 자체를 끄고
        REJECT_STREAK_LIMIT(느린 경로)만 허용하도록 추가 제한
        (_drop_expired_head_anchor가 이미 쓰는 "사용 중엔 안 놓는다" 원칙을
        점프 수용에도 적용). 활성 추적이 없을 때(제스처 사이·대기)는 여전히
        빠르게 재동기화. 테스트 1건 추가. ※반복 패턴 주의: 이 항목은 이제 4번
        연속 "실기 재확인 필요"가 붙었다 — 다음에도 재발하면 값 조정보다
        **화상으로 직접 관찰**(사용자와 함께 실시간으로 로그·화면 대조)을
        고려할 것, 원격 로그 판독만으로는 한계에 가까워 보입니다
      ★2026-08-07 5차(사용자 실기 재확인 — "내 손이 안 잡히거나 뒷사람 손이
        인식됨", 4차 배포 뒤에도 재발): 로그 대조로 **4차가 막지 못한 두 번째
        뒷문**을 찾았다 — 2연속 합의 빠른 경로는 막았지만, REJECT_STREAK_LIMIT
        (5회 연속 거부 후 강제 수용, "앵커가 굳는 것 방지" 안전판)은 활성 추적
        여부와 무관하게 그대로였습니다. 실기 로그에 정확히 이 경로로 44%·37% 점프가
        들어간 게 보인다("거부가 5회 연속(합의도 없음) — 받아들인다" 직후
        큰 폭 점프). → REJECT_STREAK_LIMIT_WHILE_TRACKED(30 = 5의 6배, ≈3초)
        신설 — 활성 추적 중엔 안전판도 훨씬 느리게, 추적 없을 때는 종전 5 그대로.
        테스트 1건 추가(341개 전체 통과). 이걸로 "앵커 점프를 받아들이는 두
        경로"(2연속 합의·스트릭 한도) 둘 다 추적 상태를 본다 — 같은 함수 안에
        더 남은 경로는 없는지 다음에도 재발하면 확인할 것
      ★2026-08-07 6차(사용자 실기 재확인, 5차 배포 뒤에도 재발) — **근본 원인을
        찾았다**: 3~5차의 "활성 추적 중엔 얼려서 보호" 자체가 역설이었습니다. 실기
        로그: 10:43:18에 손이 확정 추적됐는데, 불과 5초 뒤인 10:43:23에 "추적
        **없을 때만** 도는" 2연속 합의 로그가 다시 찍혔다 — 그 사이 손 추적
        자체가 끊겼다는 뜻입니다. 메커니즘: 앵커를 완전히 얼리면(0% 반영) 실제
        손 위치와 어긋나기 시작 → 어긋난 반경 밖으로 내 손이 밀려남 →
        release_sec(2초) 뒤 추적 해제 → "추적 없음" 상태가 되어 보호가 스스로
        풀림 → 그 순간 아무 관측(뒷사람 포함)이나 빠르게 앵커를 차지함.
        **보호가 보호 대상(손 추적)을 죽이는 자기파괴적 순환**이었습니다.
        → 완전히 얼리는 대신 TRACKED_DRIFT_ALPHA(0.1)로 **살살 계속 따라가게**
        바꿨다(hand_select._accept_anchor_frame에 alpha 매개변수 추가,
        _update_head_anchor 3분기로 재구성: 정상 수용 / 활성 추적 중 살살 수용
        / 추적 없을 때 2연속 합의·스트릭). 저역통과 필터 원리 — 노이즈는
        방향이 들쭉날쭉해 누적 상쇄되고 진짜 이동만 쌓입니다. 기존 테스트 2건을
        새 기대값(정확히 고정이 아니라 "스냅되지 않고 원점 근처")으로 갱신,
        341개 전체 통과. ★튜닝 포인트: 내 손이 여전히 밀리면 TRACKED_DRIFT_ALPHA
        를 살짝 올릴 것(0.1→0.15 등), 하이재킹이 새면 내릴 것 — 단 0에 가깝게
        내리면 3~5차와 같은 역설이 재발함을 기억할 것(완전한 0은 절대 금지)
      ★2026-08-07 7차 — 화면 캡처로 직접 확인(로그만으로는 한계 — 사용자 제안):
        캡처한 순간들은 전부 앵커·손 인식이 정확했다(하이재킹 못 잡음). 대신
        더 근본적인 문제 발견: **FPS가 3.7~7.0** — 포즈 10Hz(≈100ms 간격) 가정
        위에서 계산한 모든 임계값(폭·위치 변화율)이 애초에 안 맞았을 수 있습니다.
        실제 프레임 간격이 그보다 훨씬 벌어져 있으면 정상 움직임도 "그 사이
        못 변한다"는 판정에 걸린다 — 지금까지의 임계값 튜닝 전체가 잘못된
        전제 위에 있었을 가능성. FPS 저하에 이 세션에서 추가한 로깅(파일 핸들러가
        호출마다 동기 flush)이 일부 기여했을 가능성 — 조사·경량화 필요(미착수)
      ★2026-08-07 8차(사용자 요청 — "논문·사례 조사" + "다 구현해봐") — MOT(다중
        객체 추적) 문헌 조사 결과 이 문제는 정확히 "ID Switch"라는 이름이 있는
        잘 알려진 문제였다: MediaPipe Pose는 애초에 다중 인물 신원을 안정적으로
        유지하도록 설계되지 않았고("single person...for tracking multiple people
        with stable identity, developers need to implement their own solution"),
        순수 기하 추적(SORT)은 "lost identities through occlusions and crossings
        because it used only geometry"로 이미 알려진 한계입니다. DeepSORT가 외형
        임베딩을 더해 ID Switch를 45% 줄인 선례를 따라 **상체 색상
        히스토그램**(HSV H·S, OpenCV 표준 기법 — 새 무거운 의존 없음)을 도입:
        head_detector.HeadDetection에 appearance 필드 신설(torso_color_hist가
        어깨 사이 영역에서 추출), hand_select의 "활성 추적 중 튐" 판정에 3번째
        신호로 반영 — 외형 일치(거리≤0.3)면 기하가 튀어도 정상 속도로 수용,
        명백히 다르면(거리≥0.7) 살살 따라가는 것도 안 하고 완전 거부, 애매하면
        (0.3~0.7 또는 추출 실패) 종전 TRACKED_DRIFT_ALPHA로 절충. 테스트 3건
        추가, 344개 전체 통과. ⚠한계: 두 사람 옷이 비슷한 색이면(실기 스크린샷
        확인 — 이 세션 테스트 인원 둘 다 어두운 상의) 변별력이 떨어진다 —
        은탄환 아님, 보조 신호. 같은 요청으로 hand_arbiter.MAX_SLOTS도 2→1로
        되돌림(파일 자체 주석 참고) — 한쪽 손이 확정되면 다른 손은 슬롯조차
        못 받아 구조적으로 혼입 여지가 없어졌습니다
      ★2026-08-07 9차(사용자 실기 재확인, 8차 배포 뒤에도 재발) — **8차가 도입한
        외형 일치 경로 자체가 새 뒷문이었다**. 배포 직후 로그: 1초 안에 "앵커
        관측 수용: 기하는 튀었지만 외형 일치(거리 0.16~0.28)"가 3번 찍혔다 —
        기준(match_ratio 0.3)을 넘겨서 **정상 속도로**(살살도 아니고) 뒷사람
        쪽으로 넘어갔습니다. 두 사람 다 어두운 상의라 실제로는 다른 사람인데도
        히스토그램 거리가 낮게 나온 것 — 8차 도입 시 이미 적어둔 그 한계가
        실제로 이 세션에서 터진 것입니다. 즉 외형 신호가 방어가 아니라 **가속
        페달**이 됐습니다. → match_ratio 0.3→0.12로 대폭 강화(config.yaml 주석
        참고). ⚠이번에도 실측 없이 감으로 낮춘 값 — 그래도 재발하면 이
        옷 조합(둘 다 어두운색)에서는 색상 히스토그램의 변별력 자체가
        구조적으로 부족한 것으로 보고, match 경로를 아예 비활성화(값을 0에
        가깝게)하거나 다른 서술자(에지·질감)를 검토할 것. mismatch_ratio(0.7,
        "명백히 다른 사람"→완전 거부) 쪽은 이번 로그에서 오작동 증거가 없어
        그대로 둠
      ★2026-08-07 10차(사용자 결정 — "그거 빼자", 재확인: 0.12로도 여전히
        재발) — 외형(색상 히스토그램) 신호를 **config에서 제거해 비활성화**
        했다(코드는 남김 — appearance_match_ratio·appearance_mismatch_ratio
        키를 다시 넣으면 켜집니다). 옷 색이 뚜렷이 다른 사용자 조합으로 먼저
        검증되기 전엔 다시 켜지 말 것. 앵커 상태는 6차(살살 드리프트) 기준으로
        복귀. 같은 시점에 hand_arbiter.MAX_SLOTS 2→1도 반영(위 9차 이전 항목).
        ※그래도 뒷사람 문제가 남아있다는 사용자 보고 계속됨 — 지금까지 손댄
        모든 방어(2연속 합의·추적중 드리프트·외형)는 **앵커가 이미 존재하는
        상태의 갱신**에만 적용됩니다. 아직 안 건드린 지점: **최초 획득**
        (_update_head_anchor의 `if self._head_anchor is None:` 분기)은
        지금도 순수히 "가장 넓은 어깨"만으로 고른다 — 앵커가 소실 유예
        (1초)마다 자주 통째로 풀렸다 재획득되는 로그 패턴(앵커 획득 이력이
        세션 내내 수십 번)을 보면, 이 무기억 재획득이 뒷사람에게 넘어가는
        진짜 진입점일 가능성이 높습니다. 다음엔 여기부터 볼 것
      ★★2026-08-07 13차 — **깊이(거리) 관문 신설. 이번엔 실측이 설계를
        검증했다** (사용자 요청 — "모든 기술력을 다 찾아서 뒷사람 손이 인식
        안되게"). 1~12차가 전부 실패한 이유가 구조적으로 설명된다: 시도한
        방어가 **2D 위치**(반경·손목 거리·소유자 비교)와 **화면 가림**(실루엣
        블러) 두 축에만 있었는데, 뒷사람은 그 두 축 **어디에도 안 잡히는**
        위치에 선다 — 내 뒤에 서면 화면에서는 내 옆·내 위에 겹쳐 보이고,
        거리를 모르면 그 손과 내 손이 2D상 구분되지 않습니다. 8차 MOT 문헌
        조사의 "geometry만으로는 occlusion에서 진다"가 정확히 이 얘기였는데,
        그때는 답을 외형(색상)에서 찾다 실패했다(9·10차).
        빠져 있던 축은 **깊이**였고, 이 엔진은 이미 그걸 측정하고 있었다:
          hand_shoulder_px = span_px / span_m × 0.4
        인데 span_px = f × span_m / d 이므로 **손 크기가 약분되고 f/d만 남는다**
        — 즉 이 값은 처음부터 순수한 거리 측정치였습니다. 자(尺)로만 쓰던 값입니다.
        새 모델·새 의존·새 관측 없이 관문 둘을 세웠습니다
        (hand_select._is_far_person_hand):
        ① **절대**(depth_reject_ratio) — 손 깊이 ÷ 앵커 어깨너비. 뒷사람만
           손을 든 순간처럼 **비교할 손이 없을 때** 듣습니다
        ② **상대**(depth_rel_reject_ratio) — 손 깊이 ÷ 최근접 손 깊이. 체형
           상수가 약분돼 사라져 보정이 아예 필요 없다(_is_other_persons_hand의
           "임계값 없이 풀린다"와 같은 성질). 내 손과 함께 보일 때 강합니다
        ★배치가 중요하다: 이 관문은 **추적 면제보다 앞**에 둡니다. 면제는 위치
        예외인데, 뒷사람 손이 한 번 추적에 들면 위치 심사를 통째로 면제받아
        영영 못 쫓아냈다 — 오늘 반복된 "한번 뺏기면 안 돌아온다"의 구조적
        뒷문이 거기였습니다. 거리는 획 중에 뒤로 물러나지 않으므로(뻗으면 오히려
        가까워집니다) 이 관문이 내 획을 자를 이유가 없습니다.
        ★★실측 검증(배포 직후 12:47~12:48, 13샘플) — **완전한 이봉분포**:
             내 손(통과) 1.12 1.15 1.20 1.22 1.26
             먼 손(거부) 0.59 0.60 0.64 0.65 0.69 0.76 0.77 0.78
           **0.78~1.12 구간이 통째로 비었다**. 같은 사람의 관측 노이즈라면
           연속적으로 흩어져야 하는데 두 무리로 갈렸다 — 물리적으로 다른
           두 거리를 실제로 측정하고 있다는 직접 증거입니다. 오늘 하루 처음으로
           "값을 감으로 정하고 재발을 기다리는" 게 아니라 **분리가 데이터에
           보이는** 상태가 됐습니다. 그 빈 구간 안에서 초안 0.70 → **0.80**으로
           올림(내 손 최솟값 1.12에 40% 여유). 0.70이면 0.76~0.78 무리를
           절대 관문이 놓친다(그 셋은 상대 관문이 잡았다 — 두 관문을 겹친
           설계가 실제로 값을 했다는 근거이기도 합니다).
           로그(_log_depth_verdict)는 통과·거부 비를 **둘 다** 남긴다(2초
           throttle) — 거부만 남기면 "안 잘렸다"만 알고 여유가 얼마인지
           모릅니다. 튜닝의 근거를 남기려는 것.
        테스트 8건 추가(DepthGateTest — 절대·상대 각각 단독 동작, 내 손·
        앞으로 뻗은 손 생존, 추적 면제 뒷문 봉쇄, 깊이 미측정 시 fail-open),
        전체 372개 통과.
        ⚠남은 한계 둘: ①표본이 **한 사람·한 세션·한 거리**다 — 넓은 체형은
        기하상 0.83~0.95까지 내려갈 수 있어 여유가 준다(다른 체형에서 "내
        손이 안 잡힌다"가 나오면 이 값부터 ↓). ②**바로 뒤(수십 cm)**에 붙은
        사람은 깊이 차가 작아 못 가른다 — 단일 카메라의 물리적 한계이고,
        그 거리면 화면상 크기도 거의 같아 어떤 방법으로도 못 가르는 영역입니다
      ★★2026-08-07 21차(정리) — 사용자 요청("일단 정리해봐")으로 죽은/위험
        코드 2건 제거. 둘 다 새 기능이 아니라, 남겨두면 **나중에 같은 버그를
        되돌리는 지뢰**라 걷어냈습니다.
        ①**외형(옷 색상) 신원 확인 코드 전체 삭제** — head_detector의
        torso_color_hist·appearance_distance·HeadDetection.appearance 필드,
        hand_select의 appearance_confirms/denies 분기, 관련 config 키·테스트
        3건·헬퍼(solid_color_hist). 8차에 DeepSORT 발상으로 도입했다가 9차에
        "테스트 인원 둘 다 어두운 상의라 다른 사람을 같은 사람으로 오판 —
        방어가 아니라 가속 페달"로 실기에서 역효과가 확인돼 10차에 config
        키만 뺀 비활성 상태로 남아 있었습니다. 그 방치가 위험했다 — 같은 날 학습
        기준선(20차)이 "왜 안 쓰이지" 하고 되살아나 본인 손을 잘랐던 것과
        같은 함정이 여기도 있었습니다. 부작용이 실기로 **확정된** 기능은 비활성화가
        아니라 삭제합니다.
        ②**segmentation_mask_hold_sec 2.0 → 1.5로 축소**. 16차에 깜빡임(디버그
        창 한정 증상) 방지로 늘렸는데, 재검토하니 반대 방향 위험이 더 크다 —
        오래 붙든 마스크는 **낡은 위치**라, 사용자가 움직이는 중에 그 값으로
        블러하면 지금 손이 있는 자리가 가려져 **손 모델이 실제로 손을 못
        본다**. 화면 표시 문제보다 기능 실패가 더 나쁘므로 방향을 뒤집었습니다.
        다만 anchor_grace_sec(1.0)보다는 반드시 길어야 하므로(같은 값이면
        같이 만료돼 무의미 — 16차에서 이미 겪은 실수) 1.0 밑으로는 못 내립니다.
        이 제약을 테스트로 고정(test_hold_must_outlast_the_anchor_grace).
        테스트 정리 후 전체 417개 통과(외형 테스트 3건 제거로 순감소).
      ★★2026-08-07 20차 — **배포 시나리오 확정: 배리어 프리 키오스크 · 동사무소·
        법원 등 사람 많은 곳**(사용자). 요구는 "조종권을 뺏기면 안 된다".
        이 맥락이 두 가지 판단을 바꿨습니다.
        ①**고정 깊이 임계의 구조적 한계**가 배리어 프리에서 특히 위험합니다.
          비 = (0.4 / 내_어깨m) × (d_앵커 / d_손) — **앞 항이 체형, 뒤 항이
          거리**인데 임계 하나로 둘을 동시에 측정하고 있었습니다. 좁히면 어깨 넓은
          사용자의 자기 손이 잘리고(실측 기준선 1.07~1.76은 한 사람 값이고
          어깨 0.42m면 0.95까지 내려와 고정 0.80에 여유 19%뿐), 넓히면 뒷사람이
          샙니다. 배리어 프리에서 전자는 **정작 그 키오스크가 위해 만들어진
          사람이 못 쓰게 되는** 실패라 특히 나쁩니다.
          → 앞 항은 **이 사람의 상수**다(같은 사람 손이면 d_손 ≈ d_앵커라
          서 있는 거리가 바뀌어도 불변). 추적이 확정된 손 = 본인이 확실한
          손의 비를 9개 모아 중앙값으로 그 상수를 직접 학습합니다
          (_learn_user_depth_ratio, depth_baseline_reject_ratio 0.80).
          그 뒤로는 순수한 거리 기준이 된다:
            기준선 1.2 사용자 -> 실효 임계 0.96 (하이재킹에 **더 엄격**)
            기준선 0.9 사용자 -> 실효 임계 0.72 (체형에 **더 관대**)
          하나의 값으로 상충하던 둘이 **동시에** 좋아집니다. 세션 종료 시 반드시
          초기화한다 — 앞사람 체형으로 다음 사람을 심사하면 그게 배리어 프리 실패입니다.
        ②**takeover_idle_sec 1.5 -> 5.0**. 인계는 "추적 손이 논다"만으로 열리는
          유일한 정상 경로인데, 배리어 프리 사용자(고령·장애인·휠체어)는 화면을
          읽거나 다음 동작을 준비하며 **수 초씩 멈추는 것이 정상 사용**입니다.
          1.5초면 그 정상적인 멈춤마다 대기줄에 도전 틈이 열렸습니다. 법원·동사무소
          민원 화면에서의 조작권 탈취는 단순 불편이 아니라 개인정보·업무 중단
          문제입니다. 깊이 관문이 뒷사람 손을 먼저 막지만 그건 마지막 방어선이고,
          애초에 **틈을 열지 않는 것**이 먼저다(다층 방어).
        테스트 4건 추가(전체 398 통과).
        ※미해결로 남기는 것: min_operating_depth_px(300)는 개발 PC 실측이라
          현장 재보정이 필수입니다. **휠체어 사용자는 앉은 자세라 거리·높이가
          달라** 특히 확인이 필요하다 — 이 값이 너무 높으면 정당한 사용자가
          통째로 거부된다(배리어 프리 최악의 실패). config에 보정 절차 있음.
      ★★2026-08-07 19차 — **블러를 껐습니다. 조작권 문제는 해결됐다**
        (사용자 결정 A안). 하루 동안 6차에 걸쳐 다듬은 기능을 같은 날 끄는
        판단이라 근거를 남깁니다.
        결정적 정보 두 가지가 사용자에게서 왔다:
          ①"손으로 키오스크 조작되는 경우는 거의 없는거같아" — **원래 목표
            (조작권 탈취)는 달성됐습니다.** 그 일은 블러가 아니라 깊이 관문이 했습니다.
          ②"뒷사람이 화면에 보이는것 자체" — 남은 증상은 **표시 문제**였습니다.
        그리고 main.py 확인 결과 카메라 창은 --debug/cam on에서만 열린다 —
        제품은 stdout으로 이벤트만 보내므로 **고객 화면에는 영상이 없습니다.**
        즉 남은 증상은 디버그 창 한정이고 제품 영향이 0입니다.
        결정타는 **마스크 품질 실측**이었다(진단 로그 신설해 받은 값):
            2인 상황 — 앵커 마스크 38~55% · 타인 마스크 10~57%
            (혼자일 때 2~4%)  한 번은 49.0% + 56.7% = **105.7%**
        두 마스크가 크게 겹친다 = 깨끗한 사람별 실루엣이 아니다. 픽셀 기반
        분리의 **전제 자체가 실기에서 성립하지 않는다**. 더 나쁘게는 18차의
        뺄셈이 겹치는 만큼 **사용자 본인 몸을 블러**하므로, 놔두면 해로울 수
        있었다(내가 "옳은 방식"이라 판단해 넣은 것이 전제 붕괴로 역효과가 된 사례).
        → isolate_anchor_blur.enabled: false + output_segmentation_masks: false
          (마스크를 쓰는 곳이 없어졌으므로 산출 비용도 함께 제거).
          코드·테스트·config 주석은 전부 남겼다 — 마스크 품질이 개선되면
          (IR 전환·모델 교체) 두 키만 켜면 복귀합니다.
        ★남긴 방어(전부 블러와 무관하게 동작):
          깊이 관문 절대·상대 · 작동 거리 창 · 대기줄 이음 방어 · 관측 중앙값
        ※교훈: "증상이 남았다"에 계속 기능을 덧대는 대신, **그 증상이 제품에
          영향이 있는지**를 먼저 물었어야 했습니다. 6차에 걸쳐 다듬은 뒤에야
          "고객은 이 화면을 안 본다"를 확인했습니다.
      ★★2026-08-07 18차 — **버리고 있던 정보를 쓴다: 앵커 아닌 사람의 마스크를
        명시적으로 뺀다**(사용자 요구 — "뒤에있어도 앵커 가까이 있으면 사람이
        2명으로 인식되 앵커 잡힌 사람만 보이고 앵커 안잡힌사람은 여전히
        블러처리 되어있으면 좋겠는데").
        종전 구조의 맹점: 포즈는 **사람별 실루엣을 전부** 주는데, 엔진은 앵커
        것만 쓰고 나머지를 그냥 버렸습니다. "누가 앵커가 아닌가"를 픽셀 단위로 이미
        알고 있으면서 추정에 맡기고 있었던 셈입니다. 그래서 앵커 마스크의 유출
        (떨어져 있을 때 실측 ~7%, 붙을수록 커집니다)이 그대로 상대 몸 위에 남았습니다.
        → _collect_other_masks 신설: 앵커가 아닌 사람들의 실루엣을 성분별
        최댓값으로 합쳐 두고(임계는 소비자가 정하는 값이라 여기서 안 박습니다),
        blur_outside_mask의 exclude_mask로 빼냅니다. 두 실루엣이 겹치는 애매한
        구간은 **흐리는 쪽으로** 판정된다 — 사용자가 요구한 방향이고, 유출이
        정확히 상대 실루엣 위에서 일어나므로 이 뺄셈이 그 지점을 정조준합니다.
        ★순서: **팽창(dilate) 뒤에** 뺍니다. 먼저 빼면 팽창이 도로 상대 몸으로
        번져 뺀 의미가 없어진다(팽창은 내 손끝을 살리는 장치이지 남을 덮으라는
        장치가 아니다). 테스트가 이 순서를 고정합니다.
        테스트 3건 추가(전체 394 통과).
        ※합성으로 "가까울 때 유출량"을 재려 했으나 **2인 검출이 재현되지
          않았다**(간격 90~500px 전부 1명). 유출 크기를 모르는 채로도 옳은
          방식이라 그대로 진행했다 — 크기에 의존하지 않는 해법입니다.
      ★★2026-08-07 17차 — **깜빡임의 진짜 원인 특정: 앵커는 멀쩡한데 chosen이
        두 사람 사이를 오간다**(사용자 — "아직도 그래", 16차 배포 뒤에도 재발).
        16차는 "앵커가 풀려 블러가 꺼진다"를 고쳤는데, 재발 시점 로그에는
        **앵커 해제·획득이 0건**이었다 — 다른 경로입니다.
        로그가 지문을 그대로 남겼습니다. `반경 안 후보 2`가 **55회**, 그 순간의 점프가
            어깨폭 173->462px(167%) · 중심 이동  4px
            어깨폭 289->529px( 83%) · 중심 이동 11px
            어깨폭 385->575px( 49%) · 중심 이동 13px
        **중심은 거의 안 움직이는데 폭만 49~167% 요동칩니다.** 같은 사람의 노이즈일
        수 없다(폭이 그렇게 변하면 중심도 움직입니다). 일렬로 선 두 사람 —
        2D 위치는 같고 거리만 다르다 — 사이를 chosen이 오간 것입니다.
        구조적 이유: 이음(_update_head_anchor)은 후보를 **위치로만** 고릅니다.
        2026-08-04에 "크기로 고르면 지나가는 사람에게 끌려간다"는 실기 근거로
        일부러 그렇게 바꾼 것인데, **대기줄은 정확히 그 규칙의 사각지대**다
        (옆으로 지나가는 사람에는 맞고, 뒤에 서는 사람에는 완전히 틀립니다).
        그 결과 _accept_anchor_frame이 저장하는 실루엣 마스크가 함께 오가서
        뒷사람이 보였다 사라졌다 한다 — **앵커가 풀리지 않아도** 생기므로
        16차의 마스크 유지 시간으로는 잡히지 않았습니다.
        → _drop_farther_candidates 신설(continuity_depth_ratio 0.80):
        이음 후보 중 어깨너비가 앵커의 80% 미만인 사람을 뺍니다. 위치로 못 가르는
        것을 **거리**로 가릅니다. 값 근거: 교대 사례의 폭 비가 0.37·0.55·0.67·0.77
        이고 같은 사람의 폭 변화는 중앙값 필터 후 실측 0~6%라 사이가 넓습니다.
        ★비대칭 설계: **더 먼(작은) 후보만** 뺀다 — 더 가까운 후보는 남기므로
        앵커가 어쩌다 뒷사람에게 가 있어도 앞사람으로 되돌아올 수 있다(가두지
        않습니다). 전부 빠지면 원래 목록 사용(fail-open).
        테스트 4건 추가(전체 391 통과). 그중 하나는 **관문을 끄면 뒷사람이 실제로
        chosen을 뺏는다**를 확인해, 이 관문이 진짜로 무언가를 막고 있음을 보증합니다.
        ※교훈: 16차에서 "앵커 해제 39회"라는 강한 증거를 잡고도 그것이 **유일한**
        원인이라 단정했습니다. 같은 증상에 경로가 둘이었다 — 고친 뒤 재발하면
        "덜 고쳤나"가 아니라 "다른 경로인가"를 먼저 볼 것.
      ★★2026-08-07 16차 — **블러 깜빡임의 원인 ①: 블러가 통째로 꺼지는 구간**
        (사용자 보고 — "앵커 잡힌 사람은 누끼 잘 따지는데 앵커 안잡혀있는 뒤에
        있는 사람의 얼굴이랑 손이 보였다가 사라졌다가 반복").
        먼저 **틀린 가설을 배제했다**(합성 2인 프레임 검증): 마스크[i]가
        pose_landmarks[i]와 어긋나 앵커에게 남의 마스크가 붙는 것 아닌가 —
        24건 검사 0건 불일치로 **순서는 정확했다**. 코드가 검증 없이 기대던
        가정이었는데 이제 확인됐습니다.
        실제 원인은 코드 경로였다:
          앵커 해제 → _anchor_segmentation_mask = None
                    → 폴백 anchor_keep_sharp_box(None, ...) = None
                    → blur_outside_region(frame, None)은 **원본 그대로 반환**
                    → **블러가 사라져 뒷사람까지 전부 선명**
        실측 로그: 한 세션에 **앵커 해제 39회 · 획득 62회** — 그만큼 깜빡였습니다.
        앵커가 잠깐 끊기는 건 사용자가 떠난 게 아니라 거의 항상 검출 공백입니다
        (로그상 해제와 재획득이 같은 초에 찍힙니다).
        → **마스크 유지 시간**(segmentation_mask_hold_sec 2.0) 도입: 앵커가
        풀려도 직전 마스크를 붙들어 블러를 유지하고, 유지 시간이 지나야 버립니다
        (낡은 마스크가 다음 사람에게 승계되지 않는다는 원래 목적은 유지).
        ★설계 함정 하나를 **단위 테스트가 잡았다**: 유지 시간을 처음에 1.0으로
        뒀는데 anchor_grace_sec와 같은 값이라, 둘 다 "마지막 관측"부터 재므로
        앵커가 풀리는 바로 그 순간 마스크도 함께 만료돼 **공백을 하나도 못
        메웠다**. 2.0으로 올렸다 — 이 값은 반드시 anchor_grace_sec보다 길어야
        의미가 있다(config에 명시).
        ※같은 작업에서 스크립트 일괄 치환이 들여쓰기 때문에 이중 매칭돼 코드가
        깨진 것을 발견·수정했다 — 문자열 치환으로 코드를 고치지 말 것.
      ★★2026-08-07 15차 — **실사용 시나리오 확정: 대기줄**(사용자 — "키오스크
        줄설때 한사람씩 뒤로 서잖아 그때 인식 안되면되는거야"). 오늘 14차까지
        "뒷사람"을 막연히 다뤘는데, 실제 상황이 **일렬 대기줄**로 특정됐습니다.
        이게 왜 중요한가: ①줄은 정의상 뒤에 서므로 화면(2D)에서 앞사람과
        겹친다 — 위치·반경·손목 비교로는 **원리적으로** 못 가른다(1~12차가
        전부 그래서 실패했다는 것이 사후에 설명됩니다) ②거꾸로 이 시나리오는
        **깊이에 가장 유리**하다: 줄 간격(보통 0.6~1m)이 그대로 깊이 차가 됩니다
        ③사용자가 서는 자리가 사실상 고정이라(바닥 표시·화면 위치) 앵커에
        의존하는 비율 관문보다 **절대 하한(min_operating_depth_px)이 더
        정확하고 안정적**이다 — 주력을 그쪽으로 봐야 합니다.
        환산(사용자 1.0m 기준): 뒷사람 1.3m → 깊이비 0.77 · 1.5m → 0.67 ·
        1.8m → 0.56. 현재 depth_reject_ratio 0.80은 **줄 간격 50cm 이상**을
        잡습니다. 30cm까지 잡으려면 값을 올려야 하는데 내 손 최솟값(1.07)과의
        여유가 급격히 줄어든다 — **바닥 대기선을 1m 뒤에 긋는 운영**이 값을
        올리는 것보다 안전하다(설치 권고사항으로 회사에 전달 필요).
        config에 시나리오·환산표·**현장 보정 절차**를 적어 뒀다(설치 시 1회:
        사용 위치에서 로그의 "손 깊이 NNNpx" 최솟값을 읽고 그 0.75~0.8배로
        min_operating_depth_px를 잡습니다). ⚠현재 300은 개발 PC 실측이라
        현장 설치 시 반드시 재보정할 것 — 카메라 거치 높이·거리가 다르면
        그대로 쓰면 안 됩니다.
      ★2026-08-07 15차 성능 — **블러 연산 2.4배 단축**(코드 리뷰 [중간] 지적):
        종전엔 720p 원본에 51×51 가우시안을 매 프레임 돌렸습니다. 벤치마크
        (동일 PC, 30회 평균): **30.8ms → 13.0ms**. 1/4로 축소해 13×13을
        걸고 되돌리는 표준 기법으로 연산량이 ~1/16이 된다 — 목적이 "디테일
        파괴"라 축소-확대 화질 저하는 오히려 취지에 부합한다(축소 자체가
        저역통과입니다). 프레임당 17.8ms 회수. ※end-to-end FPS는 6.4로 나왔으나
        종전 범위(3.7~7.0)와 겹쳐 **단일 샘플로는 개선을 주장하지 않는다** —
        블러 함수 자체의 단축만 측정된 사실입니다.
      ★2026-08-07 15차 안정 — **관측 중앙값 필터**(observation_median_count 3).
        오늘 쌓은 방어 4겹은 전부 "이상값이 **들어온 뒤** 어떻게 처리할까"라
        서로 부딪혔다(막으면 앵커가 굳어 내 손이 잘리고, 풀면 하이재킹이 샌다 —
        같은 손잡이의 양끝이라 값 조정으로는 해소가 안 됩니다. 3~6차가 그 반복).
        중앙값은 층을 더 쌓는 게 아니라 **문제를 앞으로 옮긴다**: 3개 중 1개가
        튀면 정의상 그 값을 고르지 않는다 — 이상값이 결과에 **전혀** 안 섞입니다
        (EMA alpha 0.4는 쓰레기 값의 40%를 그대로 받습니다). 실측 붕괴가 정확히
        단발 스파이크 형태(209→18→200px)라 가장 잘 듣는 조건입니다.
        ★★배포 후 실측 효과: 앵커 **어깨폭** 점프가
          종전 17~30%(602→440·537→373·472→393) → **0~6%**(477→505·488→506·
          341→342·342→331·337→330)로 떨어졌습니다. 폭 붕괴는 사실상 해소.
        ※단 **중심 이동은 25~34%로 남아 있다** — 로그를 보면 x가 392→496→547→563
          으로 **단조 증가**합니다. 단발 스파이크가 아니라 지속 추세라 중앙값이
          제거하지 않는다(제거하면 안 되는 것이기도 하다 — 진짜 이동과 구분
          불가). 남은 불안정의 성격이 바뀌었으므로 다음 조사는 여기서 시작할 것.
      ★2026-08-07 14차 — 13차 코드 리뷰 지적 2건 수정 + **작동 거리 창**
        (사용자 제안 — "물류창고 PDA 카메라 같은 필터"):
        ①[높음] segmentation_mask를 numpy_view() 그대로 넘기고 있었다 —
          MediaPipe 내부 버퍼의 **뷰**인데 포즈 스레드에서 만들어 hand_select에
          저장하고 다른 스레드에서 여러 프레임 뒤에 읽는 구조입니다. result가
          GC되면 버퍼가 사라지고, 더 나쁘게는 버퍼 재사용 시 **저장해 둔
          마스크가 조용히 다음 프레임 내용으로 바뀐다**(크래시 없이 앵커와
          마스크만 어긋나 실기에서 가장 잡기 어려운 종류). → .copy() 추가,
          회귀 방지 테스트(tests/test_head_detector.py 신설 — 원본 배열을
          덮어써 버퍼 재사용을 재현합니다).
        ②[중간] 상대 깊이 관문이 "최근접 손 = 내 손"을 암묵 가정했다 —
          나보다 **앞으로** 끼어든 사람이 기준이 되면 내 손이 잘립니다
          (1.0m 침입자 vs 1.5m 나 → 0.67 < 0.70). 13차 실측이 해법을 줬다:
          내 손 절대비 1.07~1.76 vs 뒷사람 0.51~0.78 — 절대비 1.0 이상이면
          "뒤에 있는 사람"일 수 없으므로 상대 관문에서 면제.
        ③[낮음] 깊이 로그 throttle을 통과/거부로 분리 — 공유하면 흔한 통과가
          드문 거부를 가려 정작 튜닝에 필요한 쪽이 사라집니다.
        ★**작동 거리 창**(min_operating_depth_px 300) — PDA 스캐너는 정해진
        작동 거리 밖은 아예 디코딩을 시도하지 않습니다. 이걸 옮겨 13차 관문의
        실제 구멍을 메웠다: 위 두 깊이 관문은 임계가 **앵커 어깨너비에 비례**
        하는데, BlazePose 어깨 붕괴(실측 12~21px)가 나면 임계가 함께 붕괴해
        (0.80×12 = 9.6px) **아무 손도 안 걸러진다** — 하필 붕괴가 잦은 게
        사람이 겹칠 때라 가장 필요한 순간에 관문이 꺼지는 구조였습니다. 카메라
        기준 절대 하한은 그때도 삽니다. 값은 13차 실측의 빈 구간(286~391)에서
        **낮은 쪽에 붙여** 300(내 손을 자르는 쪽이 더 나쁘므로).
        ⚠**이 관문만 거리 불변이 아니다** — 다른 임계는 전부 어깨너비 배수라
        카메라 거리 무관이지만 이건 px 절대값이다(앵커 붕괴에도 사는 이유이자
        한계). 카메라·렌즈·해상도·설치 거리 변경 시, 특히 **IR 전환 시 반드시
        재보정**. 재보정법은 config 주석에 적어 뒀습니다.
        테스트 8건 추가(전체 380 통과).
- [ ] **★PDA 스캐너식 광학 필터 — IR 전환 시 켤 것(2026-08-07 배선만 완료)**
      사용자 제안("물류창고 pda 카메라 처럼 그런 필터")에서 온 항목. 바코드
      스캐너가 어두운 창고에서 가까운 것만 정확히 읽는 이유는 알고리즘이 아니라
      **광학**이다: 자기 조명 + 짧은 노출이면 빛이 1/d²로 감쇠해 가까운
      피사체만 밝고 **먼 배경은 검게 떨어진다**. 오늘 하루 14차에 걸쳐
      소프트웨어로 싸운 뒷사람 문제를 **이미지 단계에서** 없애는 방법이다 —
      안 찍힌 사람은 검출될 수 없습니다.
      camera.exposure 섹션 신설(manual·exposure_value·gain·
      auto_exposure_off_value — camera_stream._apply_exposure). **기본 꺼짐**:
      지금은 실내 조명이 균일한 RGB 웹캠이라 뒷사람도 같이 밝아 효과가 약합니다.
      값을 하는 조건은 **조명이 카메라 쪽에 있을 때** — 키오스크 전면 조명,
      그리고 무엇보다 예정된 IR 카메라 전환(아래 "IR 카메라 전환 시 전면
      재확인" 항목)입니다. IR은 조명이 장치에 딸려오므로 감쇠가 그대로 성립합니다.
      전환 시 할 일: manual: true로 켜고 exposure_value를 내려가며 뒷사람이
      어두워지는 지점을 찾습니다. 장치가 설정을 무시할 수 있어 시작 로그
      "노출 수동 설정"에서 요청값과 실제값을 대조할 것.
      ※덤: 짧은 노출은 **모션 블러도 없앤다** — 손끝 세그멘테이션 신뢰도
      저하(isolate_anchor_blur 3차)의 원인 중 하나가 그것이라 같이 개선됩니다
      ※IR 전환 시 min_operating_depth_px(작동 거리 창) 재보정 필수
      ★2026-08-07 11차(사용자 정정 — "손이 중요한거야 앵커는 다른사람한테
        안가는데 손만 다른사람한테 가니까 초점 값을 어느 이상 안되게 설정해야
        하나"): 지금까지(1~10차)는 전부 **앵커 자체**가 뒷사람으로 넘어가는
        경로를 막는 데 집중했는데, 사용자가 직접 관찰한 실제 증상은 달랐다 —
        앵커(몸통 판정)는 정확히 내게 붙어 있는데, **손 게이트만** 반경
        밖(뒷사람 손)으로 샌다는 것. 즉 문제 지점이 `_update_head_anchor`가
        아니라 `_filter_hands_by_anchor`의 reach_shoulder_widths였습니다. →
        2.09→1.8로 좁힘(패딩 +0.51은 위로 뻗기 회귀 방지를 위해 다 걷지
        않음 — config.yaml head_anchor.reach_shoulder_widths 주석 참고).
        실측 없이 절충한 값 — 사용자 재확인 전. 뒷사람이 여전히 새면 더
        내릴 것(단 위로 뻗는 동작이 가로막히는지 함께 확인), 내 손이
        잘리면 다시 ↑
      ★2026-08-07 12차(사용자 요청 — "그러니까 블러를 사람 누끼 딴거 제외
        하고 블러처리를 해달라는거야"): 11차와 별개 트랙 — 아래 "인식 대상
        외 블러" 항목을 사각형에서 **실루엣 마스크 기반**으로 바꿨다(상세는
        그 항목 참고). 이건 손 게이트 반경 자체를 조정하는 11차와 달리,
        모델 입력 단계에서 다른 사람을 아예 안 보이게 만드는 별도 방어선입니다
- [ ] **인식 대상 외 블러(2026-08-07 신설·실험적, 사용자 제안) — 실기 검증 전**
      "인식되는 사람 제외하고 모든 것을 블러 처리"로 손 모델 입력 단계에서
      다른 사람을 아예 안 보이게 하는 실험. hand_tracker.isolate_anchor_blur
      섹션(preprocessor.anchor_keep_sharp_box·blur_outside_region, 순수
      함수 테스트 7건 — tests/test_preprocessor.py). 지금 enabled: true로
      켜서 실기 확인 중 — 확인할 것 둘: ①FPS가 더 나빠지는지(이미 3.7~7.0로
      낮음) ②앵커가 흔들리는 순간 진짜 사용자가 블러돼 완전히 놓치는지.
      포즈(머리) 추론에는 미적용(자기강화 오류 방지, realtime_loop 주석).
      keep_sharp_shoulder_widths(1.6, 실측 없음)가 유일한 튜닝 지점 —
      옆 사람이 살아남으면 ↓, 내 뻗은 손이 잘리면 ↑
      ★2026-08-07 2차(사용자 결정 — "그러니까 블러를 사람 누끼 딴거 제외
        하고 블러처리를 해달라는거야"): 사각형(anchor_keep_sharp_box)은
        원리적으로 **옆에 붙어 앉은 사람과 분리가 안 된다** — 사각형 안에
        옆 사람 몸이 같이 들어가면 그대로 선명하게 남습니다. Pose Landmarker의
        output_segmentation_masks(사람별 픽셀 단위 실루엣, 2026-08-07 이
        세션 처음 켜보는 옵션)를 배선했다:
        · head_detector.HeadDetection에 segmentation_mask 필드 신설,
          HeadDetector._segmentation_mask_for가 방어적으로 추출(실패해도
          이 사람만 마스크 없이 진행 — 포즈 추론 전체는 안 죽습니다)
        · hand_select.HandSelector가 앵커 갱신 때마다 최신 마스크를 들고
          있는다(appearance와 같은 배선 지점 — 이번 관측 추출 실패 시 낡은
          마스크 대신 None으로 덮어씀). anchor_segmentation_mask() 접근자 신설
        · preprocessor.blur_outside_mask(frame, mask, mask_threshold) 신설
          — mask가 None이거나 frame과 크기가 안 맞으면 원본 그대로(방어)
        · realtime_loop._inference_loop: 마스크가 있으면 우선 쓰고, 없으면
          (output_segmentation_masks 비활성·추출 실패) 종전 사각형 폴백
        · _drop_expired_head_anchor에 segmentation_mask 초기화 추가(appearance엔
          없는 처리): appearance는 이 함수 밖에서 안 읽혀 낡아도 무해하지만,
          segmentation_mask는 anchor_segmentation_mask()로 realtime_loop가
          **앵커 유무와 무관하게 매 프레임** 읽는다 — 안 비우면 앵커 해제
          (게이트 꺼짐) 뒤에도 떠난 사람의 낡은 실루엣으로 다음 사람을 계속
          블러해 이 함수의 취지("방어보다 인식 우선")를 거스릅니다
        config: head_anchor.output_segmentation_masks(신설, true로 켬) ·
        isolate_anchor_blur.mask_threshold(신설, 0.5 — 마스크 임계, 실측 없음).
        테스트 11건 추가(SegmentationMaskTest 5건·BlurOutsideMaskTest 6건,
        전체 362개 통과). ⚠실기 검증 전 — 확인할 것 셋: ①마스크 산출이
        FPS를 얼마나 더 깎는지(이미 3.7~7.0으로 낮은 위에 사각형 블러보다
        무거울 가능성) ②mask_threshold(0.5)가 내 손끝·팔 가장자리를 자르는지
        ③옆/뒷사람이 실제로 블러되는지(11차의 반경 조정과 함께 확인 —
        둘 다 켜진 상태로 테스트하게 됨)
      ★2026-08-07 3차(사용자 실기 재확인 — "손가락 손바닥 쪽에서 블러를 잘
        못 잡는거같은데") — 2차 배포 직후 실기에서 ②가 실제로 걸렸습니다. 원인:
        Pose 세그멘테이션은 몸통 실루엣 위주로 학습돼 신뢰도가 부위마다
        고르지 않다 — 몸통은 0.9~1.0인데, 손가락처럼 얇고 제스처 중이라
        빠르게 움직이는(모션 블러 낀) 말단은 문턱(0.5) 밑으로 떨어지기 쉽습니다.
        그러면 **내 손끝이 남처럼 블러돼**, 손 모델이 봐야 할 손 모양이
        입력 단계에서부터 뭉개진다 — 블러가 다른 사람이 아니라 나 자신을
        지우는 역효과였습니다. → preprocessor.blur_outside_mask에 dilate_px
        매개변수 신설: 문턱을 넘은 "선명 영역"을 cv2.dilate로 그만큼(px)
        팽창시켜 경계 손실을 흡수합니다. mask_threshold를 낮추는 것과 달리
        배경의 애매한 픽셀을 끌어들이지 않고 이미 확실한 몸통 영역에서
        바깥으로만 여유를 준다 — 신뢰도 자체를 낮추기보다 경계를 넓히는
        접근. config: isolate_anchor_blur.mask_dilate_px(신설, 20px, 실측
        없는 초안값). 테스트 2건 추가(dilate_px 기본 0 회귀 방지 +
        팽창이 raw 마스크 경계 밖까지 선명하게 남기는지), 전체 364개 통과.
        ⚠여전히 실기 검증 전 — 20px로 손끝 잘림이 해소되는지, 그리고 반대로
        옆 사람이 이 여유 때문에 다시 살아나기 시작하는지 둘 다 확인할 것.
        손끝이 계속 잘리면 ↑, 옆 사람이 샌다면 ↓(mask_threshold를 올리는
        쪽도 같이 검토)
      ★2026-08-07 4차(사용자 실기 재확인, 3차 배포 뒤) — "뒤에 사람이 손
        올리면 블러처리된게 풀리면서 뒷사람 손이 보이고 인식이되는데":
        3차가 예고했던 트레이드오프가 그대로 걸렸습니다. 뒷사람이 손을 들면
        그 손이 카메라 2D 투영에서 내 실루엣 경계에 가깝게(또는 겹치게)
        보이고, 그 경계 부근은 원래 세그멘테이션 신뢰도가 애매한 지대입니다
        (3차의 손끝 신뢰도 저하와 **같은 뿌리** — 얇거나 겹친 구조는 신뢰도가
        낮습니다). dilate_px(20)가 그 애매한 경계를 선명 영역으로 끌어들이는
        폭까지 키운 것 — 손끝 구제와 겹침 차단이 "마스크 경계를 얼마나
        관대하게 볼 것인가"라는 **같은 축의 반대 방향**이라 한 값으로 동시에
        완전히 풀리지 않는다(mask_threshold도 같은 축). → 20→8로 축소
        (config.yaml mask_dilate_px 주석 참고). 사용자에게 명확히 전달한
        한계: 뒷사람 손이 내 실루엣에 실제로 닿을 만큼 가까워지는 겹침
        자체는 **단일 2D 카메라의 원리적 한계**다(픽셀 겹침에서 신뢰도로
        완벽히 못 가른다 — MOT 문헌 조사 8차의 결론과 같은 벽). 블러는
        그 위에 얹은 보조 방어선일 뿐, 진짜 백스톱은 blur와 무관하게 항상
        도는 head_anchor.reach_shoulder_widths(1.8)·
        wrist_reach_shoulder_widths(0.95) 게이트다 — 거기서도 계속 새면
        (11차 미해결 항목) 그 반경을 더 좁히는 쪽이 근본 대응이고, 블러
        dilate_px는 "내 손끝 vs 뒷사람 겹침" 사이의 미세 조정용으로 한계가
        있다는 것을 인지할 것. 다음: 8로도 여전히 새면 0까지 낮춰 순수
        원본 마스크 경계로 되돌리고, 그래도 새면(=dilation 문제가 아니라
        원본 마스크 자체가 겹침에서 애매) 이건 blur_outside_mask 쪽 튜닝이
        아니라 위 reach_shoulder_widths 반경 쪽에서 막아야 합니다
- [x] **어깨 가시성 관문은 안 듣는다(2026-08-04 실측 — 다시 꺼내지 말 것)**
      anchor_frame()에 손목처럼 가시성 관문을 넣으려 했는데, 8개 단계 전부
      관문 켬/끔이 **소수점까지 같았다**(중앙 0.023 · 95% 0.095 · 최대 4.967, n=89).
      어깨 가시성이 중앙 0.999 · 95% 1.000이라 관문이 한 번도 안 걸린다 —
      bdfd96f의 손목 발견("주머니에 손을 넣어도 0.98로 보고")과 같은 결론입니다.
      BlazePose의 visibility는 가림 판별에 못 씁니다
- [ ] **앵커 실측 도구(scripts/anchor_check.py) 결함 3건 — 다시 쓸 때 고칠 것**
      실사용 로깅으로 방향을 틀어 지금은 안 고쳤다(안 쓸 코드를 다듬지 않으려고):
      ①미검출 구간을 이어 붙인다 — 관측이 빠져도 _prev를 안 비워, 공백을 건너뛴
        변화가 한 걸음이 된다(걸어오기 안 잡힘 21 · 95% 0.873 · 최대 16.375).
        가시성 관문엔 "잇지 않는다"를 넣어 놓고 미검출 경로엔 빠뜨렸습니다
      ②span_split이 걸어오기에서 거짓 양성 — 어깨폭이 64→142px로 **연속** 증가하면
        중앙값으로 갈라도 두 무리가 됩니다. 사람 바뀜과 거리 변화를 구분 못 합니다.
        중앙값 **교차 횟수**로 가르면 될 것(오가면 잦고, 걸어오면 1회)
      ③귀↔어깨 거리를 px로 낸다 — 카메라 거리 비례라 회차마다 135→154로 흔들려
        [C](앵커 중심 판단)의 재현성이 없습니다. 어깨너비로 정규화해야 합니다
- [x] **제스처 12칸 채우기 + select 1회(2026-08-04 사용자 결정)** — 손 펼침이
      4방향(left/right/up/down)을 맡고, 회사 정의 없는 칸은 temp_ 로 자리만 잡았습니다
      (temp_bottom · temp_fist_bottom). 이벤트 10종 → **14종** — 델파이 분기 추가 필요.
      select는 쥐기 2회 → **1회 + 정지 유예(dwell_sec 0.4)**. 유예가 없으면
      1회 전환이 방어를 통째로 날린다: 제자리 조건은 쥐기 **사이**만 보는데 1회엔
      사이가 없어, 주먹을 만드는 순간 select가 나가 back·home·confirm이 매번
      오발을 단다(명령은 반드시 주먹을 거칩니다). 시나리오 03c가 이 방어를 지킵니다
      ※dwell_sec 0.4는 **실측 전 잠정값** — 주먹 명령이 select를 달고 나오면 ↑,
        select가 굼뜨면 ↓
- [x] **⚠아래 쓸기는 팔 내리기와 구분 불가 — 오발 감수(2026-08-04 사용자 결정)**
      down을 살리려고 두 축으로 실측했고 **둘 다 안 갈렸다**:
      ① 위치(골반선 기준): 진짜 아래 쓸기 126건 중 **60건이 팔 내리기 구간 안**.
         모든 내리기를 잡는 경계는 쓸기 절반을 죽이고, 모든 쓸기를 살리는 경계는
         내리기를 다 통과시킵니다
      ② 간 거리(확정 유예 0.25초 동안): 쓸기 중앙 0.50 / 내리기 중앙 **0.49** —
         사실상 동일. 게다가 같은 아래 쓸기가 서서 0.50 · 앉아서 0.11로 **5배** 차이라
         자세 하나를 맞추면 다른 자세가 무너집니다
      가드를 넣은 상태의 기능 검증: 팔 내리기 오발은 0건이었지만 **진짜 아래 쓸기를
      87% 삼켰다**(5회 시도 → 1회 발화). 그래서 가드·확정 유예·골반선 기계를 전부
      제거하고 오발을 받아들였습니다. 시나리오 22가 이 결정을 눈에 보이게 남긴다 —
      "왜 팔 내릴 때 down이 나가지?"를 다시 파기 전에 위 숫자를 볼 것
- [x] **손목 가시성 관문 배선 누락 정정(2026-08-04 사용자 보고)** — "손이 안 잡히니
      손목쪽에서 오염이 나온다". `reliable_wrists`의 가시성 관문이 `len(wrist) > 2`
      조건부였는데 head_detector가 (x, y) 2-튜플을 넘겨 **한 번도 실행되지 않았다**
      (가시성을 싣는 `_points`가 정의만 되고 안 불렸습니다). 배선을 고쳤습니다.
      ※단 **이것으로 주머니 손이 걸러지지는 않는다**(같은 날 실측 정정): 주머니에
        손을 넣어도 BlazePose는 손목 가시성을 0.98로 보고하고, 전 자세 통틀어 최솟값이
        0.73이라 임계 0.5는 걸리지 않습니다. 포즈는 손목 위치를 **맞게** 보고 있습니다.
        같은 이유로 "선 아래 인체가 없으면 거짓"도 이 증상엔 안 듣는다 — 손이 주머니에
        있으면 팔은 몸을 따라 내려가 있고 선 아래에 몸이 있습니다.
        남은 방어는 팔 길이 심사(기하)와 손 소유자 판정. 오염이 계속 보이면 **증상**
        (선이 남의 몸으로 건너가는지·없는 곳에 생기는지)부터 받아 좁힐 것
- [ ] **★IR 카메라 전환 시 전면 재확인**(2026-08-04 사용자 고지: "본 개발은
      IR카메라로 변경됨") — 오늘까지의 임계값·동작은 **전부 광학(RGB) 카메라
      실측**입니다. IR은 질감·대비·색이 달라 MediaPipe 랜드마크 품질 자체가
      바뀔 수 있다(손 검출률·모양 판별·포즈 손목 신뢰도). 카메라를 바꾸면:
      ① `py scripts\calibrate.py` 재보정 ② 손 검출률·추적률 재측정
      ③ 손 모양 판별(주먹/한 손가락/손바닥) 재확인 ④ 포즈 어깨·손목 신뢰도
      (visibility) 재확인 — 손목 심사·소유자 판정이 여기에 얹혀 있습니다.
      ※MediaPipe는 RGB 입력을 전제로 학습됐다 — IR 단채널을 3채널로 복제해
      넣는 방식이 품질에 어떤 영향을 주는지가 첫 확인 항목
- [ ] **쥐기 select 실기 확인**(2026-08-03 신설, 미측정) — `window_sec` 3.0이
      적당한지. 래치 전환이 8프레임(≈0.27초)을 요구해 2회에 최소 ≈1.1초가 듭니다.
      둘째 쥐기가 자꾸 놓치면 ↑, 무심코 쥐었다 폈다 하다 select가 튀면 ↓
- [x] **현장 문제 일괄 해소** — 획 중간 끊김(머리 추론 스레드 분리·재등장 이음) ·
      몸쪽 관통 획 씹힘(획 교차 보호) · 근거리 위 쓸기 무반응(존 밖 정지 시 게이트
      해제) · 카메라 거치 의존(휴식 존을 몸 기준 어깨선으로) · 원거리 저하(ROI 줌) ·
      엔진 먹통(카메라 프로브 오픈 타임아웃)
- [x] **버전 정합 회귀 확인** — mediapipe 0.10.14 하향 후 L/R 라벨·30 FPS 유지 실측.
      opencv 이중 설치(contrib 4.11이 4.10을 덮어씀) 발견·정정

## ✅ 완료 (2026-07-29 — 포즈 스택 제거: MediaPipe 단독 추론)

- [x] **포즈(ONNX Runtime + rtmlib) 완전 제거(사용자 결정)** — 제스처 판정(모양·궤적)은
      손 데이터만으로 끝난다: 추론 = MediaPipe HandLandmarker(내장 TFLite/XNNPACK,
      Apache-2.0) 단독. CPU 대폭 절감, 의존성 mediapipe·opencv·numpy·pyyaml·psutil로
      축소, GPU 스택(torch)·포즈 모델(~190MB)·rtmlib 캐시 반입 절차 소멸.
      포즈가 하던 일의 대체(hand_select.py): 잠금→크기+연속성 선별 · 어깨너비 자→
      손 실측(월드 랜드마크 미터) 기반 가상 어깨 자(기존 임계 숫자 유지) ·
      어깨선 게이트→화면 하단 띠 폴백 · 손목 브리지→소실 유예(0.35초)
- [x] ⚠ **스펙 주의(№1 연계 — 회사 확인 항목으로 승격)**: 다인(대기줄) 사용자 잠금이
      휴리스틱 대체로 약해짐 — "한 명 사용" 가정. 아래 회사 확인 절 참조
- [x] **B안(보조 카메라 표 융합) 폐기 — 사용자 결정(2026-07-29 키오스크 실기)**:
      보조 시점 표가 허수 데이터(옆 사람 손·좌우 재라벨 오염)를 과다 유입.
      A안(카메라 프로브 자동 선별 — 이 브랜치) 단독 진행 —
      feat/think_win_cam_b 브랜치는 기록용 보존(이식 중단)
- [x] **지시 손 고정(command_hand_lock) — 사용자 제안 로직(2026-07-29, v2 정정 07-30)**:
      양손이 보일 때 활성 팔이 높이 비교로 왔다갔다 — 지시 손이 생기면 그 손만
      계속 인식하고 반대 손은 무시. v1(래치=지시)은 쉬는 손이 먼저 활성이면
      오고정되는 역효과(실기 보고) — v2 "지시" = 모양 있는 손의 실제 이동
      (move_dist/window). 원거리 난입 손(rejoin_dist 밖 재등장 — 다른 사람)도
      무시. 해제는 지시 손 소실 유예(shape_latch.release_sec) 초과뿐

## ✅ 완료 (2026-07-28 — 손 모델 교체: wholebody 손 21점 → MediaPipe HandLandmarker)

- [x] **손 모양·손 중심 소스 교체** — wholebody 손 21점의 실기 문제 2건(노트북 검증):
      ① 톱다운 포즈는 안 보이는 손도 강제 출력 → 한 팔에 좌/우 손 겹침(유령 손)으로
      궤적·다수결 오염 ② 2D 투영만으로는 원근 단축(화면 가리키기)을 굽힘과 구분 불가
      — balanced 모델·임계 튜닝 모두 무효(07-28 실측). MediaPipe HandLandmarker
      (Apache-2.0) 도입: 보이는 손만 보고 + 좌/우(handedness) 내장 + z(깊이)로 3D
      판별(hand_shape v3 — 가리키기 자세가 "폄"으로 풀림). 07-23 "새 의존성 0" 결정
      번복 — 라이선스 기준(B안)은 동일 Apache-2.0이라 №9 영향 없음(항목 갱신)
- [x] pose_engine wholebody→body 경량화 (wholebody 필수 사유가 손 판별이었고 소멸 —
      CPU 절감). hand_landmarker.task는 download_weights.py가 models/weights/에 받고
      오프라인 번들엔 프로젝트 폴더째 포함. 테스트 118건 재작성·통과

## ✅ 완료 (2026-07-15 — 동작 체계 범용 설계 개편, 같은 날 2차 확정)

- [x] 쓸기 동작 도입: 팔 좌/우 쓸기=포커스 이동 · 아래=이전 화면(**go_back 이벤트 신설**) ·
      위=처음으로 — 포즈 손목 궤적 판정이라 손·손가락이 없는 사용자도 동일 조작
- [x] **(2차) 선택(OK) = 고개 꾸벅 2회 확정** — "끄덕임=예"라 직관적이고 팔이 전혀 없어도
      가능. 신호 = 목 길이 비율(코~어깨 중점, 어깨 너비 정규화), 숙였다 제때(0.8초) 복귀 ×2.
      내려다보기(지갑·신분증)는 복귀 시간 초과로 무효 — 오탐 방지 설계
- [x] (2차) 1차 안이던 손등/팔등 보이기 경로 전면 제거 — MediaPipe 손 검출·팔등 CNN·
      수집/학습 스크립트 삭제 → **포즈(RTMPose) 단일 엔진** (requirements에서 mediapipe 제거)
- [x] 구 동작 전면 제거: 주먹→펴기·OK핀치·양손바닥 10초·레거시(5.1 초안) 토글
- [x] 구 HaGRID ONNX 제스처 엔진 제거 — 신규 스펙 판정 불가로 사장 + AGPL 리스크 해소
      (제거 산출물 백업: ~/Documents/backup/gesture_kiosk_2026-07-15_before_gesture_rework/,
       _before_nod_select/)
- [x] 단위 테스트 43건 재작성·통과 (쓸기·꾸벅 판정·목 길이 신호·잠금)
- [x] **(2026-07-16) 쓸기 팔꿈치 폴백** — 손 절단 사용자는 손목 키포인트 신뢰도가
      낮을 수 있어(사용자 지적), 손목 미달 시 팔꿈치 궤적 + elbow_gain 배율로 판정.
      추적점 전환 시 궤적 리셋, 화면 라벨 "(E)" 표시. 테스트 48건
- [x] **(2026-07-16) 쓸기 정지 재장전** — 우로 쓸고 원위치 복귀가 좌로 오인되는
      실기 오탐(사용자 보고) 해결: 이벤트 후 추적점이 잠깐 멈춰야(rearm_still) 다음
      쓸기 장전 — 복귀는 연속 동작이라 구조적으로 통과 불가. 상하 복귀·왕복 흔들기
      연발까지 같은 원리로 차단. 테스트 52건
- [x] **(2026-07-16) 한 번에 한 팔만 인식** — 양팔 동시 추적 시 쉬는 팔의 잡음이
      간섭(사용자 보고: 인식 저하). 활성 팔 = 더 높이 든 팔(switch_margin으로 교체
      진동 방지), 팔 교체 시 궤적 리셋. 맥 config 추론 상한 60→30 고정(캡처 FPS
      반토막 방지 — 배포와 동일 조건). 테스트 55건
- [x] **(2026-07-16) 쓸기 임계 어깨너비 정규화** — 카메라 위치·거리에 따라 인식이
      갈리는 문제(사용자 보고): 임계 단위를 화면 비율 → 어깨너비 배수로 전환
      ("자기 어깨너비의 몇 배를 움직였나"). EMA 평활·측면 회전 하한 클램프·미검출
      폴백(fallback_ratio=0.22, 종전 임계와 등가) 포함. 꾸벅은 애초에 정규화돼 있어
      전 제스처가 거리 불변. 프레임 밖 이탈은 소프트웨어로 불가 — 설치 기준 문서화. 테스트 58건
- [x] **(2026-07-16) 주민등록증 OCR 기능 전면 제거 — 제스처 집중(사용자 결정)**:
      src/ocr·OCR 워커·/ocr 엔드포인트·fill_id_fields 이벤트·easyocr(+맥 torch) 의존성 삭제.
      **№11 주민등록번호 처리 이슈 소멸**, 미니PC CPU 부담·설치 용량 대폭 감소.
      classes 계약 변경(fill_id_fields 제외 — №7 UI 반영 확인). 백업: _before_ocr_removal/
- [x] **(2026-07-16) 판정 계기판 + 임계 완화** — 실기 인식 저하(사용자 보고) 원인이
      근거리 시험에서 정규화 임계가 커진 것(어깨 1.1배=화면폭 40%+·프레임 이탈)으로
      추정 → min_dist 0.8/0.5·window 0.8초로 완화 + 판정 내부값(스케일·진행도·장전)을
      /data·화면·데모UI에 노출 — 이후 튜닝은 수치 근거로. 테스트 48건
- [x] **(2026-07-16) 토크백/보이스오버 문법 정렬 — 사용자 결정**: 위/아래 1회=확인
      (read_focus 신설)/선택, 위/아래 2연속=go_home/go_back(파괴적 동작 안전장치),
      고개 꾸벅 제거(쓸기 일원화). classes 6종 — №7 UI 반영 확인.
      1회 동작은 판정 창(double_within_sec 1.2초)만큼 지연 발화 — 실기 튜닝 대상. 테스트 42건
- [x] **(2026-07-16) 정지 재장전 제거 → 반대 방향 1회 삼킴** — 멈춤 판정이 키포인트
      떨림에 갇혀 인식 불능 유발(사용자 보고: 인식 안 됨) + 복귀 중 멈칫하면 방어가
      뚫림(복귀 오탐 재발). 대체: 동작 후 return_suppress_sec(2.5초) 안의 같은 축
      반대 방향 쓸기 1건을 복귀로 무시 — 멈춤 불요·연속 쓸기 즉응. 테스트 35건
- [x] **(2026-07-16 최종) 확인·선택 통합 + 아래 1회=go_back 즉시 + 전완 플릭 인식** —
      read_focus 제거(위 1회=select 통합), 아래는 대기 없이 즉시 이전 화면,
      임계 0.55/0.35로 2차 완화(팔꿈치 고정 플릭 인식 — 사용자 보고 대응).
      classes 5종 — №7 UI 반영 확인. 테스트 35건.
      정정(같은 날): **처음으로=아래 2연속**(위 2연속 오배치 수정) — 분기가 아래로
      이동해 select(위 1회)는 즉시 발화, go_back이 판정 창(1.2초) 지연
- [x] **(2026-07-16) 좌·아래·아래2연속 인식 불량 해결** — 복귀 판정을 시간→위치 기반
      (직전 획 끝 경유 검사)으로: 의도적 반대 쓸기 통과, 삼킴 창 1.6초 단축.
      획 유예(0.5초, 빠른 2연속 차단 부작용) → 복귀 확인 게이트(어깨 0.15배 되돌림)로
      교체 — 빠른 아래 2연속 시간 제한 없이 인정. 테스트 37건

## ✅ 완료 (2026-07-08 — 1주차)

- [x] 기획서 2.3 디렉터리 구조 + 4장 코딩 컨벤션 적용한 저장소 골격
- [x] 캡스톤 코드 구조 이식 — 단일 USB 카메라·제스처용으로 정리
- [x] 실시간 파이프라인: 캡처 스레드 → 전처리 → YOLO 추론 → 판정 → 이벤트
- [x] 무학습 모델 경로 확보: HaGRIDv2 사전학습 YOLOv10n (다운로드 스크립트 포함)
- [x] 예시 UI · 이벤트 전송 접점(event_sender) · 단위 테스트

## ✅ 완료 (2026-07-10 — 전면 리팩토링, feat/think_win_gpu)

- [x] 타깃 확정: **윈도우 + NVIDIA + Python 3.11.5** (정부 민원발급기)
- [x] 신규 동작 체계: 주먹→펴기 이동(왼손=왼쪽·오른손=오른쪽) / OK=선택·확인 통일 /
      양 손바닥 10초=처음으로. 레거시(5.1 초안)는 config 토글로 병행 유지
- [x] 사용자 잠금: YOLO11n-pose + 얼굴 선명도(라플라시안)×크기 점수 —
      잠긴 사용자의 손목 근처 손만 인식 (다른 사람 손 차단, 거울 좌/우 보정)
- [x] 주민등록증 OCR(EasyOCR): 이름·주민번호 자동 입력(fill_id_fields) —
      프레임·원문 미저장, 로그 마스킹
- [x] 토크백: 이벤트 자동 안내 + UI 요청(POST /announce) TTS(pyttsx3)
- [x] 이식 체계: requirements 버전 고정 · install.bat / run.bat ·
      내부망용 make_offline_bundle.bat · 설치가이드.md · smoke_test
- [x] 예시 UI를 민원발급기 흐름(증명서 선택→본인확인→발급)으로 교체
- [x] 단위 테스트 43건 통과 (FSM·사용자 잠금·OCR 파싱)

## 🔴 회사 확인 필요 — 멋대로 진행 금지 (기획서 9장 연동)

- [ ] **№1 제스처 스펙 확정** — 2026-07-29 개편(사용자 결정): 한 손가락+좌/우/위=
      포커스 이동(left/right/select — 상하(top/bottom) 제거, 아래 방향 정의 없음),
      주먹+왼쪽/위/오른쪽=back/home/confirm(구 ok).
      ⚠ **양손이 모두 없는 사용자의 조작 수단 부재**(손목·팔꿈치 폴백
      제거) — UI 자동 순회(스캐닝) 도입 여부 회사 협의 필요.
      직원 호출(help_call)은 계약에서 제외 유지
- [ ] **№7 회사 키오스크 프로그램(UI) 파일 수령** — 수령 후 demo_ui 교체 작업 시작
- [ ] **№7 이벤트 전달 규격 합의** — 파이프(stdio) 확정(2026-07-23): stdout에
      GESTURE| 텍스트 한 줄. 회사 측 세부 규격 답신 오면 Sender 추가로 대응.
      **classes 14종**(2026-08-04 현재): left/right/up/down/select/back/home/confirm
      + temp_left/temp_right/temp_top/temp_bottom/temp_fist_bottom + click.
      UI 분기문에 up·down·temp_bottom·temp_fist_bottom 추가 필요 (델파이7_연동가이드 §3).
      ※temp_ 5종은 회사 정의 대기 — 이름은 EVENT_BY_SHAPE 한 곳에서 바꿉니다
- [ ] **№8 통합 범위 확정** — UI 통합까지인지 이벤트 출력까지인지
- [ ] **№5·№6 KPI 측정 기준 합의** — 정확도 85% 산식 / 30 FPS(엔드투엔드 vs 추론 단독)
- [ ] **№9 라이선스 검토** — 2026-07-29 최종 단순화: 추론 = MediaPipe HandLandmarker
      (Apache-2.0, 내장 TFLite) **단독** — rtmlib·ONNX Runtime 제거로 검토 대상 축소.
      카피레프트·저작자표시 의무 없음 (원문 확인 07-28).
      잔여: 배포물에 Apache-2.0 고지문 동봉 절차만 최종 빌드 때 확인
- [ ] **№1-2 (신규 2026-07-29) 다인 사용 가정 확인** — 포즈 잠금 제거로 "옆 사람
      무시"가 크기+연속성 휴리스틱으로 약해짐(한 명 사용 가정). 키오스크 카메라
      화각에 대기줄이 상시 잡히는 배치라면 포즈 잠금 구판(기반 2ea58a5) 복원 검토 —
      회사에 설치 배치·화각 확인 필요
- [ ] **№13 (신규 2026-07-31) 시선추적 모듈과 카메라 공유 방식** — 회사 측
      시선추적 도입 예정 확인됨(사용자 전언). 같은 카메라를 두 프로세스가 동시에
      열 수 없어(윈도우) 공유 구조(별도 카메라? 단일 프로세스 통합?) 협의 필요.
      이 판(몸통판)은 얼굴을 다루지 않아 얼굴 처리 충돌은 없음
- [ ] **№10 코드 저장 위치·백업 정책** — 외부 Git 허용 여부
- [x] ~~**№11 주민등록번호 처리 법적 근거**~~ — 2026-07-16 OCR 기능 제거로 **이슈 소멸**
      (엔진은 더 이상 개인정보를 다루지 않습니다. 본인확인은 회사 프로그램 담당)
- [x] ~~**№12 설치 대상 PC 사양·망 환경**~~ — 확인됨(2026-07~08 실기): 인텔 12세대
      vPro·램 8GB·**GPU 없음**(07-29 포즈 제거로 GPU 불필요), 내부망. 배포 zip에
      wheelhouse·모델 사전 포함 — install.bat 하나로 오프라인 설치(08-03 검증)

## 🟡 키오스크 실기 검증 (남은 항목 — 2026-08-03 갱신)

> 구 항목(GPU PC·torch·onnxruntime·TensorRT·run.bat/make_offline_bundle.bat
> 동작 확인)은 **대상 소멸로 삭제** — 07-29 포즈 스택 제거로 GPU 경로가 없어졌고,
> 08-03 통합으로 bat은 install.bat 하나다(install/실행/오프라인 구동은 08-03 검증 완료).

- [ ] **2026-08-03 보정값 실기 확인** — 쓸기 임계를 실측(작게 5회/크게 5회)대로
      내렸다(좌우 0.55→0.20 · 위 0.30→0.16 · 플릭 0.28→0.15): 작게 한 동작이
      잡히는지, 반대로 오발이 늘지 않는지. 늘면 config 주석의 방향(↑)대로 복원
- [ ] **탭 클릭 실기 확인** — 판정을 손 전체 "주먹"에서 **검지 비율 20% 하강**으로
      교체(실측: 까딱은 1.35→0.97까지만 내려가 주먹선 0.85에 미도달). 검지만
      까딱 2회로 click이 나가는지 (종전엔 손목을 크게 두 번 해야 인식)
- [ ] **손 모양 오판 재확인** — 손바닥→주먹 오판이 실측 26%(래치가 상당 부분 흡수).
      실사용에서 temp 3종이 명령(back/home/confirm)으로 잘못 나가는 일이 있는지
- [ ] **처진 손 오판 — 미해결**(2026-08-03 사용자 보고: "팔이 가로로 되어 있고 손목이
      아래로 쳐져 있으면 검지 주먹이 주먹으로 들어간다"). 당일 두 가지를 시도했다가
      **둘 다 되돌렸다** — 근거는 hand_shape.py 첫머리 주석에 남겼다:
      ①PIP 관절각 임계(101°) → 실기에서 처진 주먹의 20%를 finger로 오판
      ②손바닥 정면도 게이트(0.45) → 정지 자세만 측정해서 생긴 착시. 움직이는 실사용에선
        정상 자세의 90%가 걸려 판별이 멈췄고, 래치가 굳어 주먹·손바닥이 전부 finger로
        나가는 더 큰 사고가 됐다(사용자 보고로 발견 → 즉시 제거)
      이 자세는 랜드마크 단계에서 이미 정보가 뭉개진다(3라운드 반복 측정: 편 검지
      최저 0.618 vs 같은 손 굽힌 손가락 최고 0.632 — 한 손 안에서 뒤집힙니다).
      판별 규칙이 아니라 **관측을 바꾸는 방향**으로 접근할 것 — 카메라 각도·해상도,
      또는 모양 판별을 손 실루엣 분류로 교체. 회사 협의 필요(№?)
      ※교훈: 판별 임계는 반드시 **움직이는 손**으로 측정합니다. 정지 측정은 실사용이 아니다
- [ ] benchmark 30 FPS 확인 (`py scripts\benchmark.py`) — 키오스크 CPU 기준
- [ ] 두 사람 동시 프레임 진입 시 단일 손 추적 유지 확인 (옆 사람 손 차단)
- [ ] 정확도 매트릭스 측정 (`py scripts\eval_accuracy.py`) — KPI 85% 검증(№5)

## 🟢 이후 (조건부 — 학습이 필요해질 때, feat/study)

- [ ] 제스처 정확도 미달 시: 데이터 수집·라벨링 → training/으로 파인튜닝 (기획서 5.4 인물 단위 분할)
- [ ] 학습 가중치 명명 규칙: {모델}_{데이터셋}_{지표}_{날짜}.pt (기획서 4.8)

## 메모

- 학습은 5080 / 맥북 M5 Pro 등 아무 환경 — 결과 .pt만 추론(윈도우)으로 이식 (2026-07-10)
- 예시 UI의 증명서 목록은 데모용 더미 — 실제 화면·업무 흐름은 회사 프로그램 담당
