# 2026-09-03 (목) 개발일지

어제 "배치 288/288"을 확인하고 마감했는데, 오늘 **광각 카메라**가 요구사항에
들어왔습니다. 그리고 그 한마디가 어제까지의 검증에 큰 구멍이 있었다는 것을
드러냈습니다.

그동안 "광각에서도 되나"를 **초점거리만 줄여서** 시험했습니다. 그건 틀린
시험이었습니다 — 초점거리는 배율일 뿐이고, 상대 회전 매핑은 배율에 원리적으로
면역이라 **무엇을 넣어도 통과합니다.** 시험이 통과한 이유가 코드가 옳아서가
아니라 시험이 아무것도 안 걸고 있었기 때문이었습니다.

광각의 진짜 문제는 화각이 아니라 **배럴 왜곡**이었습니다.

테스트는 580개에서 **673개**로 늘었습니다.

---

## 1. 왜곡을 넣자 드러난 것

가상 카메라에 Brown-Conrady 방사왜곡을 넣었습니다.

    x_d = x_u (1 + k1 r^2 + k2 r^4),   r^2 = x_u^2 + y_u^2

  · **Conrady, A.E. (1919).** "Decentred Lens-Systems." *MNRAS* 79(5), 384-390.
  · **Brown, D.C. (1966).** "Decentering Distortion of Lenses."
    *Photogrammetric Engineering* 32(3), 444-462. — k1·k2 방사 계수의 출처.
  · **Zhang, Z. (2000).** "A Flexible New Technique for Camera Calibration."
    *IEEE TPAMI* 22(11), 1330-1334. — 이 모형이 표준 보정에 쓰이는 형태.

왜곡은 **초점거리를 곱하기 전** 정규화 좌표에 걸어야 합니다. 렌즈가 먼저
빛을 휘게 하고 센서가 나중에 배율을 먹이는 순서니까요.

재 보니(얼굴이 화면 옆+위에 있을 때, 가로 선형성 이탈 / 세로 휨 / 몸
평행이동 끌림):

| 렌즈 | 가로 | 세로 | 끌림 |
|---|---|---|---|
| 왜곡없음 | 0.14% | 4.34% | 0.0000 |
| **일반 65도** | **10.74%** | **9.78%** | **0.0355** |
| 광각 90도 | 12.44% | 16.80% | 0.0383 |
| 초광각 120도 | 14.79% | 33.73% | 0.0421 |

**광각만의 문제가 아니었습니다.** 평범한 65도 웹캠에서도, 사용자가 화면
가운데가 아니라 옆에 서면 10%가 넘게 틀어집니다. 끌림 한도는 0.020인데
전부 넘습니다.

---

## 2. auto_arc는 실사용에서 한 번도 걸리지 않고 있었다

세로 휨은 auto_arc가 지우기로 돼 있었습니다. 그런데 1200프레임을 줘도 계수가
0.000이었습니다. 이유를 확인했습니다.

| 움직임 | 표본 | 2차 계수 | R² | 판정 |
|---|---|---|---|---|
| 가로만 (이상적) | 552 | −0.271 | 0.568 | 통과 |
| **가로+세로 섞임 (실사용)** | 547 | −0.625 | **0.051** | **거부** |

사람이 실제로 쓸 때는 좌우와 상하를 함께 움직입니다. 그러면 세로값의 분산을
상하 동작이 지배해서 R²가 0.15 밑으로 떨어지고, 안전장치 ⑤가 그 창을
버립니다. **안전장치가 제 일을 한 것이지만, 결과적으로 보정은 한 번도 일어나지
않았습니다.**

R² 문턱을 낮출까 했지만 그만뒀습니다 — 섞인 동작에서 나온 계수(−0.625)가
순수 동작의 것(−0.271)과 크게 다릅니다. 게이트를 풀면 **틀린 값을 쓰게 됩니다.**
대신 휨 자체를 없애기로 했습니다(3절).

---

## 3. 세로 휨의 진짜 원인 — 코가 튀어나와서

원인은 얼굴 좌표축이었습니다. 이 코드에서 "아래쪽" 축을 이마(10)에서
코끝(4)으로 잡는데, **코가 25mm 앞으로 나와 있어 그 벡터가 얼굴 평면에서
19.5도 앞으로 기웁니다.**

기울어진 축에 대고 "얼굴이 향하는 벡터"를 사영하면(그노몬 투영), 좌우로
돌릴 때 그 궤적이 원뿔을 그리고 화면에서는 원뿔곡선 — 즉 **활 모양**이 됩니다.
auto_arc가 지우려던 그 휨이 바로 이것이었습니다.

**회전벡터(축각)로 분해하면 원리적으로 사라집니다.** 상대 회전을 회전벡터
w로 바꾸면 1차 근사에서

    dot(향하는벡터, x축) 는 -dot(w, y축)      (가로 = 세로축 둘레 회전)
    dot(향하는벡터, y축) 는 +dot(w, x축)      (세로 = 가로축 둘레 회전)

이고, **축이 얼마나 기울었든 축 둘레의 회전량은 그대로 나옵니다.** 세로축
둘레로만 돌린 회전은 x축 성분이 정확히 0이므로 세로가 0입니다.

| 렌즈 | 그노몬 투영 | 회전벡터 |
|---|---|---|
| 왜곡없음 | 3.90% | **1.58%** |
| 일반 65도 | 4.27% | **0.92%** |
| 광각 90도 | 8.20% | **4.45%** |
| 초광각 120도 | 13.80% | **9.47%** |

부호 규약은 그대로 성립합니다. 거울 반전은 회전벡터를 (w_x, −w_y, −w_z)로,
축을 (−x, y, z)로 바꾸는데 가로·세로가 함께 뒤집혀 기존 규칙이 유지됩니다.
288/288 부호 시험이 그대로 통과해 확인됐습니다.

---

## 4. 두 경로 중 무엇을 먼저 쓸까 — 뒤집었다

회전을 얻는 길은 둘입니다. MediaPipe의 **변환행렬**과 **랜드마크 상대 정합**.
지금까지 행렬을 1순위로 썼는데, 왜곡을 넣고 재 보니 그 경로가 훨씬 약했습니다.

| 렌즈 | 행렬 경로 | 랜드마크 경로 |
|---|---|---|
| 왜곡없음 | 10.40 / 12.21% | 3.85 / 5.41% |
| 일반 65도 | 10.74 / 9.78% | 3.83 / 4.80% |
| 광각 90도 | 12.44 / 16.80% | 4.95 / 9.28% |
| 초광각 120도 | 14.79 / 33.73% | 7.64 / 15.53% |

배치 9종으로도 재 봤는데 **전부 랜드마크가 이겼습니다**(최악은 "위에서 25도 +
광각"으로 행렬 72.55% 대 랜드마크 23.85%).

이유는 구조적입니다. 변환행렬은 **정규 얼굴 모형을 관측에 맞춘 절대 자세**라
렌즈가 상을 휘게 하면 편향이 그대로 들어옵니다. 랜드마크 정합은 **같은 사람의
중립 배치와 지금 배치를 맞대는 상대 정합**이라, 얼굴 근처의 공통된 휨이 양쪽에
똑같이 들어 있어 상쇄됩니다.

### 그런데 바로 뒤집지는 않았습니다

랜드마크 경로에 약점이 하나 나왔습니다. 화면 좌표를 그대로 3차원 점으로 보고
정합하는 탓에, 사람이 옆으로 걸어가면 **원근 단축을 회전으로 오해**합니다.
몸 평행이동 끌림이 0.023~0.045로 한도(0.020)를 넘었습니다. 잡음을 꺼도
0.0198이라 기하 문제였습니다.

그리고 정직하게 적어 둘 것이 있습니다 — 이 비교의 "행렬"은 가상 카메라가
**흉내 낸** 것입니다(정규 모형을 관측에 Kabsch 정합). 실제 MediaPipe는 더
정교할 수 있습니다. 그래서 **증거 없이 뒤집지 않고**, 렌즈를 알아냈을 때만
뒤집도록 했습니다(5절). 모르면 지금까지 검증된 행렬 우선 그대로입니다.

---

## 5. 사람 얼굴을 보정판으로 쓴다

왜곡을 되돌리려면 k1을 알아야 합니다. 그런데 **현장에서 체커보드를 들고 잴
수는 없습니다** — 이 프로젝트의 규칙은 "재지 않는다"입니다.

### 실패한 시도 둘 (기록해 둡니다)

**① 강체 잔차 최소화** — 얼굴은 강체이니 k1을 바꿔 가며 정합 잔차가 최소가
되는 값을 찾으면 될 줄 알았습니다. 잔차 감소가 0~3%였고, 최소점이 참값
반대편(참 −0.30인데 최소 +0.10)이었습니다.

**② 2차원만으로 재시도** — z 잡음에 묻힌 탓인가 싶어 2차원 재투영 잔차로
다시 했습니다. 잔차에 구조는 있었지만(곡률 32~145%) 최소점은 여전히
엉뚱했습니다(참 −0.30, 최소 −0.02). 원근 자체가 잔차를 지배해서, k1을
양수로 키우면 원근 단축을 흉내 내 버립니다.

**두 번 실패했으므로 k1을 추측해 넣지 않았습니다.**

### 성공한 방법 — Zhang 보정, 보정판은 사용자의 얼굴

얼굴은 강체이고, 사용자는 어차피 키오스크 앞에서 움직입니다. **같은 강체를
화면 여러 곳에서 여러 자세로 본 관측**이 쌓이면 그것이 곧 카메라 보정 자료입니다.
`cv2.calibrateCamera`에 얼굴 22점을 보정판으로 넣었습니다.

모든 렌즈에 **틀린 초기값 f=700**으로 출발시켜도 참값 부근으로 수렴했습니다:

| 렌즈 | 참 f / k1 | 복원 f / k1 |
|---|---|---|
| 왜곡없음 | 700 / +0.00 | 721.8 / **+0.012** |
| 일반 65도 | 700 / −0.05 | 721.8 / −0.041 |
| 준광각 78도 | 560 / −0.10 | 582.1 / −0.093 |
| 광각 90도 | 430 / −0.15 | **426.6 / −0.152** |
| 초광각 120도 | 290 / −0.30 | 300.3 / −0.271 |

없는 왜곡을 지어내지 않는다는 것(왜곡없음 → +0.012)이 특히 중요합니다.

**2모수(f, k1)가 3모수(f, k1, k2)보다 정확했습니다** — k2까지 풀면 둘이 서로를
상쇄합니다(참 −0.30일 때 2모수 −0.284, 3모수 −0.236).

### f가 더 크게 듣는다

k1보다 **f**가 더 큰 일을 했습니다. 핀홀 모형에서 u = f·X/Z + cx 이고
MediaPipe의 z는 f·dZ/Z0에 해당하므로

    X 는 (u − cx) · (1 + z/f) 에 비례한다

즉 중심을 뺀 좌표에 (1 + z/f)를 곱하면 **원근이 풀리고 4절의 약점이
사라집니다.** 그리고 놀랍게도 f에 둔감합니다:

| 배치 | 되돌리기 전 | 참 f | f가 20% 틀림 | f가 40% 틀림 |
|---|---|---|---|---|
| 정면 | 0.0230 | 0.0062 | 0.0065 | 0.0088 |
| 밑에서 35도 | 0.0384 | 0.0059 | 0.0064 | 0.0107 |
| 심하게 비스듬히 | 0.0449 | 0.0078 | 0.0115 | 0.0161 |

**f가 40% 틀려도 안 고친 것보다 훨씬 낫습니다.** 정확한 f가 필요한 게 아니라
대략만 맞으면 되므로 자가 보정으로 충분합니다.

---

## 6. 안전장치 — 틀린 값을 쓰느니 안 쓴다

자가 보정은 **사용자가 충분히 움직여야** 성립합니다. 한 자리에 붙어 서 있으면
왜곡의 증거가 없고, 그때 나오는 값은 부호까지 틀립니다.

뷰 80개를 반으로 갈라 따로 보정해 봤습니다:

| 움직임 | 전반 k1 | 후반 k1 | 차이 | 참값 |
|---|---|---|---|---|
| ±60mm | −0.413 | −0.205 | **0.208** | −0.30 |
| ±120mm | −0.295 | −0.266 | 0.029 | −0.30 |
| ±200mm | −0.279 | −0.279 | 0.001 | −0.30 |

**교차검증 하나가 못 믿을 경우를 전부 걸러냅니다.** 문턱 0.06으로 깨끗이
갈립니다. 여기에 더해 뷰 개수, 얼굴이 화면에서 퍼진 정도(비싼 계산 **전에**
보는 사전 게이트), 재투영 오차, 값의 물리적 범위, 시도 횟수 상한을 겁니다.

거울 여부는 **랜드마크에서 직접 읽습니다** — 반전이 없으면 왼눈 바깥(33)이
오른눈 바깥(263)보다 화면 왼쪽에 찍힙니다. 설정에 기대면 설정 한 줄로
깨지는데, 8/31에 실제로 그랬기 때문입니다.

---

## 7. 프레임 — 얹은 비용과 그것을 줄인 방법

| 단계 | 중앙 | p95 | 최대 |
|---|---|---|---|
| pointing_offset (렌즈 없음) | 0.026ms | 0.066ms | 0.371ms |
| pointing_offset (렌즈 보정 포함) | 0.188ms | 0.392ms | 0.919ms |
| 보정기 뷰 수집 | 0.056ms | 0.088ms | 0.538ms |

프레임당 **+0.22ms**입니다. 추론 예산 43ms의 0.5%라 무시할 만합니다.

문제는 보정 계산이었습니다. 60뷰 기준 한 번에 60~81ms이고 교차검증까지 세 번
부르므로, 그대로 두면 200ms 가까이 CPU를 붙잡습니다. 추론 대역 부하로 재 보니
최대 지연이 6.78ms에서 **109ms**로 뛰었습니다.

두 가지로 줄였습니다.

  · **스레드 우선순위를 낮춥니다** — 보정은 몇 초 늦어도 되지만 커서가
    100ms 멈추면 사용자가 느낍니다. 최대 지연 136ms → 77ms.
  · **세 번의 보정 사이에 숨을 돌립니다** — 총 시간은 같지만 한 번에 오래
    붙잡지 않습니다.

`cv2.setNumThreads(1)`로 단일 스레드화도 재 봤는데 **오히려 나빴고**(최대
52ms 대 99ms) 전역 설정이라 다른 스레드까지 건드려서 채택하지 않았습니다.

그리고 실제로는 상시 부담이 아닙니다 — 가상 카메라 흐름에서 **27.8초 만에
1회 실행하고 채택한 뒤 다시 돌지 않았습니다.**

---

## 8. 최종 결과

### 렌즈별 (얼굴이 화면 옆+위)

| 렌즈 | 전: 가로/세로/끌림 | 후: 가로/세로/끌림 |
|---|---|---|
| 왜곡없음 | 0.14 / 4.34 / 0.0000 | 1.52 / 4.92 / 0.0056 |
| 일반 65도 | 10.74 / 9.78 / 0.0355 | **1.55 / 4.92 / 0.0056** |
| 준광각 78도 | 11.48 / 10.60 / 0.0367 | **1.96 / 6.30 / 0.0070** |
| 광각 90도 | 12.44 / 16.80 / 0.0383 | **2.53 / 9.11 / 0.0088** |
| 초광각 120도 | 14.79 / 33.73 / 0.0421 | **4.01 / 12.43 / 0.0133** |

### 배치별 (광각 90도) — 끌림 한도 0.020

| 배치 | 전 | 후 |
|---|---|---|
| 정면 | 12.44 / 16.80 / 0.0383 | 2.53 / 9.11 / 0.0088 |
| 밑에서 20도 | 11.03 / 19.53 / 0.1306 | 2.91 / 9.71 / 0.0105 |
| 밑에서 35도 | 10.33 / 28.44 / 0.1158 | 3.16 / 9.03 / 0.0108 |
| 위에서 25도 | 9.55 / **72.55** / 0.1308 | 3.39 / **6.10** / 0.0051 |
| 왼쪽에서 25도 | 4.81 / 10.63 / 0.1127 | 2.80 / 8.84 / 0.0089 |
| 오른쪽에서 25도 | 8.37 / 54.68 / 0.0978 | 3.42 / 6.60 / 0.0079 |
| 옆으로 기울어짐 20도 | 12.23 / 17.69 / 0.0388 | 2.72 / 8.83 / 0.0109 |
| 비스듬히 | 11.97 / 15.55 / 0.1150 | 2.91 / 8.42 / 0.0119 |
| 심하게 비스듬히 | 4.39 / 10.10 / 0.1079 | 3.65 / 9.38 / 0.0081 |

**최악 끌림 0.1308 → 0.0119. 아홉 배치 전부 한도 안으로 들어왔습니다.**

### 표에서 정직하게 읽어야 할 곳

"왜곡없음"만 전(0.14/4.34/0.0000)이 후(1.52/4.92/0.0056)보다 좋아 보입니다.
그 "전"은 가상 카메라가 **정답 행렬을 그대로 준** 값입니다 — 실제 MediaPipe로는
나올 수 없는 수치입니다. 같은 조건(둘 다 관측에서 나옴)으로 맞춰 재면
행렬 10.40/12.21% 대 랜드마크 3.85/5.41%로 뒤집힙니다.

---

## 9. 검증

| 항목 | 결과 |
|---|---|
| 테스트 | **673개** 전부 통과 (580 → 673, 신규 93개) |
| 구문 | 105개 파일, 오류 0 |
| 진입점 임포트 | head/eyebrow/forehead/main/main_dpad + 신규 모듈 OK |
| 정적 검사 (pyflakes) | **0건** — 안 쓰는 임포트 7개도 정리 |
| 부호 방향 | 렌즈 5종 × 거울 2종, 렌즈 5종 × 배치 9종 전부 통과 |
| 시험의 자기 검사 | 왜곡을 넣으면 관측이 실제로 달라지는지 확인(2px 이상) |

신규 시험 93개 중 절반은 **"언제 거부하는가"** 를 겁니다 — 움직임이 부족할 때,
뷰가 모자랄 때, 말이 안 되는 입력이 올 때, 무한정 CPU를 태우려 할 때.
자가 보정은 틀린 값을 쓰면 안 쓰느니만 못하기 때문입니다.

### 오늘 추가된 논문

  · **Conrady, A.E. (1919).** "Decentred Lens-Systems." *MNRAS* 79(5), 384-390.
  · **Brown, D.C. (1966).** "Decentering Distortion of Lenses."
    *Photogrammetric Engineering* 32(3), 444-462.
  · **Zhang, Z. (2000).** "A Flexible New Technique for Camera Calibration."
    *IEEE TPAMI* 22(11), 1330-1334.

---

## 오늘의 교훈

**통과하는 시험이 무언가를 걸고 있다는 뜻은 아닙니다.** 어제까지 "광각에서도
된다"를 초점거리 400~1400px로 확인했다고 믿었는데, 그 축은 상대 회전 매핑이
원리적으로 면역인 축이었습니다. 무엇을 넣어도 통과할 시험이었던 겁니다.
그래서 오늘 시험에 **"왜곡을 넣으면 관측이 실제로 달라지는가"** 를 확인하는
시험을 따로 넣었습니다 — 시험이 스스로를 검사하게.

**그리고 두 번 실패했으면 추측하지 말아야 합니다.** k1을 잔차 최소화로 찾으려다
두 번 실패했는데, 그때 "대충 −0.15쯤 넣자"로 갔으면 왜곡 없는 카메라에 없는
왜곡을 만들어 넣었을 겁니다. 방법을 바꿔(Zhang 보정) 제대로 재는 쪽이
답이었고, 그렇게 얻은 값은 왜곡 없는 렌즈를 +0.012로 정확히 알아봤습니다.

---

# — 오후: 어제 넣은 것을 의심하고 다시 검증 —

오전에 렌즈 자가 보정을 올리고 나서, 시험 28개가 **전부 순환 논리**였다는
것을 알아챘습니다.

가상 카메라는 `FACE_MODEL`로 얼굴을 만들고, 렌즈 보정은 **똑같은**
`CANONICAL_FACE`로 그것을 맞췄습니다. 두 상수가 같은 얼굴이었습니다. 내가
만든 얼굴을 내가 맞히는 셈이라, **"사람마다 얼굴이 다르면?"** 이라는 질문에
아무 답도 못 하고 있었습니다.

렌즈 보정은 **사용자의 얼굴을 보정판으로 삼는데**, 실제 사람 얼굴은
제각각입니다. 눈이 넓거나 코가 낮으면 **치수가 틀린 자로 재는 셈**입니다.

---

## 10. 인체 계측 범위의 얼굴 10종을 만들었다

`tests/virtual_camera.py`에 `varied_face()`와 `FACE_VARIANTS`를 넣어, 정규
모형과 **다른** 얼굴을 만들 수 있게 했습니다.

    안구간거리  55~70mm  (평균 63) -> 0.87 ~ 1.11
    얼굴 폭     +-10%
    코 높이     15~30mm  -> 0.6 ~ 1.2
    전체 크기   어린이~큰 어른  0.85 ~ 1.15

## 11. 두 가지가 드러났다

**하나 — f는 크게 틀릴 수 있는데, 그래도 원근 되돌리기는 듣는다.**

갸름한 얼굴에서 f가 −64%, 코 낮은 얼굴에서 +302% 틀렸습니다. 그런데도
**10종 전부에서 커서가 좋아졌습니다.** 끌림이 0.0429에서 0.0367로,
0.0282에서 0.0173으로. 원근 되돌리기는 f에 이토록 둔감합니다.

**둘 — f가 크게 틀린 채로 k1을 얹으면 파국이다.**

| 얼굴 | 보정 없음 (세로) | k1까지 적용 (세로) |
|---|---|---|
| 갸름한 얼굴 | 15.10% | **83.89%** |

다섯 배 넘게 나빠졌습니다. 그리고 **그때 k1의 교차검증은 통과했습니다**
(차이 0.026 < 문턱 0.06). k1만 보고 f를 안 보면 이런 경우를 놓칩니다.
어제 올린 코드의 실제 구멍이었습니다.

## 12. 초점거리 타당성 게이트를 넣었다

f의 "말이 되는 범위"를 **절대 픽셀이 아니라 화면 높이에 대한 비율**로
잡았습니다. 해상도가 바뀌어도 그대로 성립해야 하기 때문입니다.

    0.30H ~ 2.50H   (화면 높이 720이면 216~1800px, 세로 화각 약 22~118도)

그리고 f의 교차검증도 따로 봅니다. 실측에서 두 무리가 뚜렷이 갈렸습니다 —
**믿을 만한 경우 1.8~5.9%, 못 믿을 경우 13~182%.** 문턱을 그 사이(10%)에
두었습니다.

## 13. 그리고 k1을 기본으로 껐다

f 게이트를 넣고도 갸름한 얼굴에서 세로가 83.89%로 튀는 경우가 남았습니다.
"정규 얼굴에 가까운가"를 가려낼 지표를 찾아 나섰습니다.

**재투영 오차(RMS)가 잘 갈라내는 듯했습니다:**

    정규 모형에 가까운 얼굴   0.467 ~ 0.509px
    먼 얼굴                  0.619 ~ 1.621px
    진짜 광각 렌즈(정규 얼굴) 0.465 ~ 0.470px

깨끗한 분리라 이걸로 게이트를 만들려 했는데, **랜드마크 잡음을 2배로 올리자
두 무리가 겹쳤습니다**(정규 최대 0.0249 대 비정규 0.017~0.031, 얼굴 크기로
정규화한 값). 잡음 수준에 묶인 문턱은 현장에서 못 믿습니다.

**가려낼 수 없으면 안 쓰는 쪽이 맞습니다.**

| 조건 | 원근만 (f) | 원근+왜곡 (f, k1) |
|---|---|---|
| 정규 얼굴 · 초광각 120도 | 4.28 / 15.48 / 0.0143 | 5.69 / 7.87 / 0.0199 |
| 정규 얼굴 · 광각 90도 | 2.75 / 8.73 / 0.0093 | 3.83 / 5.32 / 0.0129 |
| 갸름한 얼굴 · 광각 90도 | 안전 | **세로 83.89%** |

k1은 정규 얼굴이면 세로 휨을 절반으로 줄여 주지만, 얼굴이 다르면 다섯 배로
악화시킵니다. **f만으로도 가장 큰 몫인 끌림이 잡히고, 이쪽은 10종 전부에서
안전했습니다.**

기본을 끄고, 카메라를 아는 배포처를 위해 설정
(`orientation_lens_distortion`)으로 켤 수 있게 남겼습니다.

## 14. 재현성 — 같은 입력에 같은 결과

보정을 딴 스레드로 돌리니 그동안에도 뷰가 쌓여서, 다음 시도가 몇 개를 가지고
시작할지가 **스레드 타이밍에 좌우**됐습니다. 같은 입력에 다른 결과가 나오면
시험도 못 믿고 현장 문제도 재현할 수 없습니다.

시도 시점을 **지금까지 본 뷰의 총수**로 미리 정했습니다(총 60개째, 80개째...).
이 총수는 스레드가 언제 끝나든 달라지지 않습니다.

중간에 한 번 잘못 갔습니다 — "앞에서 n개만 쓴다"로 했더니 앞부분은 사람이
아직 안 움직인 구간이라 **10종 전부가 "움직임 부족"으로 거부**됐습니다.
시도 **시점**만 고정하고 묶음은 최근 것 전체를 쓰는 것이 맞았습니다.

3회 반복해서 채택된 값이 모두 같은 것을 확인했습니다.

## 15. main.py가 이 기능을 못 받고 있었다

`configs/config.yaml`의 `orientation_mapping`이 `false`였습니다.
head/eyebrow/forehead 세 진입점은 코드에서 이미 `true`로 켜고 있었는데,
**main.py 계열만 옛 방식(감도 + 곡률 상수)으로 돌고 있었습니다.**

옛 방식은 카메라 배치마다 값을 다시 재야 하므로 "재지 않는다"는 원칙과
어긋납니다. `true`로 바꿨습니다.

## 16. 논문 설명 문서

`docs/PAPERS.md`를 새로 썼습니다. 지금까지 인용한 논문 10편이 **어떤
논문인지 / 무슨 문제를 푸는지 / 이 프로젝트 어디에 쓰였는지**를 적었고,
**f(초점거리)와 k1(방사 왜곡 계수)이 무엇인지**도 풀어 썼습니다.

"검토했지만 안 쓴 것" 표도 함께 넣었습니다 — 접선 왜곡, k2·k3, 강체 잔차로
k1 추정(두 번 실패), 보정판 개인화(시제품이 발산), DESP 예측 필터,
cv2.setNumThreads(1). **반쪽짜리로 넣지 않은 것이 그 표의 요점입니다.**

---

## 17. 오후 검증

| 항목 | 결과 |
|---|---|
| 테스트 | **705개** 전부 통과 (오전 673 → 705, 신규 32개) |
| 구문 | 106개 파일, 오류 0 |
| 정적 검사 (pyflakes) | 0건 |
| 진입점 임포트 | head/eyebrow/forehead/main/main_dpad OK |
| 얼굴 변이 | 10종 × "나빠지지 않는가" 전부 통과 |
| 재현성 | 같은 입력 3회 반복, 채택값 동일 |

신규 시험의 핵심은 **`test_never_makes_the_cursor_worse_for_any_face`** 입니다.
보정은 사용자가 고를 수 있는 것이 아니라 저절로 켜지므로, **어떤 사람에게도
손해가 되어서는 안 됩니다.** 정확도가 아니라 안전을 지키는 시험입니다.

그리고 시험이 스스로를 검사하게 했습니다 — "얼굴 변이가 실제로 다른
얼굴인가"를 확인하는 시험을 따로 두었습니다. 처음에는 2차원 좌표로 비교했다가
**코 높이 변이가 0.2~0.4px밖에 안 나와** 놓칠 뻔했습니다(코는 z만 바꿉니다).
3차원으로 봐야 했습니다.

---

## 오후에 논의한 것 — 우클릭 (아직 구현 안 함)

우클릭을 어떤 신호로 받을지 논의했고, **구현은 하지 않기로 했습니다.**
오간 내용을 남겨 둡니다.

**신호 지도** — 입은 포화 상태입니다.

| 신호 | 지금 쓰임 |
|---|---|
| jawOpen 입 벌림 | 좌클릭 + 유지하면 드래그 |
| mouthPucker 입 오므림 | 재중심 |
| eyeBlink | 깜빡임 게이트 |
| 드웰(응시) | select 1.5초 |
| **눈썹·볼·미소 등 나머지 48개 blendshape** | **안 씀** |

**접은 후보들**: 눈썹 올리기(앞머리에 가림), 볼 부풀리기(실기에서 안 됨),
미소(무의식적으로 나와 무장이 오발하면 좌클릭이 우클릭으로 바뀜),
윙크(한쪽만 감기가 어렵고 깜빡임 게이트와 충돌), 고개 흔들기(겨눈 곳을 잃음).

**찾은 빈 축** — 커서 매핑은 회전벡터의 세 성분 중 **둘만 씁니다.**

    horizontal = -dot(w, y축)    # 좌우
    vertical   =  dot(w, x축)    # 상하
                                 # dot(w, z축) = 고개 갸웃(roll) -> 버려짐

즉 **고개를 갸웃해도 커서가 수학적으로 정확히 0만큼 움직입니다.** 겨눈 곳을
유지한 채 신호를 낼 수 있고, 이미 계산 중이라 비용도 0입니다.

**그런데 우클릭보다 큰 구멍을 찾았습니다.**

`orientation_half_span_x_deg: 15.0`이 **고정**이고, 사람의 실제 가동범위를
재서 넣는 코드가 **어디에도 없습니다.** 바꾸는 API는 있는데 호출부가 없습니다.

목을 7도밖에 못 돌리는 분은 **커서가 화면 절반까지만 갑니다.** 화면 가장자리에
영원히 못 닿습니다. 우클릭 이전에 기본 기능의 문제이고, 가장자리 버튼 같은
해법을 통째로 무력화합니다.

**마비가 있는 사용자에 대해 확인한 것 두 가지:**

  · **처음부터 고개가 기울어 있는 경우** — 중립 캘리브레이션이 그 사람의
    랜드마크로 좌표축을 만들므로 대체로 흡수됩니다. 다만 회전축이 "머리 축"이
    아니라 "중력 축"이면 좌우 이동의 sin(기울기)만큼이 세로로 샙니다
    (20도 기울면 34%). 카메라가 기운 것과 사람이 기운 것을 구분할 수 없어
    실측이 필요합니다.
  · **떨림** — 커서는 1€ 필터가 잡습니다. 문턱 제스처는 **주파수로 갈라낼 수
    있습니다**(병적 떨림 4~12Hz 대 의도적 머리 동작 2Hz 이하). "N밀리초 이상
    유지"를 요구하면 떨림은 원리적으로 못 넘습니다.

**다음에 할 일로 남긴 순서** (오늘은 넣지 않음):

  1. 시작할 때 3초 "둘러보기" — 가동범위·이동 축 방향·떨림 크기를 한 번에
  2. `half_span` 자동 적응 — 이게 있어야 가장자리 버튼이 누구에게나 닿는다
  3. 가장자리 무장 버튼 + 우클릭 발사 (드래그 중에는 죽는 규칙 포함)
  4. 고개 갸웃 무장 — 설정으로, 목 측굴이 되는 분만

---

## 오후의 교훈

**내가 방금 넣은 것을 가장 먼저 의심해야 합니다.** 오전에 시험 28개를 붙여
놓고 "검증했다"고 적었는데, 그 28개가 전부 같은 상수를 양쪽에서 쓰는 순환
논리였습니다. 시험 개수는 안심의 근거가 못 됩니다.

**그리고 좋아 보이는 지표를 한 조건에서만 확인하고 채택하면 안 됩니다.**
재투영 오차가 정규 얼굴과 비정규 얼굴을 0.51 대 0.62로 깨끗이 갈라내길래
게이트로 쓰려 했는데, 잡음을 2배로 올리자 겹쳤습니다. 잡음은 조명·거리·
카메라마다 달라지는 값이라, 그대로 넣었으면 현장에서 조용히 틀렸을 겁니다.
"**가려낼 수 없으면 안 쓴다**"로 물러선 것이 오늘 가장 잘한 판단입니다.

---

## 18. 올린 직후 간헐 실패를 발견했다

오후 작업을 깃허브에 올린 **직후** 전체 스위트를 다시 돌렸더니 하나가
실패했습니다. 개별로 돌리면 6회 중 6회 통과하는데, 전체에서는 4회 중 1회
실패했습니다.

원인이 둘이었습니다.

**하나 — 잡음 난수가 진행된 채로 재고 있었습니다.** 한 카메라로 가로·세로를
먼저 재고 그 다음 끌림을 재는데, 앞 항목의 호출 횟수가 뒤 항목의 잡음을
바꿉니다. VirtualCamera.reset_noise() 를 넣어 항목마다 되감게 했습니다.

**둘 — 보정 스레드가 도는 동안에도 뷰가 쌓였습니다.** 다음 시도가 무엇을
보는지가 기계 부하에 좌우돼 초점거리가 418~424px로 흔들렸습니다. 시험
도우미가 시도마다 스레드를 기다리게 했습니다(실사용은 그대로 안 기다립니다).

**그리고 이 실패가 알려 준 진짜 사실이 있습니다.**

    "오른쪽에서 25도 + 광각 90도" 끌림 = 0.0184   (한도 0.020, 여유 8%)

k1을 기본으로 끄면서 이 배치가 가장 빠듯해졌습니다 — k1을 켜면 0.0079였으니
두 배 이상 벌어집니다. 한도 안이지만 **여유가 얇습니다.** 안전을 위해 k1을
끈 대가가 여기에 드러나 있습니다.

간헐 실패가 아니었으면 이 사실을 못 봤을 겁니다. 시험이 흔들린 덕에
"어디가 가장 빠듯한가"를 알게 됐습니다.
