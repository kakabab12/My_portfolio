# 폴더별 프로젝트 정리

`E:\작업\광명테크 인턴.all` 안의 폴더가 각각 무엇인지, 언제 작업한 것인지.

---

## 계보 — 한눈에

```
GMtech_project (팀 저장소)          ← 손 제스처 본체, 다른 팀원이 관리
        │
        ├─ 프로토타입_1 (CPU/GPU)   ← 초기 갈래
        ├─ 주 목표/OpenVINO.ver     ← 추론 엔진 확정판
        ├─ 헤드트래커_프로토타입_win.ver ← 내가 헤드트래커를 처음 만든 곳
        ├─ 헤드트래커_프로토타입_rpi.ver ← 라즈베리파이 이식
        ├─ GMtech_project2          ← 손끝 방향판(D-pad)
        └─ 리모콘 ui (gesture_kiosk) ← 지금 작업 중인 최종본
```

---

## 각 폴더

### `GMtech_project` — 팀 본체
- **무엇** 손 제스처 키오스크 본체. 다른 팀원이 관리하는 저장소.
- **내 역할** 이 위에 헤드트래커를 얹었습니다.
- **작업 시기** 7/23 ~ 8/4
- **기록** 커밋 19건 (7/06, 7/08, 7/13, 8/03)
- **주요 작업** 델파이 7 연동 시험, OpenVINO 전환, 실기 보정, 경량화,
  헤드트래커 통합
- ⚠ 이 저장소를 새로 받을 때마다 **내 헤드트래커 작업이 덮어써졌다** (7/31)

### `프로토타입_1` — CPU판 / GPU판
- **무엇** 그래픽카드 있는 판과 없는 판을 나눠 만든 초기 갈래
- **왜** 키오스크에는 별도 그래픽카드가 없다 (7/13)
- **파일 수** 6만 3천여 개 (라이브러리 포함)

### `주 목표/OpenVINO.ver` — 추론 엔진 확정판
- **무엇** 인텔 CPU + 내장그래픽에서 돌리는 최종 추론 구성
- **왜 OpenVINO** 키오스크에 인텔 칩만 있어서, CPU와 내장그래픽을
  같이 굴리는 데 이게 맞다 (7/27)
- **작업 시기** 7/06 ~ 7/24
- **기록** 커밋 75건 — 이 프로젝트에서 **가장 기록이 촘촘한 폴더**
- **주요 작업** 손끝 추적 3단 폴백, 어깨너비 정규화, 델파이 7 UDP→웹소켓,
  INT8 양자화(나중에 제거), 윈도우 통합판(GPU 자동 감지)

### `헤드트래커_프로토타입_win.ver` — 헤드트래커 출발점
- **무엇** 손동작 대신 **머리로 조작**하는 방식을 처음 만든 곳
- **작업 시기** 7/18 ~ 7/23 (+ 8/3~8/4 보고서)
- **여기서 나온 것**
  - 코끝 커서 / 입 벌리기 / 응시 / 눈 감기
  - 눈썹 올리기·볼 부풀리기·코 찡그리기·입 오므리기 **폐기**
  - 아이트래킹 모드 (상하 이동이 안 돼 8/4 폐기)
- **포함** `run.bat`, `install.bat`, `make_offline_bundle.bat`

### `헤드트래커_프로토타입_rpi.ver` — 라즈베리파이 이식
- **무엇** RPi5 8GB용 포팅
- **작업 시기** 7/22
- **막힌 곳** 파이썬 3.13.5 → 3.11.5 강제, `install.sh` 휠 설치 실패
- **포함** `run.sh`, `install.sh`, `설명서.md`, `설치가이드.md`
- **결론** 발열 문제가 지적돼 주력에서 빠짐

### `GMtech_project2` — 손끝 방향판(D-pad)
- **무엇** 화면에 방향판을 띄우고 **손끝이 닿으면** 함수가 나가는 방식
- **왜** 팔 쓸기 방식은 **복귀 동작이 반대 방향으로 오인**돼 오탐이 많았습니다
- **작업 시기** 8/4 ~ 8/6
- **기록** 커밋 81건
- **여기서 나온 것** — **오버레이(투명 화면) 구조**.
  방향판 자체는 회사 요청으로 폐기됐지만, 바탕화면 위에 투명하게 얹혀
  실제 마우스를 움직이는 이 구조가 **지금 헤드트래커의 형태**가 됐습니다

### `광명테크/gesture_model` — 모델 파일
- 학습·추론에 쓰는 모델과 가상환경

### `광명테크 ppt 일지 자료`
- 업무일지(hwp), 인턴 계획서, 주간별 계획서, 캡처·녹화 자료

### `gesture_kiosk_backup_2026-07-30.zip`
- 7/30 시점 백업 (4.1 GB)

### `리모콘 ui` (현재 작업, `E:\작업\리모콘 ui`)
- **무엇** 헤드트래커 최종본. `head.py`(코끝) / `eyebrow.py`(미간)
- **작업 시기** 8/13 ~ 현재
